OpenAI Codex v0.104.0 (research preview)
--------
workdir: /home/stephen-reilly/dev/bach_gen
model: gpt-5.3-codex
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: high
reasoning summaries: auto
session id: 019c8f5b-86fd-7370-91a5-184eee415214
--------
user
You are working in this repo.

Read:
- AGENTS.md
- SPEC.md
- TODO.md
- PROGRESS.md
- docs/exec/EXECPLAN_stage1.md
- docs/skills/python-test-env/SKILL.md
- .codex/loop_runs/20260224_030212/context_3.md (current context snapshot)

Before coding, run:
- bash docs/skills/python-test-env/scripts/run_tests.sh --check

Do the next unchecked item in docs/exec/EXECPLAN_stage1.md.
Keep TODO.md in sync.
Run the verify command mentioned in AGENTS.md (or reuse the failing one from the context).
Update TODO.md (check completed items) and append notes to PROGRESS.md.

Your final message MUST be valid JSON matching loop_output.schema.json.
If you made no progress, set progress_made=false and explain why in summary.

mcp: chrome-devtools starting
mcp: chrome-devtools ready
mcp startup: ready: chrome-devtools

thinking
**Planning staged execution with parallel reads**
codex
{"done":false,"progress_made":true,"summary":"I’m starting by loading the requested docs and context, then I’ll run the `--check` test helper to confirm the environment before implementing the next unchecked Stage 1 item.","tests_ran":false,"tests_status":"not_run","next_step":"Read AGENTS/SPEC/TODO/PROGRESS/EXECPLAN/SKILL/context files and identify the next unchecked task.","stop_reason":""}

thinking
**Planning parallel file reads**
exec
/bin/bash -lc "sed -n '1,320p' docs/exec/EXECPLAN_stage1.md" in /home/stephen-reilly/dev/bach_genexec
/bin/bash -lc "sed -n '1,260p' SPEC.md" in /home/stephen-reilly/dev/bach_genexec
/bin/bash -lc "sed -n '1,260p' TODO.md" in /home/stephen-reilly/dev/bach_genexec
/bin/bash -lc "sed -n '1,260p' PROGRESS.md" in /home/stephen-reilly/dev/bach_genexec
/bin/bash -lc "sed -n '1,220p' AGENTS.md" in /home/stephen-reilly/dev/bach_genexec
/bin/bash -lc "sed -n '1,320p' .codex/loop_runs/20260224_030212/context_3.md" in /home/stephen-reilly/dev/bach_genexec
/bin/bash -lc "sed -n '1,260p' docs/skills/python-test-env/SKILL.md" in /home/stephen-reilly/dev/bach_gen succeeded in 50ms:
# SPEC — Stage-1 Tokenizer & Dataset (Condensed)

## Goal
Implement the Stage-1 symbolic event stream and dataset builder for Bach-style polyphony on a fixed 24 TPQ grid.

## Core Rules
- TPQ is fixed at 24.
- Events are sparse: emit `POS_t` only where at least one voice event starts.
- Per-voice continuity is tracked across barlines by `prev_pitch` and `active_until`.
- Absolute pitch truth is `ABS_VOICE_*` + `MEL_INT12_*`.
- `HARM_*` tokens are derived from the lowest active reference pitch at time t.

## Event Schema (v1)
Structural:
- `BAR`, `TIME_SIG_*`, `KEY_*`, `TEMPO_*`, `POS_*`

Voice events:
- `VOICE_{0..K-1}`, `DUR_{ticks}`, optional `REST_{ticks}`

Anchors:
 succeeded in 51ms:
# ExecPlan — Stage-1 Tokenizer & Dataset

## Rules
- One task per run.
- Run `bash docs/skills/python-test-env/scripts/run_tests.sh` and do not weaken tests.
- If you add a new feature without tests, add tests in the same run.
- Do not hard-code tests; test real behavior.
- If tests fail, fix the underlying code issue before proceeding.
- Update `TODO.md` and append to `PROGRESS.md`.

## Checklist (one item per loop)
- [x] Define EventSpec + DescriptorSpec in `src/tokens/schema.py` with version `remi_tab_v1` and `tpq=24`.
- [x] Implement interval math helpers (melodic + harmonic ref) with QA vs production behavior.
- [ ] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering.
- [ ] Add round-trip tests for pitch reconstruction and `HARM_*` consistency.
- [ ] Implement `scripts/make_dataset.py` basic pipeline (parse, normalize, collapse doublings, track assignment, tokenize, stats).
- [ ] Produce dataset artifacts in `data/processed/` and document counts in `stats.json`.
- [ ] Verify `bash docs/skills/python-test-env/scripts/run_tests.sh` passes.
 succeeded in 51ms:
# Context (iter 3) — 2026-02-24T03:13:37-08:00

## Git status
## codex-loop-20260224-030212
?? .codex/loop_runs/20260224_030212/context_3.md

## Diffstat

## Verify (may fail)
...............s......................................                   [100%]
53 passed, 1 skipped in 46.98s

 succeeded in 51ms:
# TODO

## Milestone: Stage-1 Tokenizer & Dataset
- [x] Implement `src/tokens/schema.py` (EventSpec + DescriptorSpec, versioned)
- [x] Implement tokenizer interval math + round-trip tests
- [ ] Implement `scripts/make_dataset.py` basic pipeline
- [ ] Produce `data/processed/{events.parquet,barplans.parquet,stats.json}`
- [ ] CI: `pytest -q` passes
 succeeded in 51ms:
# AGENTS.md — bach-gen

## How to run
- Preferred runtime: `conda run -n bach python`
- Optional venv fallback: `python -m venv .venv && source .venv/bin/activate`
- Environment/test helper: `bash docs/skills/python-test-env/scripts/run_tests.sh --check`
- Tests: `bash docs/skills/python-test-env/scripts/run_tests.sh`

## Rules
- Do one task per run.
- Prefer small diffs; don't refactor unrelated code.
- Always run tests for touched areas (use `bash docs/skills/python-test-env/scripts/run_tests.sh`).
- Don't weaken or remove tests to make them pass.
- If you add a new feature without tests, add tests in the same run.
- Do not hard-code tests; test real behavior.
- If tests fail, find and fix the underlying code issue before proceeding.
- Update `TODO.md` when items are completed.
- Append a short entry to `PROGRESS.md` for each change set.
- Don't add heavy deps without asking.
 succeeded in 51ms:
---
name: python-test-env
description: Ensure deterministic Python test execution in this repo by selecting a valid runtime (`conda` env `bach` first, then `.venv`) and running pytest with dependency checks. Use when preparing to run tests, when a loop run needs a stable verify command, or when failures may be caused by environment mismatch.
---

# Python Test Env

## Workflow

1. Run `bash docs/skills/python-test-env/scripts/run_tests.sh --check`.
2. If check passes, use the printed verify command for all test runs.
3. If check fails, fix environment selection first before code changes.
4. Run `bash docs/skills/python-test-env/scripts/run_tests.sh` to execute tests.

## Environment Selection Rules

- Prefer `conda` env `bach` when available.
- Fall back to `.venv` only if `.venv/bin/python` is executable.
- Reject broken `.venv` layouts (for example zero-byte `python` launcher files).
- Verify imports: `pytest`, `torch`, `music21`, `pandas`, `pyarrow`.
 succeeded in 51ms:
# PROGRESS

- 2026-02-24: Initialized AGENTS.md, SPEC.md, TODO.md, and PROGRESS.md for Stage-1 loop setup.
- 2026-02-24: Added ExecPlan, Codex loop script, config, and schema for structured loop output.
- 2026-02-24: Added python-test-env skill and test helper script; loop now defaults to conda env `bach` and includes environment precheck in prompt.
- 2026-02-24: Fixed loop compatibility with installed Codex CLI (removed hard dependency on `-a`, added auto-detect for approval flags, and fail-fast on CLI usage errors).
- 2026-02-24: Updated loop to use `--full-auto` on newer Codex CLI builds where explicit approval flags are unavailable.
- 2026-02-24: Completed Stage-1 ExecPlan item 1 by defining versioned `EventSpec`/`DescriptorSpec` in `src/tokens/schema.py` (`remi_tab_v1`, `tpq=24`) and added `tests/test_schema.py`.
- 2026-02-24: Completed Stage-1 ExecPlan item 2 by adding `src/tokens/intervals.py` (melodic/harmonic helpers with QA vs production behavior), wiring repairs into `eventize_musicxml`, updating harmonic validation to shared math helpers, and adding `tests/test_intervals.py`; verify run: `53 passed, 1 skipped`.

thinking
**Running precheck before coding**
codex
{"done":false,"progress_made":true,"summary":"I identified the next unchecked ExecPlan item: implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering. I’ll run the required environment precheck now before making edits.","tests_ran":false,"tests_status":"not_run","next_step":"Run `bash docs/skills/python-test-env/scripts/run_tests.sh --check`.","stop_reason":""}
exec
/bin/bash -lc 'bash docs/skills/python-test-env/scripts/run_tests.sh --check' in /home/stephen-reilly/dev/bach_gen succeeded in 1.74s:
Environment check passed.
Using verify command: CONDA_NO_PLUGINS=true conda run -n bach python -m pytest -q

thinking
**Identifying tokenizer file structure**
codex
{"done":false,"progress_made":true,"summary":"Environment precheck passed and confirmed verify command uses `conda run -n bach ... pytest -q`. Next I’m inspecting tokenizer and related tests to implement serialization/parsing with canonical token order.","tests_ran":false,"tests_status":"not_run","next_step":"Read `src/tokens/tokenizer.py` and existing token tests to scope minimal changes.","stop_reason":""}
exec
/bin/bash -lc "rg --files src tests scripts | sed -n '1,240p'" in /home/stephen-reilly/dev/bach_genexec
/bin/bash -lc "sed -n '1,320p' src/tokens/tokenizer.py" in /home/stephen-reilly/dev/bach_genexec
/bin/bash -lc "sed -n '1,320p' src/tokens/schema.py" in /home/stephen-reilly/dev/bach_genexec
/bin/bash -lc "sed -n '1,320p' tests/test_schema.py" in /home/stephen-reilly/dev/bach_genexec
/bin/bash -lc "sed -n '1,320p' tests/test_intervals.py" in /home/stephen-reilly/dev/bach_gen succeeded in 50ms:
scripts/eventize_piece.py
scripts/make_dataset.py
scripts/run_imslp_omr_and_timesigs.ps1
scripts/train_v1.py
scripts/extract_time_signatures.py
scripts/compare_imslp_tobis_timesigs.py
scripts/ralph_codex.sh
scripts/build_vocab.py
tests/test_intervals.py
tests/test_model_integration.py
tests/test_tokens.py
tests/test_schema.py
tests/test_descriptors.py
src/tokens/validator.py
src/tokens/eventizer.py
src/tokens/intervals.py
src/tokens/schema.py
src/tokens/roundtrip.py
src/tokens/__init__.py
src/dataio/__init__.py
 exited 2 in 51ms:
sed: can't read src/tokens/tokenizer.py: No such file or directory
 succeeded in 51ms:
from dataclasses import dataclass, field
from typing import Dict, List, Optional

SCHEMA_VERSION = "remi_tab_v1"
STAGE1_TPQ = 24


def _default_event_tokens() -> Dict[str, List[str]]:
    return {
        "structural": [
            "BAR",
            "TIME_SIG_*",
            "KEY_*",
            "TEMPO_*",
            "POS_*",
        ],
        "voice_events": [
            "VOICE_{0..K-1}",
            "DUR_{ticks}",
            "REST_{ticks}",
 succeeded in 50ms:
from pathlib import Path
import sys

import pytest

sys.path.insert(0, str(Path(__file__).parents[1]))

from src.tokens.schema import DescriptorSpec, EventSpec, SCHEMA_VERSION, STAGE1_TPQ


def test_event_spec_defaults():
    spec = EventSpec()
    assert spec.version == SCHEMA_VERSION
    assert spec.tpq == STAGE1_TPQ

    assert set(spec.tokens) == {
        "structural",
        "voice_events",
        "anchors",
        "intervals",
 succeeded in 51ms:
import sys
from pathlib import Path

import pytest
from music21 import meter, note, stream

sys.path.insert(0, str(Path(__file__).parents[1]))

from src.tokens.eventizer import eventize_musicxml
from src.tokens.intervals import (
    IntervalRepairStats,
    compute_harmonic_interval,
    compute_melodic_interval,
    compute_reference_pitch,
    harmonic_tokens_for_pitch,
)


def test_compute_reference_pitch():
    assert compute_reference_pitch([67, 60, 64]) == 60

thinking
**Inspecting token-related files**
exec
/bin/bash -lc "sed -n '1,360p' src/tokens/eventizer.py" in /home/stephen-reilly/dev/bach_genexec
/bin/bash -lc "sed -n '1,360p' src/tokens/validator.py" in /home/stephen-reilly/dev/bach_genexec
/bin/bash -lc "sed -n '1,360p' tests/test_tokens.py" in /home/stephen-reilly/dev/bach_genexec
/bin/bash -lc "sed -n '1,360p' src/tokens/roundtrip.py" in /home/stephen-reilly/dev/bach_genexec
/bin/bash -lc "sed -n '1,360p' scripts/make_dataset.py" in /home/stephen-reilly/dev/bach_gen succeeded in 51ms:
from dataclasses import dataclass
from pathlib import Path
import re
import warnings
from statistics import median
from typing import Dict, List, Optional, Tuple, Union

import music21
from music21.musicxml.xmlToM21 import MusicXMLWarning

from src.tokens.intervals import (
    IntervalRepairStats,
    compute_reference_pitch,
    compute_melodic_interval,
    format_signed_interval,
    harmonic_tokens_for_pitch,
)

DEFAULT_MAX_VOICES = 8
DEFAULT_MAX_OCTAVES_PER_PITCH_CLASS = 3
 succeeded in 50ms:
from typing import List, Optional, Tuple

from src.tokens.intervals import compute_reference_pitch, harmonic_tokens_for_pitch


def _infer_num_voices(tokens: List[str]) -> int:
    max_idx = -1
    for tok in tokens:
        if tok.startswith("VOICE_"):
            try:
                v = int(tok.split("_", 1)[1])
            except ValueError:
                continue
            max_idx = max(max_idx, v)
        elif tok.startswith("ABS_VOICE_"):
            parts = tok.split("_")
            if len(parts) == 4:
                try:
                    v = int(parts[2])
                except ValueError:
 succeeded in 50ms:
import sys
from pathlib import Path
import pytest
from music21 import corpus

# Ensure src is in path if running from root
sys.path.insert(0, str(Path(__file__).parents[1]))

from src.tokens.eventizer import eventize_musicxml
from src.tokens.roundtrip import tokens_to_midi
from src.tokens.validator import validate_harm_tokens

def _local_xml(path: Path) -> str:
    if not path.exists():
        pytest.skip(f"Local file {path} not found")
    return str(path)

@pytest.fixture
def bwv66_xml(tmp_path):
    """Creates a temporary MusicXML file for BWV 66.6."""
 succeeded in 51ms:
from typing import List, Optional, Tuple

import music21


def _parse_time_sig_token(token: str) -> Tuple[int, int]:
    parts = token.split("_")
    if len(parts) != 4:
        raise ValueError(f"bad time signature token: {token}")
    return int(parts[2]), int(parts[3])


def _parse_key_token(token: str) -> music21.key.Key:
    key_str = token[len("KEY_") :]
    mode = "major"
    if key_str.endswith("m"):
        mode = "minor"
        key_str = key_str[:-1]
    tonic = key_str.replace("b", "-")
    return music21.key.Key(tonic, mode)
 succeeded in 51ms:
import argparse
import json
import random
import sys
import tempfile
from pathlib import Path
from typing import Dict, List, Any, Tuple, Optional

# Ensure project root is in path
ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT))

import pandas as pd
from src.tokens.eventizer import eventize_musicxml
from src.tokens.roundtrip import tokens_to_midi
from src.dataio.descriptors import compute_bar_plan
from src.tokens.schema import BarPlan


def _infer_num_voices(tokens: List[str]) -> int:

thinking
**Inspecting truncated eventizer output**
exec
/bin/bash -lc "sed -n '360,760p' src/tokens/eventizer.py" in /home/stephen-reilly/dev/bach_gen succeeded in 51ms:
                )
        kept.sort(key=lambda ev: ev.pitch)
        collapsed[onset] = kept
    return collapsed


def _assign_events_by_continuity(
    events_by_tick: Dict[int, List[NoteEvent]],
    *,
    max_voices: int,
) -> Dict[int, List[NoteEvent]]:
    events_by_voice: Dict[int, List[NoteEvent]] = {v: [] for v in range(max_voices)}
    prev_pitch: List[Optional[int]] = [None] * max_voices
    active_until: List[int] = [0] * max_voices

    for onset in sorted(events_by_tick.keys()):
        events = sorted(events_by_tick[onset], key=lambda ev: ev.pitch)

        active_count = sum(1 for v in range(max_voices) if active_until[v] > onset)
        free_voices = [v for v in range(max_voices) if active_until[v] <= onset]

thinking
**Planning tokenizer implementation**
exec
/bin/bash -lc "sed -n '1,220p' src/tokens/__init__.py" in /home/stephen-reilly/dev/bach_genexec
/bin/bash -lc "sed -n '1,260p' src/dataio/descriptors.py" in /home/stephen-reilly/dev/bach_genexec
/bin/bash -lc "rg -n \"tokenizer|canonical|VOICE_\" tests src | sed -n '1,260p'" in /home/stephen-reilly/dev/bach_gen succeeded in 50ms:

 succeeded in 50ms:
from typing import Any, Dict, List, Optional, Tuple

from src.tokens.schema import BarPlan


def _extract_time_sig(tokens: List[str], fallback: Optional[str]) -> str:
    for tok in tokens:
        if tok.startswith("TIME_SIG_"):
            parts = tok.split("_")
            if len(parts) != 4:
                raise ValueError(f"bad TIME_SIG token: {tok}")
            return f"{parts[2]}/{parts[3]}"
    if fallback is not None:
        return fallback
    raise ValueError("missing TIME_SIG token and no fallback provided")


def _extract_key(tokens: List[str], fallback: Optional[str]) -> str:
    for tok in tokens:
        if tok.startswith("KEY_"):
 succeeded in 50ms:
src/tokens/validator.py:9:        if tok.startswith("VOICE_"):
src/tokens/validator.py:15:        elif tok.startswith("ABS_VOICE_"):
src/tokens/validator.py:89:        if tok.startswith("ABS_VOICE_"):
src/tokens/validator.py:109:        if tok.startswith("REF_VOICE_"):
src/tokens/validator.py:137:                if tt.startswith("ABS_VOICE_"):
src/tokens/validator.py:152:                if tt.startswith("REF_VOICE_"):
src/tokens/validator.py:156:                if tt.startswith("VOICE_"):
src/tokens/validator.py:220:                if tt.startswith("ABS_VOICE_"):
src/tokens/validator.py:237:                if tt.startswith("REF_VOICE_"):
src/tokens/validator.py:241:                if tt.startswith("VOICE_"):
src/tokens/eventizer.py:539:            tokens.append(f"ABS_VOICE_{v}_{anchor_pitch}")
src/tokens/eventizer.py:584:                        tokens.append(f"ABS_VOICE_{v}_{ev.pitch}")
src/tokens/eventizer.py:587:                tokens.append(f"VOICE_{v}")
src/tokens/schema.py:18:            "VOICE_{0..K-1}",
src/tokens/schema.py:23:            "ABS_VOICE_{v}_{MIDI}",
tests/test_model_integration.py:19:        "VOICE_0": 7,
tests/test_model_integration.py:37:            "tokens": "BAR TIME_SIG_4_4 KEY_C POS_0 VOICE_0 DUR_24 MEL_INT12_0 HARM_OCT_0 HARM_CLASS_0",
src/tokens/roundtrip.py:45:        if tok.startswith("VOICE_"):
src/tokens/roundtrip.py:51:        elif tok.startswith("ABS_VOICE_"):
src/tokens/roundtrip.py:139:        if tok.startswith("ABS_VOICE_"):

thinking
**Reviewing token-related tests**
exec
/bin/bash -lc "sed -n '1,280p' tests/test_descriptors.py" in /home/stephen-reilly/dev/bach_gen succeeded in 51ms:
import sys
from pathlib import Path

import pytest

sys.path.insert(0, str(Path(__file__).parents[1]))

from src.dataio.descriptors import (
    _classify_density,
    _compute_pitch_range,
    _extract_key,
    _extract_time_sig,
    compute_bar_plan,
)


def _voice_event(voice, duration, mel):
    return [
        f"VOICE_{voice}",
        f"DUR_{duration}",

thinking
**Inspecting eventizer tests and scripts**
exec
/bin/bash -lc "sed -n '1,260p' scripts/eventize_piece.py" in /home/stephen-reilly/dev/bach_gen succeeded in 51ms:
#!/usr/bin/env python
import argparse
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT))

from src.tokens.eventizer import eventize_musicxml  # noqa: E402
from src.tokens.roundtrip import tokens_to_midi  # noqa: E402


def _write_tokens(tokens, path, pretty=True):
    if not pretty:
        text = ", ".join(tokens)
        path.write_text(text, encoding="utf-8")
        return

    lines = []
    current = []

thinking
**Assessing tokenizer scope**
exec
/bin/bash -lc 'ls -la' in /home/stephen-reilly/dev/bach_gen succeeded in 51ms:
total 144
drwxrwxrwx 13 stephen-reilly stephen-reilly  4096 Feb 24 02:24 .
drwxrwxr-x 12 stephen-reilly stephen-reilly  4096 Feb  5 20:01 ..
drwxrwxr-x  3 stephen-reilly stephen-reilly  4096 Feb 24 02:51 .codex
drwxrwxr-x  8 stephen-reilly stephen-reilly  4096 Feb 24 03:13 .git
-rw-rw-r--  1 stephen-reilly stephen-reilly    18 Feb 24 01:43 .gitattributes
-rw-rw-r--  1 stephen-reilly stephen-reilly   845 Feb 24 01:43 .gitignore
drwxrwxrwx  3 stephen-reilly stephen-reilly  4096 Dec 22 10:44 .pytest_cache
drwxrwxrwx  6 stephen-reilly stephen-reilly  4096 Jan  5 15:11 .venv
-rw-rw-r--  1 stephen-reilly stephen-reilly   893 Feb 24 02:43 AGENTS.md
-rw-rw-r--  1 stephen-reilly stephen-reilly  1141 Feb 24 03:13 PROGRESS.md
-rw-rw-rw-  1 stephen-reilly stephen-reilly 30064 Jan 12 16:41 README.md
-rw-rw-r--  1 stephen-reilly stephen-reilly  1936 Feb 24 02:23 SPEC.md
-rw-rw-r--  1 stephen-reilly stephen-reilly   350 Feb 24 03:13 TODO.md
drwxrwxrwx  4 stephen-reilly stephen-reilly  4096 Jan  8 08:47 data
drwxrwxr-x  2 stephen-reilly stephen-reilly  4096 Feb 24 01:43 data-testing
drwxrwxr-x  4 stephen-reilly stephen-reilly  4096 Feb 24 02:41 docs
drwxrwxrwx  5 stephen-reilly stephen-reilly  4096 Feb 24 01:43 frontend
-rw-rw-rw-  1 stephen-reilly stephen-reilly  9385 Jan 12 16:42 frontend-readme.md
-rw-rw-r--  1 stephen-reilly stephen-reilly   595 Feb 24 02:24 loop_output.schema.json

thinking
**Inspecting final output schema**
codex
{"done":false,"progress_made":true,"summary":"I’ve finished reading the requested files and confirmed the next unchecked ExecPlan item targets `src/tokens/tokenizer.py`, which doesn’t exist yet. I’m now checking the loop output schema and then I’ll implement that module with focused tests and update TODO/PROGRESS.","tests_ran":false,"tests_status":"not_run","next_step":"Inspect `loop_output.schema.json`, add tokenizer parsing/serialization, and add tests for canonical ordering behavior.","stop_reason":""}
exec
/bin/bash -lc 'cat loop_output.schema.json' in /home/stephen-reilly/dev/bach_gen succeeded in 51ms:
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "done": { "type": "boolean" },
    "progress_made": { "type": "boolean" },
    "summary": { "type": "string" },
    "tests_ran": { "type": "boolean" },
    "tests_status": { "type": "string", "enum": ["pass", "fail", "not_run"] },
    "next_step": { "type": "string" },
    "stop_reason": { "type": "string" }
  },
  "required": [
    "done",
    "progress_made",
    "summary",
    "tests_ran",
    "tests_status",
    "next_step",

thinking
**Planning canonical event parsing**
exec
/bin/bash -lc "rg -n \"canonical ordering|parse|serialization|tokenizer.py|MEL_INT12|DUP_\" docs SPEC.md README.md src tests | sed -n '1,320p'" in /home/stephen-reilly/dev/bach_genexec
/bin/bash -lc "rg -n \"DUP_\" src tests | sed -n '1,200p'" in /home/stephen-reilly/dev/bach_gen succeeded in 51ms:
SPEC.md:8:- Events are sparse: emit `POS_t` only where at least one voice event starts.
SPEC.md:10:- Absolute pitch truth is `ABS_VOICE_*` + `MEL_INT12_*`.
SPEC.md:25:- `MEL_INT12_{-24..+24}`
SPEC.md:30:`VOICE_v, DUR_{ticks}, (optional DUP_{n}), MEL_INT12_*, HARM_OCT_*, HARM_CLASS_*`
SPEC.md:42:- Reconstruct pitch from anchors + `MEL_INT12` and verify `HARM_*` matches `pitch - ref_pitch_t` when reference exists.
README.md:38:* **NoteLM output (v1):** symbolic music only (BAR/POS/DUR/VOICE + `ABS_*`, `MEL_INT12`, and `HARM_*`).
README.md:49:* **Pitch:** `ABS_VOICE_*` anchors + `MEL_INT12` reconstruct `pitchMidi`.
README.md:70:      tokenizer.py# interval math + BPE encoding
README.md:128:    * **Melodic:** `MEL_INT12 ∈ [-24..+24]` — semitone delta vs previous note in the same voice.
README.md:163:   - (Optional) record a token `DUP_{n}` indicating how many notes were collapsed into that event.
README.md:172:* **Voice events (sparse, duration-first):** `VOICE_{0..K-1}`, `DUR_{ticks}`, `REST_{ticks}` (optional)
README.md:180:* **Doubling (optional):** `DUP_{1..N}` emitted only when exact unison doublings were collapsed at that onset.
README.md:185:    * `MEL_INT12_{-24..+24}`
README.md:193:* **Auxiliary (non-serialized):** `NEXT_MEL_INT12_*` teacher-forced targets.
README.md:250:* `MEL_INT12 = curr_pitch - prev_pitch[v]`
README.md:259:2. **Pitched onset:** `VOICE_v, DUR_d, MEL_INT12_Δ, ...`
README.md:269:**Truth:** absolute pitch is determined exclusively by `ABS_VOICE_* + MEL_INT12`. `HARM_*` are derived features and must match `pitch - ref_pitch_t` when reference exists.
README.md:278:VOICE_v, DUR_{ticks}, (optional) DUP_{n}, MEL_INT12_{Δ}, HARM_OCT_{o|NA}, HARM_CLASS_{c|NA}
README.md:291:**Parsing rule:** After `VOICE_v`, if the next token is `REST_{ticks}`, the event ends; otherwise it must be `DUR_{ticks}` followed by optional `DUP_{n}`, `MEL_INT12_*`, `HARM_OCT_*`, `HARM_CLASS_*`.
README.md:295:**v1 pitch truth:** `ABS_VOICE_* + MEL_INT12`.
 succeeded in 50ms:
src/tokens/validator.py:167:                    if next_idx < n and tokens[next_idx].startswith("DUP_"):
src/tokens/validator.py:258:                    if next_idx < n and tokens[next_idx].startswith("DUP_"):
src/tokens/schema.py:32:        "doubling": ["DUP_{1..N}"],
src/tokens/roundtrip.py:174:            if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
src/dataio/descriptors.py:203:            if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):

thinking
**Reviewing README parsing rules**
exec
/bin/bash -lc "sed -n '240,370p' README.md" in /home/stephen-reilly/dev/bach_gen succeeded in 50ms:
    * `diff = pitch - ref_pitch_t`
    * `(octave, harm_class) = divmod(diff, 12)`
    * emit `HARM_OCT_{octave}`, `HARM_CLASS_{harm_class}`

* Else (rare — e.g., before first onset):

    * emit `HARM_OCT_NA`, `HARM_CLASS_NA` (and optionally down-weight/skip HARM loss)

**Melodic intervals**

* `MEL_INT12 = curr_pitch - prev_pitch[v]`

### 4.4 Pitch Reconstruction (source of truth)

Each voice maintains `prev_pitch[v]` and `active_until[v]`.

1. **Anchors:** `ABS_VOICE_{v}_{MIDI}` sets `prev_pitch[v] = midi`.
   (Optional `ABS_LOW_{MIDI}` / `ABS_HIGH_{MIDI}` do not set per-track state.)

2. **Pitched onset:** `VOICE_v, DUR_d, MEL_INT12_Δ, ...`

thinking
**Designing robust event parser/serializer**
codex
{"done":false,"progress_made":true,"summary":"I found no existing `src/tokens/tokenizer.py`, so this loop’s Stage-1 task is to add it and wire canonical voice-event serialization. Plan: (1) add a focused tokenizer module for `VOICE` event parse/serialize and stream canonicalization; (2) update `eventizer` to emit voice bundles through that serializer; (3) add unit tests for parse/serialize/canonical behavior, then run the full verify command and update TODO/PROGRESS.","tests_ran":false,"tests_status":"not_run","next_step":"Create `src/tokens/tokenizer.py` and add tests.","stop_reason":""}

thinking
**Planning tokenizer integration**
file update
A /home/stephen-reilly/dev/bach_gen/src/tokens/tokenizer.py
from dataclasses import dataclass
from typing import List, Sequence, Tuple, Union

from src.tokens.intervals import format_signed_interval


@dataclass(frozen=True)
class VoiceEvent:
    voice: int
    duration_ticks: int = 0
    rest_ticks: int = 0
    mel_int: int = 0
    harm_oct: Union[int, None] = None
    harm_class: Union[int, None] = None
    dup_count: Union[int, None] = None
    string: Union[int, None] = None
    fret: Union[int, None] = None

    def __post_init__(self) -> None:
        if self.voice < 0:
            raise ValueError("voice must be >= 0")
        if self.duration_ticks < 0:
            raise ValueError("duration_ticks must be >= 0")
        if self.rest_ticks < 0:
            raise ValueError("rest_ticks must be >= 0")
        if self.dup_count is not None and self.dup_count < 1:
            raise ValueError("dup_count must be >= 1 when set")
        if (self.string is None) ^ (self.fret is None):
            raise ValueError("string and fret must both be set or both be None")

        is_rest = self.rest_ticks > 0
        is_pitched = self.duration_ticks > 0
        if is_rest == is_pitched:
            raise ValueError("voice event must be exactly one of rest or pitched")

        if is_rest:
            if self.dup_count is not None:
                raise ValueError("rest event cannot set dup_count")
            if self.harm_oct is not None or self.harm_class is not None:
                raise ValueError("rest event cannot set harmonic interval fields")
            if self.string is not None or self.fret is not None:
                raise ValueError("rest event cannot set tab fields")
            return

        if (self.harm_oct is None) ^ (self.harm_class is None):
            raise ValueError("harm_oct and harm_class must both be set or both be None")
        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
            raise ValueError("harm_class must be in [0, 11]")

    @property
    def is_rest(self) -> bool:
        return self.rest_ticks > 0


def tokenize_event_text(text: str) -> List[str]:
    raw = text.replace("\n", ",")
    tokens = [tok.strip() for tok in raw.split(",")]
    return [tok for tok in tokens if tok]


def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
    if start_idx >= len(tokens):
        raise ValueError("start_idx out of range")

    voice_tok = tokens[start_idx]
    if not voice_tok.startswith("VOICE_"):
        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")

    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
    next_idx = start_idx + 1
    if next_idx >= len(tokens):
        raise ValueError(f"truncated VOICE event at index {start_idx}")

    first_tok = tokens[next_idx]
    if first_tok.startswith("REST_"):
        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1

    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
    next_idx += 1

    dup_count = None
    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
        next_idx += 1

    if next_idx + 2 >= len(tokens):
        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")

    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
    next_idx += 3

    string = None
    fret = None
    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
        next_idx += 1
        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
            raise ValueError(f"missing FRET token after STR at index {next_idx}")
        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
        next_idx += 1
    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
        raise ValueError(f"FRET token must follow STR token at index {next_idx}")

    return (
        VoiceEvent(
            voice=voice,
            duration_ticks=duration_ticks,
            mel_int=mel_int,
            harm_oct=harm_oct,
            harm_class=harm_class,
            dup_count=dup_count,
            string=string,
            fret=fret,
        ),
        next_idx,
    )


def serialize_voice_event(event: VoiceEvent) -> List[str]:
    tokens = [f"VOICE_{event.voice}"]
    if event.is_rest:
        tokens.append(f"REST_{event.rest_ticks}")
        return tokens

    tokens.append(f"DUR_{event.duration_ticks}")
    if event.dup_count is not None:
        tokens.append(f"DUP_{event.dup_count}")

    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
    if event.harm_oct is None:
        tokens.append("HARM_OCT_NA")
        tokens.append("HARM_CLASS_NA")
    else:
        tokens.append(f"HARM_OCT_{event.harm_oct}")
        tokens.append(f"HARM_CLASS_{event.harm_class}")

    if event.string is not None:
        tokens.append(f"STR_{event.string}")
        tokens.append(f"FRET_{event.fret}")

    return tokens


def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
    parsed: List[Union[str, VoiceEvent]] = []
    idx = 0
    while idx < len(tokens):
        tok = tokens[idx]
        if tok.startswith("VOICE_"):
            event, idx = parse_voice_event(tokens, idx)
            parsed.append(event)
            continue
        parsed.append(tok)
        idx += 1
    return parsed


def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
    tokens: List[str] = []
    for item in items:
        if isinstance(item, VoiceEvent):
            tokens.extend(serialize_voice_event(item))
        else:
            tokens.append(item)
    return tokens


def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
    return serialize_event_stream(parse_event_stream(tokens))


def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
    if not token.startswith(prefix):
        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
    value = token[len(prefix) :]
    try:
        return int(value)
    except ValueError as exc:
        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc


def _parse_signed_mel(token: str, idx: int) -> int:
    if not token.startswith("MEL_INT12_"):
        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
    value = token[len("MEL_INT12_") :]
    try:
        return int(value)
    except ValueError as exc:
        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc


def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
    if not token.startswith("HARM_OCT_"):
        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
    value = token[len("HARM_OCT_") :]
    if value == "NA":
        return None
    try:
        return int(value)
    except ValueError as exc:
        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc


def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
    if not token.startswith("HARM_CLASS_"):
        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
    value = token[len("HARM_CLASS_") :]
    if value == "NA":
        return None
    try:
        parsed = int(value)
    except ValueError as exc:
        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
    if not (0 <= parsed <= 11):
        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
    return parsed
apply_patch(auto_approved=true) exited 0 in 14ms:
Success. Updated the following files:
A src/tokens/tokenizer.py
file update:
diff --git a/src/tokens/tokenizer.py b/src/tokens/tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..3d7839be70301fe3c5376ba858263c8c4f66da1b
--- /dev/null
+++ b/src/tokens/tokenizer.py
@@ -0,0 +1,219 @@
+from dataclasses import dataclass
+from typing import List, Sequence, Tuple, Union
+
+from src.tokens.intervals import format_signed_interval
+
+
+@dataclass(frozen=True)
+class VoiceEvent:
+    voice: int
+    duration_ticks: int = 0
+    rest_ticks: int = 0
+    mel_int: int = 0
+    harm_oct: Union[int, None] = None
+    harm_class: Union[int, None] = None
+    dup_count: Union[int, None] = None
+    string: Union[int, None] = None
+    fret: Union[int, None] = None
+
+    def __post_init__(self) -> None:
+        if self.voice < 0:
+            raise ValueError("voice must be >= 0")
+        if self.duration_ticks < 0:
+            raise ValueError("duration_ticks must be >= 0")
+        if self.rest_ticks < 0:
+            raise ValueError("rest_ticks must be >= 0")
+        if self.dup_count is not None and self.dup_count < 1:
+            raise ValueError("dup_count must be >= 1 when set")
+        if (self.string is None) ^ (self.fret is None):
+            raise ValueError("string and fret must both be set or both be None")
+
+        is_rest = self.rest_ticks > 0
+        is_pitched = self.duration_ticks > 0
+        if is_rest == is_pitched:
+            raise ValueError("voice event must be exactly one of rest or pitched")
+
+        if is_rest:
+            if self.dup_count is not None:
+                raise ValueError("rest event cannot set dup_count")
+            if self.harm_oct is not None or self.harm_class is not None:
+                raise ValueError("rest event cannot set harmonic interval fields")
+            if self.string is not None or self.fret is not None:
+                raise ValueError("rest event cannot set tab fields")
+            return
+
+        if (self.harm_oct is None) ^ (self.harm_class is None):
+            raise ValueError("harm_oct and harm_class must both be set or both be None")
+        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
+            raise ValueError("harm_class must be in [0, 11]")
+
+    @property
+    def is_rest(self) -> bool:
+        return self.rest_ticks > 0
+
+
+def tokenize_event_text(text: str) -> List[str]:
+    raw = text.replace("\n", ",")
+    tokens = [tok.strip() for tok in raw.split(",")]
+    return [tok for tok in tokens if tok]
+
+
+def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
+    if start_idx >= len(tokens):
+        raise ValueError("start_idx out of range")
+
+    voice_tok = tokens[start_idx]
+    if not voice_tok.startswith("VOICE_"):
+        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")
+
+    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
+    next_idx = start_idx + 1
+    if next_idx >= len(tokens):
+        raise ValueError(f"truncated VOICE event at index {start_idx}")
+
+    first_tok = tokens[next_idx]
+    if first_tok.startswith("REST_"):
+        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
+        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1
+
+    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
+    next_idx += 1
+
+    dup_count = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
+        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
+        next_idx += 1
+
+    if next_idx + 2 >= len(tokens):
+        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")
+
+    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
+    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
+    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
+    next_idx += 3
+
+    string = None
+    fret = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
+        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
+        next_idx += 1
+        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
+            raise ValueError(f"missing FRET token after STR at index {next_idx}")
+        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
+        next_idx += 1
+    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
+        raise ValueError(f"FRET token must follow STR token at index {next_idx}")
+
+    return (
+        VoiceEvent(
+            voice=voice,
+            duration_ticks=duration_ticks,
+            mel_int=mel_int,
+            harm_oct=harm_oct,
+            harm_class=harm_class,
+            dup_count=dup_count,
+            string=string,
+            fret=fret,
+        ),
+        next_idx,
+    )
+
+
+def serialize_voice_event(event: VoiceEvent) -> List[str]:
+    tokens = [f"VOICE_{event.voice}"]
+    if event.is_rest:
+        tokens.append(f"REST_{event.rest_ticks}")
+        return tokens
+
+    tokens.append(f"DUR_{event.duration_ticks}")
+    if event.dup_count is not None:
+        tokens.append(f"DUP_{event.dup_count}")
+
+    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
+    if event.harm_oct is None:
+        tokens.append("HARM_OCT_NA")
+        tokens.append("HARM_CLASS_NA")
+    else:
+        tokens.append(f"HARM_OCT_{event.harm_oct}")
+        tokens.append(f"HARM_CLASS_{event.harm_class}")
+
+    if event.string is not None:
+        tokens.append(f"STR_{event.string}")
+        tokens.append(f"FRET_{event.fret}")
+
+    return tokens
+
+
+def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
+    parsed: List[Union[str, VoiceEvent]] = []
+    idx = 0
+    while idx < len(tokens):
+        tok = tokens[idx]
+        if tok.startswith("VOICE_"):
+            event, idx = parse_voice_event(tokens, idx)
+            parsed.append(event)
+            continue
+        parsed.append(tok)
+        idx += 1
+    return parsed
+
+
+def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
+    tokens: List[str] = []
+    for item in items:
+        if isinstance(item, VoiceEvent):
+            tokens.extend(serialize_voice_event(item))
+        else:
+            tokens.append(item)
+    return tokens
+
+
+def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
+    return serialize_event_stream(parse_event_stream(tokens))
+
+
+def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
+    if not token.startswith(prefix):
+        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
+    value = token[len(prefix) :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc
+
+
+def _parse_signed_mel(token: str, idx: int) -> int:
+    if not token.startswith("MEL_INT12_"):
+        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
+    value = token[len("MEL_INT12_") :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_OCT_"):
+        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
+    value = token[len("HARM_OCT_") :]
+    if value == "NA":
+        return None
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_CLASS_"):
+        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
+    value = token[len("HARM_CLASS_") :]
+    if value == "NA":
+        return None
+    try:
+        parsed = int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
+    if not (0 <= parsed <= 11):
+        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
+    return parsed

file update:
diff --git a/src/tokens/tokenizer.py b/src/tokens/tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..3d7839be70301fe3c5376ba858263c8c4f66da1b
--- /dev/null
+++ b/src/tokens/tokenizer.py
@@ -0,0 +1,219 @@
+from dataclasses import dataclass
+from typing import List, Sequence, Tuple, Union
+
+from src.tokens.intervals import format_signed_interval
+
+
+@dataclass(frozen=True)
+class VoiceEvent:
+    voice: int
+    duration_ticks: int = 0
+    rest_ticks: int = 0
+    mel_int: int = 0
+    harm_oct: Union[int, None] = None
+    harm_class: Union[int, None] = None
+    dup_count: Union[int, None] = None
+    string: Union[int, None] = None
+    fret: Union[int, None] = None
+
+    def __post_init__(self) -> None:
+        if self.voice < 0:
+            raise ValueError("voice must be >= 0")
+        if self.duration_ticks < 0:
+            raise ValueError("duration_ticks must be >= 0")
+        if self.rest_ticks < 0:
+            raise ValueError("rest_ticks must be >= 0")
+        if self.dup_count is not None and self.dup_count < 1:
+            raise ValueError("dup_count must be >= 1 when set")
+        if (self.string is None) ^ (self.fret is None):
+            raise ValueError("string and fret must both be set or both be None")
+
+        is_rest = self.rest_ticks > 0
+        is_pitched = self.duration_ticks > 0
+        if is_rest == is_pitched:
+            raise ValueError("voice event must be exactly one of rest or pitched")
+
+        if is_rest:
+            if self.dup_count is not None:
+                raise ValueError("rest event cannot set dup_count")
+            if self.harm_oct is not None or self.harm_class is not None:
+                raise ValueError("rest event cannot set harmonic interval fields")
+            if self.string is not None or self.fret is not None:
+                raise ValueError("rest event cannot set tab fields")
+            return
+
+        if (self.harm_oct is None) ^ (self.harm_class is None):
+            raise ValueError("harm_oct and harm_class must both be set or both be None")
+        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
+            raise ValueError("harm_class must be in [0, 11]")
+
+    @property
+    def is_rest(self) -> bool:
+        return self.rest_ticks > 0
+
+
+def tokenize_event_text(text: str) -> List[str]:
+    raw = text.replace("\n", ",")
+    tokens = [tok.strip() for tok in raw.split(",")]
+    return [tok for tok in tokens if tok]
+
+
+def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
+    if start_idx >= len(tokens):
+        raise ValueError("start_idx out of range")
+
+    voice_tok = tokens[start_idx]
+    if not voice_tok.startswith("VOICE_"):
+        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")
+
+    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
+    next_idx = start_idx + 1
+    if next_idx >= len(tokens):
+        raise ValueError(f"truncated VOICE event at index {start_idx}")
+
+    first_tok = tokens[next_idx]
+    if first_tok.startswith("REST_"):
+        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
+        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1
+
+    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
+    next_idx += 1
+
+    dup_count = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
+        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
+        next_idx += 1
+
+    if next_idx + 2 >= len(tokens):
+        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")
+
+    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
+    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
+    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
+    next_idx += 3
+
+    string = None
+    fret = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
+        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
+        next_idx += 1
+        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
+            raise ValueError(f"missing FRET token after STR at index {next_idx}")
+        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
+        next_idx += 1
+    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
+        raise ValueError(f"FRET token must follow STR token at index {next_idx}")
+
+    return (
+        VoiceEvent(
+            voice=voice,
+            duration_ticks=duration_ticks,
+            mel_int=mel_int,
+            harm_oct=harm_oct,
+            harm_class=harm_class,
+            dup_count=dup_count,
+            string=string,
+            fret=fret,
+        ),
+        next_idx,
+    )
+
+
+def serialize_voice_event(event: VoiceEvent) -> List[str]:
+    tokens = [f"VOICE_{event.voice}"]
+    if event.is_rest:
+        tokens.append(f"REST_{event.rest_ticks}")
+        return tokens
+
+    tokens.append(f"DUR_{event.duration_ticks}")
+    if event.dup_count is not None:
+        tokens.append(f"DUP_{event.dup_count}")
+
+    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
+    if event.harm_oct is None:
+        tokens.append("HARM_OCT_NA")
+        tokens.append("HARM_CLASS_NA")
+    else:
+        tokens.append(f"HARM_OCT_{event.harm_oct}")
+        tokens.append(f"HARM_CLASS_{event.harm_class}")
+
+    if event.string is not None:
+        tokens.append(f"STR_{event.string}")
+        tokens.append(f"FRET_{event.fret}")
+
+    return tokens
+
+
+def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
+    parsed: List[Union[str, VoiceEvent]] = []
+    idx = 0
+    while idx < len(tokens):
+        tok = tokens[idx]
+        if tok.startswith("VOICE_"):
+            event, idx = parse_voice_event(tokens, idx)
+            parsed.append(event)
+            continue
+        parsed.append(tok)
+        idx += 1
+    return parsed
+
+
+def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
+    tokens: List[str] = []
+    for item in items:
+        if isinstance(item, VoiceEvent):
+            tokens.extend(serialize_voice_event(item))
+        else:
+            tokens.append(item)
+    return tokens
+
+
+def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
+    return serialize_event_stream(parse_event_stream(tokens))
+
+
+def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
+    if not token.startswith(prefix):
+        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
+    value = token[len(prefix) :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc
+
+
+def _parse_signed_mel(token: str, idx: int) -> int:
+    if not token.startswith("MEL_INT12_"):
+        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
+    value = token[len("MEL_INT12_") :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_OCT_"):
+        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
+    value = token[len("HARM_OCT_") :]
+    if value == "NA":
+        return None
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_CLASS_"):
+        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
+    value = token[len("HARM_CLASS_") :]
+    if value == "NA":
+        return None
+    try:
+        parsed = int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
+    if not (0 <= parsed <= 11):
+        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
+    return parsed


thinking
**Modifying import and token append**
file update
M /home/stephen-reilly/dev/bach_gen/src/tokens/eventizer.py
@@ -14,5 +14,5 @@
     compute_melodic_interval,
-    format_signed_interval,
     harmonic_tokens_for_pitch,
 )
+from src.tokens.tokenizer import VoiceEvent, serialize_voice_event
 
@@ -586,4 +586,2 @@
 
-                tokens.append(f"VOICE_{v}")
-                tokens.append(f"DUR_{ev.duration_tick}")
                 base_pitch = prev_pitch[v] if prev_pitch[v] is not None else ev.pitch
@@ -594,3 +592,2 @@
                 )
-                tokens.append(f"MEL_INT12_{format_signed_interval(mel_int)}")
 
@@ -602,4 +599,17 @@
                 )
-                tokens.append(harm_oct_tok)
-                tokens.append(harm_cls_tok)
+                harm_oct = None if harm_oct_tok.endswith("_NA") else int(harm_oct_tok.rsplit("_", 1)[1])
+                harm_class = (
+                    None if harm_cls_tok.endswith("_NA") else int(harm_cls_tok.rsplit("_", 1)[1])
+                )
+                tokens.extend(
+                    serialize_voice_event(
+                        VoiceEvent(
+                            voice=v,
+                            duration_ticks=ev.duration_tick,
+                            mel_int=mel_int,
+                            harm_oct=harm_oct,
+                            harm_class=harm_class,
+                        )
+                    )
+                )
 
apply_patch(auto_approved=true) exited 0 in 5ms:
Success. Updated the following files:
M src/tokens/eventizer.py
file update:
diff --git a/src/tokens/eventizer.py b/src/tokens/eventizer.py
index f7c666b524c3f03edbf62558e3a89e26917d8d43..53385718babf181fdc8367c1ca227731a1c1b799
--- a/src/tokens/eventizer.py
+++ b/src/tokens/eventizer.py
@@ -12,9 +12,9 @@
     IntervalRepairStats,
     compute_reference_pitch,
     compute_melodic_interval,
-    format_signed_interval,
     harmonic_tokens_for_pitch,
 )
+from src.tokens.tokenizer import VoiceEvent, serialize_voice_event
 
 DEFAULT_MAX_VOICES = 8
 DEFAULT_MAX_OCTAVES_PER_PITCH_CLASS = 3
@@ -584,15 +584,12 @@
                         tokens.append(f"ABS_VOICE_{v}_{ev.pitch}")
                     prev_pitch[v] = ev.pitch
 
-                tokens.append(f"VOICE_{v}")
-                tokens.append(f"DUR_{ev.duration_tick}")
                 base_pitch = prev_pitch[v] if prev_pitch[v] is not None else ev.pitch
                 mel_int = compute_melodic_interval(
                     ev.pitch,
                     base_pitch,
                     qa_mode=True,
                 )
-                tokens.append(f"MEL_INT12_{format_signed_interval(mel_int)}")
 
                 harm_oct_tok, harm_cls_tok = harmonic_tokens_for_pitch(
                     ev.pitch,
@@ -600,8 +597,21 @@
                     qa_mode=qa_mode,
                     stats=interval_repairs,
                 )
-                tokens.append(harm_oct_tok)
-                tokens.append(harm_cls_tok)
+                harm_oct = None if harm_oct_tok.endswith("_NA") else int(harm_oct_tok.rsplit("_", 1)[1])
+                harm_class = (
+                    None if harm_cls_tok.endswith("_NA") else int(harm_cls_tok.rsplit("_", 1)[1])
+                )
+                tokens.extend(
+                    serialize_voice_event(
+                        VoiceEvent(
+                            voice=v,
+                            duration_ticks=ev.duration_tick,
+                            mel_int=mel_int,
+                            harm_oct=harm_oct,
+                            harm_class=harm_class,
+                        )
+                    )
+                )
 
                 prev_pitch[v] = ev.pitch
                 active_until[v] = t + ev.duration_tick
diff --git a/src/tokens/tokenizer.py b/src/tokens/tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..3d7839be70301fe3c5376ba858263c8c4f66da1b
--- /dev/null
+++ b/src/tokens/tokenizer.py
@@ -0,0 +1,219 @@
+from dataclasses import dataclass
+from typing import List, Sequence, Tuple, Union
+
+from src.tokens.intervals import format_signed_interval
+
+
+@dataclass(frozen=True)
+class VoiceEvent:
+    voice: int
+    duration_ticks: int = 0
+    rest_ticks: int = 0
+    mel_int: int = 0
+    harm_oct: Union[int, None] = None
+    harm_class: Union[int, None] = None
+    dup_count: Union[int, None] = None
+    string: Union[int, None] = None
+    fret: Union[int, None] = None
+
+    def __post_init__(self) -> None:
+        if self.voice < 0:
+            raise ValueError("voice must be >= 0")
+        if self.duration_ticks < 0:
+            raise ValueError("duration_ticks must be >= 0")
+        if self.rest_ticks < 0:
+            raise ValueError("rest_ticks must be >= 0")
+        if self.dup_count is not None and self.dup_count < 1:
+            raise ValueError("dup_count must be >= 1 when set")
+        if (self.string is None) ^ (self.fret is None):
+            raise ValueError("string and fret must both be set or both be None")
+
+        is_rest = self.rest_ticks > 0
+        is_pitched = self.duration_ticks > 0
+        if is_rest == is_pitched:
+            raise ValueError("voice event must be exactly one of rest or pitched")
+
+        if is_rest:
+            if self.dup_count is not None:
+                raise ValueError("rest event cannot set dup_count")
+            if self.harm_oct is not None or self.harm_class is not None:
+                raise ValueError("rest event cannot set harmonic interval fields")
+            if self.string is not None or self.fret is not None:
+                raise ValueError("rest event cannot set tab fields")
+            return
+
+        if (self.harm_oct is None) ^ (self.harm_class is None):
+            raise ValueError("harm_oct and harm_class must both be set or both be None")
+        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
+            raise ValueError("harm_class must be in [0, 11]")
+
+    @property
+    def is_rest(self) -> bool:
+        return self.rest_ticks > 0
+
+
+def tokenize_event_text(text: str) -> List[str]:
+    raw = text.replace("\n", ",")
+    tokens = [tok.strip() for tok in raw.split(",")]
+    return [tok for tok in tokens if tok]
+
+
+def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
+    if start_idx >= len(tokens):
+        raise ValueError("start_idx out of range")
+
+    voice_tok = tokens[start_idx]
+    if not voice_tok.startswith("VOICE_"):
+        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")
+
+    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
+    next_idx = start_idx + 1
+    if next_idx >= len(tokens):
+        raise ValueError(f"truncated VOICE event at index {start_idx}")
+
+    first_tok = tokens[next_idx]
+    if first_tok.startswith("REST_"):
+        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
+        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1
+
+    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
+    next_idx += 1
+
+    dup_count = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
+        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
+        next_idx += 1
+
+    if next_idx + 2 >= len(tokens):
+        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")
+
+    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
+    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
+    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
+    next_idx += 3
+
+    string = None
+    fret = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
+        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
+        next_idx += 1
+        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
+            raise ValueError(f"missing FRET token after STR at index {next_idx}")
+        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
+        next_idx += 1
+    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
+        raise ValueError(f"FRET token must follow STR token at index {next_idx}")
+
+    return (
+        VoiceEvent(
+            voice=voice,
+            duration_ticks=duration_ticks,
+            mel_int=mel_int,
+            harm_oct=harm_oct,
+            harm_class=harm_class,
+            dup_count=dup_count,
+            string=string,
+            fret=fret,
+        ),
+        next_idx,
+    )
+
+
+def serialize_voice_event(event: VoiceEvent) -> List[str]:
+    tokens = [f"VOICE_{event.voice}"]
+    if event.is_rest:
+        tokens.append(f"REST_{event.rest_ticks}")
+        return tokens
+
+    tokens.append(f"DUR_{event.duration_ticks}")
+    if event.dup_count is not None:
+        tokens.append(f"DUP_{event.dup_count}")
+
+    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
+    if event.harm_oct is None:
+        tokens.append("HARM_OCT_NA")
+        tokens.append("HARM_CLASS_NA")
+    else:
+        tokens.append(f"HARM_OCT_{event.harm_oct}")
+        tokens.append(f"HARM_CLASS_{event.harm_class}")
+
+    if event.string is not None:
+        tokens.append(f"STR_{event.string}")
+        tokens.append(f"FRET_{event.fret}")
+
+    return tokens
+
+
+def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
+    parsed: List[Union[str, VoiceEvent]] = []
+    idx = 0
+    while idx < len(tokens):
+        tok = tokens[idx]
+        if tok.startswith("VOICE_"):
+            event, idx = parse_voice_event(tokens, idx)
+            parsed.append(event)
+            continue
+        parsed.append(tok)
+        idx += 1
+    return parsed
+
+
+def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
+    tokens: List[str] = []
+    for item in items:
+        if isinstance(item, VoiceEvent):
+            tokens.extend(serialize_voice_event(item))
+        else:
+            tokens.append(item)
+    return tokens
+
+
+def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
+    return serialize_event_stream(parse_event_stream(tokens))
+
+
+def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
+    if not token.startswith(prefix):
+        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
+    value = token[len(prefix) :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc
+
+
+def _parse_signed_mel(token: str, idx: int) -> int:
+    if not token.startswith("MEL_INT12_"):
+        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
+    value = token[len("MEL_INT12_") :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_OCT_"):
+        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
+    value = token[len("HARM_OCT_") :]
+    if value == "NA":
+        return None
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_CLASS_"):
+        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
+    value = token[len("HARM_CLASS_") :]
+    if value == "NA":
+        return None
+    try:
+        parsed = int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
+    if not (0 <= parsed <= 11):
+        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
+    return parsed

file update:
diff --git a/src/tokens/eventizer.py b/src/tokens/eventizer.py
index f7c666b524c3f03edbf62558e3a89e26917d8d43..53385718babf181fdc8367c1ca227731a1c1b799
--- a/src/tokens/eventizer.py
+++ b/src/tokens/eventizer.py
@@ -12,9 +12,9 @@
     IntervalRepairStats,
     compute_reference_pitch,
     compute_melodic_interval,
-    format_signed_interval,
     harmonic_tokens_for_pitch,
 )
+from src.tokens.tokenizer import VoiceEvent, serialize_voice_event
 
 DEFAULT_MAX_VOICES = 8
 DEFAULT_MAX_OCTAVES_PER_PITCH_CLASS = 3
@@ -584,15 +584,12 @@
                         tokens.append(f"ABS_VOICE_{v}_{ev.pitch}")
                     prev_pitch[v] = ev.pitch
 
-                tokens.append(f"VOICE_{v}")
-                tokens.append(f"DUR_{ev.duration_tick}")
                 base_pitch = prev_pitch[v] if prev_pitch[v] is not None else ev.pitch
                 mel_int = compute_melodic_interval(
                     ev.pitch,
                     base_pitch,
                     qa_mode=True,
                 )
-                tokens.append(f"MEL_INT12_{format_signed_interval(mel_int)}")
 
                 harm_oct_tok, harm_cls_tok = harmonic_tokens_for_pitch(
                     ev.pitch,
@@ -600,8 +597,21 @@
                     qa_mode=qa_mode,
                     stats=interval_repairs,
                 )
-                tokens.append(harm_oct_tok)
-                tokens.append(harm_cls_tok)
+                harm_oct = None if harm_oct_tok.endswith("_NA") else int(harm_oct_tok.rsplit("_", 1)[1])
+                harm_class = (
+                    None if harm_cls_tok.endswith("_NA") else int(harm_cls_tok.rsplit("_", 1)[1])
+                )
+                tokens.extend(
+                    serialize_voice_event(
+                        VoiceEvent(
+                            voice=v,
+                            duration_ticks=ev.duration_tick,
+                            mel_int=mel_int,
+                            harm_oct=harm_oct,
+                            harm_class=harm_class,
+                        )
+                    )
+                )
 
                 prev_pitch[v] = ev.pitch
                 active_until[v] = t + ev.duration_tick
diff --git a/src/tokens/tokenizer.py b/src/tokens/tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..3d7839be70301fe3c5376ba858263c8c4f66da1b
--- /dev/null
+++ b/src/tokens/tokenizer.py
@@ -0,0 +1,219 @@
+from dataclasses import dataclass
+from typing import List, Sequence, Tuple, Union
+
+from src.tokens.intervals import format_signed_interval
+
+
+@dataclass(frozen=True)
+class VoiceEvent:
+    voice: int
+    duration_ticks: int = 0
+    rest_ticks: int = 0
+    mel_int: int = 0
+    harm_oct: Union[int, None] = None
+    harm_class: Union[int, None] = None
+    dup_count: Union[int, None] = None
+    string: Union[int, None] = None
+    fret: Union[int, None] = None
+
+    def __post_init__(self) -> None:
+        if self.voice < 0:
+            raise ValueError("voice must be >= 0")
+        if self.duration_ticks < 0:
+            raise ValueError("duration_ticks must be >= 0")
+        if self.rest_ticks < 0:
+            raise ValueError("rest_ticks must be >= 0")
+        if self.dup_count is not None and self.dup_count < 1:
+            raise ValueError("dup_count must be >= 1 when set")
+        if (self.string is None) ^ (self.fret is None):
+            raise ValueError("string and fret must both be set or both be None")
+
+        is_rest = self.rest_ticks > 0
+        is_pitched = self.duration_ticks > 0
+        if is_rest == is_pitched:
+            raise ValueError("voice event must be exactly one of rest or pitched")
+
+        if is_rest:
+            if self.dup_count is not None:
+                raise ValueError("rest event cannot set dup_count")
+            if self.harm_oct is not None or self.harm_class is not None:
+                raise ValueError("rest event cannot set harmonic interval fields")
+            if self.string is not None or self.fret is not None:
+                raise ValueError("rest event cannot set tab fields")
+            return
+
+        if (self.harm_oct is None) ^ (self.harm_class is None):
+            raise ValueError("harm_oct and harm_class must both be set or both be None")
+        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
+            raise ValueError("harm_class must be in [0, 11]")
+
+    @property
+    def is_rest(self) -> bool:
+        return self.rest_ticks > 0
+
+
+def tokenize_event_text(text: str) -> List[str]:
+    raw = text.replace("\n", ",")
+    tokens = [tok.strip() for tok in raw.split(",")]
+    return [tok for tok in tokens if tok]
+
+
+def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
+    if start_idx >= len(tokens):
+        raise ValueError("start_idx out of range")
+
+    voice_tok = tokens[start_idx]
+    if not voice_tok.startswith("VOICE_"):
+        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")
+
+    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
+    next_idx = start_idx + 1
+    if next_idx >= len(tokens):
+        raise ValueError(f"truncated VOICE event at index {start_idx}")
+
+    first_tok = tokens[next_idx]
+    if first_tok.startswith("REST_"):
+        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
+        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1
+
+    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
+    next_idx += 1
+
+    dup_count = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
+        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
+        next_idx += 1
+
+    if next_idx + 2 >= len(tokens):
+        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")
+
+    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
+    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
+    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
+    next_idx += 3
+
+    string = None
+    fret = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
+        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
+        next_idx += 1
+        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
+            raise ValueError(f"missing FRET token after STR at index {next_idx}")
+        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
+        next_idx += 1
+    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
+        raise ValueError(f"FRET token must follow STR token at index {next_idx}")
+
+    return (
+        VoiceEvent(
+            voice=voice,
+            duration_ticks=duration_ticks,
+            mel_int=mel_int,
+            harm_oct=harm_oct,
+            harm_class=harm_class,
+            dup_count=dup_count,
+            string=string,
+            fret=fret,
+        ),
+        next_idx,
+    )
+
+
+def serialize_voice_event(event: VoiceEvent) -> List[str]:
+    tokens = [f"VOICE_{event.voice}"]
+    if event.is_rest:
+        tokens.append(f"REST_{event.rest_ticks}")
+        return tokens
+
+    tokens.append(f"DUR_{event.duration_ticks}")
+    if event.dup_count is not None:
+        tokens.append(f"DUP_{event.dup_count}")
+
+    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
+    if event.harm_oct is None:
+        tokens.append("HARM_OCT_NA")
+        tokens.append("HARM_CLASS_NA")
+    else:
+        tokens.append(f"HARM_OCT_{event.harm_oct}")
+        tokens.append(f"HARM_CLASS_{event.harm_class}")
+
+    if event.string is not None:
+        tokens.append(f"STR_{event.string}")
+        tokens.append(f"FRET_{event.fret}")
+
+    return tokens
+
+
+def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
+    parsed: List[Union[str, VoiceEvent]] = []
+    idx = 0
+    while idx < len(tokens):
+        tok = tokens[idx]
+        if tok.startswith("VOICE_"):
+            event, idx = parse_voice_event(tokens, idx)
+            parsed.append(event)
+            continue
+        parsed.append(tok)
+        idx += 1
+    return parsed
+
+
+def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
+    tokens: List[str] = []
+    for item in items:
+        if isinstance(item, VoiceEvent):
+            tokens.extend(serialize_voice_event(item))
+        else:
+            tokens.append(item)
+    return tokens
+
+
+def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
+    return serialize_event_stream(parse_event_stream(tokens))
+
+
+def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
+    if not token.startswith(prefix):
+        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
+    value = token[len(prefix) :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc
+
+
+def _parse_signed_mel(token: str, idx: int) -> int:
+    if not token.startswith("MEL_INT12_"):
+        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
+    value = token[len("MEL_INT12_") :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_OCT_"):
+        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
+    value = token[len("HARM_OCT_") :]
+    if value == "NA":
+        return None
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_CLASS_"):
+        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
+    value = token[len("HARM_CLASS_") :]
+    if value == "NA":
+        return None
+    try:
+        parsed = int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
+    if not (0 <= parsed <= 11):
+        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
+    return parsed


thinking
**Designing tokenizer tests**
codex
{"done":false,"progress_made":true,"summary":"I’ve added a new tokenizer module and started wiring event emission through it so voice bundles are serialized canonically in one place. Next I’m adding targeted unit tests for parse/serialize/canonical stream behavior before running the full verify command.","tests_ran":false,"tests_status":"not_run","next_step":"Add `tests/test_tokenizer.py` and run verify tests.","stop_reason":""}
file update
A /home/stephen-reilly/dev/bach_gen/tests/test_tokenizer.py
import sys
from pathlib import Path

import pytest

sys.path.insert(0, str(Path(__file__).parents[1]))

from src.tokens.tokenizer import (
    VoiceEvent,
    canonicalize_event_stream,
    parse_event_stream,
    parse_voice_event,
    serialize_event_stream,
    serialize_voice_event,
    tokenize_event_text,
)


def test_parse_pitched_voice_event_with_dup_and_tab():
    tokens = [
        "VOICE_2",
        "DUR_12",
        "DUP_3",
        "MEL_INT12_+7",
        "HARM_OCT_1",
        "HARM_CLASS_7",
        "STR_4",
        "FRET_2",
        "POS_6",
    ]

    event, next_idx = parse_voice_event(tokens, 0)
    assert next_idx == 8
    assert event.voice == 2
    assert event.duration_ticks == 12
    assert event.dup_count == 3
    assert event.mel_int == 7
    assert event.harm_oct == 1
    assert event.harm_class == 7
    assert event.string == 4
    assert event.fret == 2


def test_parse_rest_voice_event():
    tokens = ["VOICE_1", "REST_18", "BAR"]
    event, next_idx = parse_voice_event(tokens, 0)
    assert next_idx == 2
    assert event.voice == 1
    assert event.is_rest
    assert event.rest_ticks == 18


def test_serialize_voice_event_uses_canonical_order():
    event = VoiceEvent(
        voice=3,
        duration_ticks=24,
        dup_count=2,
        mel_int=11,
        harm_oct=0,
        harm_class=11,
    )
    assert serialize_voice_event(event) == [
        "VOICE_3",
        "DUR_24",
        "DUP_2",
        "MEL_INT12_+11",
        "HARM_OCT_0",
        "HARM_CLASS_11",
    ]


def test_canonicalize_event_stream_normalizes_mel_sign():
    tokens = [
        "BAR",
        "POS_0",
        "VOICE_0",
        "DUR_6",
        "MEL_INT12_2",
        "HARM_OCT_0",
        "HARM_CLASS_2",
        "VOICE_1",
        "REST_3",
    ]
    assert canonicalize_event_stream(tokens) == [
        "BAR",
        "POS_0",
        "VOICE_0",
        "DUR_6",
        "MEL_INT12_+2",
        "HARM_OCT_0",
        "HARM_CLASS_2",
        "VOICE_1",
        "REST_3",
    ]


def test_parse_event_stream_roundtrip_and_text_tokenization():
    text = "BAR, POS_0, VOICE_0, DUR_4, MEL_INT12_0, HARM_OCT_NA, HARM_CLASS_NA"
    tokens = tokenize_event_text(text)
    parsed = parse_event_stream(tokens)
    assert serialize_event_stream(parsed) == tokens


def test_parse_rejects_mixed_harm_na_state():
    tokens = ["VOICE_0", "DUR_8", "MEL_INT12_0", "HARM_OCT_NA", "HARM_CLASS_2"]
    with pytest.raises(ValueError, match="harm_oct and harm_class must both be set or both be None"):
        parse_voice_event(tokens, 0)
apply_patch(auto_approved=true) exited 0 in 26ms:
Success. Updated the following files:
A tests/test_tokenizer.py
file update:
diff --git a/src/tokens/eventizer.py b/src/tokens/eventizer.py
index f7c666b524c3f03edbf62558e3a89e26917d8d43..53385718babf181fdc8367c1ca227731a1c1b799
--- a/src/tokens/eventizer.py
+++ b/src/tokens/eventizer.py
@@ -12,9 +12,9 @@
     IntervalRepairStats,
     compute_reference_pitch,
     compute_melodic_interval,
-    format_signed_interval,
     harmonic_tokens_for_pitch,
 )
+from src.tokens.tokenizer import VoiceEvent, serialize_voice_event
 
 DEFAULT_MAX_VOICES = 8
 DEFAULT_MAX_OCTAVES_PER_PITCH_CLASS = 3
@@ -584,15 +584,12 @@
                         tokens.append(f"ABS_VOICE_{v}_{ev.pitch}")
                     prev_pitch[v] = ev.pitch
 
-                tokens.append(f"VOICE_{v}")
-                tokens.append(f"DUR_{ev.duration_tick}")
                 base_pitch = prev_pitch[v] if prev_pitch[v] is not None else ev.pitch
                 mel_int = compute_melodic_interval(
                     ev.pitch,
                     base_pitch,
                     qa_mode=True,
                 )
-                tokens.append(f"MEL_INT12_{format_signed_interval(mel_int)}")
 
                 harm_oct_tok, harm_cls_tok = harmonic_tokens_for_pitch(
                     ev.pitch,
@@ -600,8 +597,21 @@
                     qa_mode=qa_mode,
                     stats=interval_repairs,
                 )
-                tokens.append(harm_oct_tok)
-                tokens.append(harm_cls_tok)
+                harm_oct = None if harm_oct_tok.endswith("_NA") else int(harm_oct_tok.rsplit("_", 1)[1])
+                harm_class = (
+                    None if harm_cls_tok.endswith("_NA") else int(harm_cls_tok.rsplit("_", 1)[1])
+                )
+                tokens.extend(
+                    serialize_voice_event(
+                        VoiceEvent(
+                            voice=v,
+                            duration_ticks=ev.duration_tick,
+                            mel_int=mel_int,
+                            harm_oct=harm_oct,
+                            harm_class=harm_class,
+                        )
+                    )
+                )
 
                 prev_pitch[v] = ev.pitch
                 active_until[v] = t + ev.duration_tick
diff --git a/src/tokens/tokenizer.py b/src/tokens/tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..3d7839be70301fe3c5376ba858263c8c4f66da1b
--- /dev/null
+++ b/src/tokens/tokenizer.py
@@ -0,0 +1,219 @@
+from dataclasses import dataclass
+from typing import List, Sequence, Tuple, Union
+
+from src.tokens.intervals import format_signed_interval
+
+
+@dataclass(frozen=True)
+class VoiceEvent:
+    voice: int
+    duration_ticks: int = 0
+    rest_ticks: int = 0
+    mel_int: int = 0
+    harm_oct: Union[int, None] = None
+    harm_class: Union[int, None] = None
+    dup_count: Union[int, None] = None
+    string: Union[int, None] = None
+    fret: Union[int, None] = None
+
+    def __post_init__(self) -> None:
+        if self.voice < 0:
+            raise ValueError("voice must be >= 0")
+        if self.duration_ticks < 0:
+            raise ValueError("duration_ticks must be >= 0")
+        if self.rest_ticks < 0:
+            raise ValueError("rest_ticks must be >= 0")
+        if self.dup_count is not None and self.dup_count < 1:
+            raise ValueError("dup_count must be >= 1 when set")
+        if (self.string is None) ^ (self.fret is None):
+            raise ValueError("string and fret must both be set or both be None")
+
+        is_rest = self.rest_ticks > 0
+        is_pitched = self.duration_ticks > 0
+        if is_rest == is_pitched:
+            raise ValueError("voice event must be exactly one of rest or pitched")
+
+        if is_rest:
+            if self.dup_count is not None:
+                raise ValueError("rest event cannot set dup_count")
+            if self.harm_oct is not None or self.harm_class is not None:
+                raise ValueError("rest event cannot set harmonic interval fields")
+            if self.string is not None or self.fret is not None:
+                raise ValueError("rest event cannot set tab fields")
+            return
+
+        if (self.harm_oct is None) ^ (self.harm_class is None):
+            raise ValueError("harm_oct and harm_class must both be set or both be None")
+        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
+            raise ValueError("harm_class must be in [0, 11]")
+
+    @property
+    def is_rest(self) -> bool:
+        return self.rest_ticks > 0
+
+
+def tokenize_event_text(text: str) -> List[str]:
+    raw = text.replace("\n", ",")
+    tokens = [tok.strip() for tok in raw.split(",")]
+    return [tok for tok in tokens if tok]
+
+
+def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
+    if start_idx >= len(tokens):
+        raise ValueError("start_idx out of range")
+
+    voice_tok = tokens[start_idx]
+    if not voice_tok.startswith("VOICE_"):
+        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")
+
+    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
+    next_idx = start_idx + 1
+    if next_idx >= len(tokens):
+        raise ValueError(f"truncated VOICE event at index {start_idx}")
+
+    first_tok = tokens[next_idx]
+    if first_tok.startswith("REST_"):
+        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
+        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1
+
+    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
+    next_idx += 1
+
+    dup_count = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
+        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
+        next_idx += 1
+
+    if next_idx + 2 >= len(tokens):
+        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")
+
+    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
+    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
+    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
+    next_idx += 3
+
+    string = None
+    fret = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
+        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
+        next_idx += 1
+        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
+            raise ValueError(f"missing FRET token after STR at index {next_idx}")
+        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
+        next_idx += 1
+    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
+        raise ValueError(f"FRET token must follow STR token at index {next_idx}")
+
+    return (
+        VoiceEvent(
+            voice=voice,
+            duration_ticks=duration_ticks,
+            mel_int=mel_int,
+            harm_oct=harm_oct,
+            harm_class=harm_class,
+            dup_count=dup_count,
+            string=string,
+            fret=fret,
+        ),
+        next_idx,
+    )
+
+
+def serialize_voice_event(event: VoiceEvent) -> List[str]:
+    tokens = [f"VOICE_{event.voice}"]
+    if event.is_rest:
+        tokens.append(f"REST_{event.rest_ticks}")
+        return tokens
+
+    tokens.append(f"DUR_{event.duration_ticks}")
+    if event.dup_count is not None:
+        tokens.append(f"DUP_{event.dup_count}")
+
+    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
+    if event.harm_oct is None:
+        tokens.append("HARM_OCT_NA")
+        tokens.append("HARM_CLASS_NA")
+    else:
+        tokens.append(f"HARM_OCT_{event.harm_oct}")
+        tokens.append(f"HARM_CLASS_{event.harm_class}")
+
+    if event.string is not None:
+        tokens.append(f"STR_{event.string}")
+        tokens.append(f"FRET_{event.fret}")
+
+    return tokens
+
+
+def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
+    parsed: List[Union[str, VoiceEvent]] = []
+    idx = 0
+    while idx < len(tokens):
+        tok = tokens[idx]
+        if tok.startswith("VOICE_"):
+            event, idx = parse_voice_event(tokens, idx)
+            parsed.append(event)
+            continue
+        parsed.append(tok)
+        idx += 1
+    return parsed
+
+
+def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
+    tokens: List[str] = []
+    for item in items:
+        if isinstance(item, VoiceEvent):
+            tokens.extend(serialize_voice_event(item))
+        else:
+            tokens.append(item)
+    return tokens
+
+
+def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
+    return serialize_event_stream(parse_event_stream(tokens))
+
+
+def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
+    if not token.startswith(prefix):
+        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
+    value = token[len(prefix) :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc
+
+
+def _parse_signed_mel(token: str, idx: int) -> int:
+    if not token.startswith("MEL_INT12_"):
+        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
+    value = token[len("MEL_INT12_") :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_OCT_"):
+        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
+    value = token[len("HARM_OCT_") :]
+    if value == "NA":
+        return None
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_CLASS_"):
+        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
+    value = token[len("HARM_CLASS_") :]
+    if value == "NA":
+        return None
+    try:
+        parsed = int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
+    if not (0 <= parsed <= 11):
+        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
+    return parsed
diff --git a/tests/test_tokenizer.py b/tests/test_tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..4f4741f7ebc6a851a695b5f6081e10cfc2c03f03
--- /dev/null
+++ b/tests/test_tokenizer.py
@@ -0,0 +1,107 @@
+import sys
+from pathlib import Path
+
+import pytest
+
+sys.path.insert(0, str(Path(__file__).parents[1]))
+
+from src.tokens.tokenizer import (
+    VoiceEvent,
+    canonicalize_event_stream,
+    parse_event_stream,
+    parse_voice_event,
+    serialize_event_stream,
+    serialize_voice_event,
+    tokenize_event_text,
+)
+
+
+def test_parse_pitched_voice_event_with_dup_and_tab():
+    tokens = [
+        "VOICE_2",
+        "DUR_12",
+        "DUP_3",
+        "MEL_INT12_+7",
+        "HARM_OCT_1",
+        "HARM_CLASS_7",
+        "STR_4",
+        "FRET_2",
+        "POS_6",
+    ]
+
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 8
+    assert event.voice == 2
+    assert event.duration_ticks == 12
+    assert event.dup_count == 3
+    assert event.mel_int == 7
+    assert event.harm_oct == 1
+    assert event.harm_class == 7
+    assert event.string == 4
+    assert event.fret == 2
+
+
+def test_parse_rest_voice_event():
+    tokens = ["VOICE_1", "REST_18", "BAR"]
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 2
+    assert event.voice == 1
+    assert event.is_rest
+    assert event.rest_ticks == 18
+
+
+def test_serialize_voice_event_uses_canonical_order():
+    event = VoiceEvent(
+        voice=3,
+        duration_ticks=24,
+        dup_count=2,
+        mel_int=11,
+        harm_oct=0,
+        harm_class=11,
+    )
+    assert serialize_voice_event(event) == [
+        "VOICE_3",
+        "DUR_24",
+        "DUP_2",
+        "MEL_INT12_+11",
+        "HARM_OCT_0",
+        "HARM_CLASS_11",
+    ]
+
+
+def test_canonicalize_event_stream_normalizes_mel_sign():
+    tokens = [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+    assert canonicalize_event_stream(tokens) == [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_+2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+
+
+def test_parse_event_stream_roundtrip_and_text_tokenization():
+    text = "BAR, POS_0, VOICE_0, DUR_4, MEL_INT12_0, HARM_OCT_NA, HARM_CLASS_NA"
+    tokens = tokenize_event_text(text)
+    parsed = parse_event_stream(tokens)
+    assert serialize_event_stream(parsed) == tokens
+
+
+def test_parse_rejects_mixed_harm_na_state():
+    tokens = ["VOICE_0", "DUR_8", "MEL_INT12_0", "HARM_OCT_NA", "HARM_CLASS_2"]
+    with pytest.raises(ValueError, match="harm_oct and harm_class must both be set or both be None"):
+        parse_voice_event(tokens, 0)

file update:
diff --git a/src/tokens/eventizer.py b/src/tokens/eventizer.py
index f7c666b524c3f03edbf62558e3a89e26917d8d43..53385718babf181fdc8367c1ca227731a1c1b799
--- a/src/tokens/eventizer.py
+++ b/src/tokens/eventizer.py
@@ -12,9 +12,9 @@
     IntervalRepairStats,
     compute_reference_pitch,
     compute_melodic_interval,
-    format_signed_interval,
     harmonic_tokens_for_pitch,
 )
+from src.tokens.tokenizer import VoiceEvent, serialize_voice_event
 
 DEFAULT_MAX_VOICES = 8
 DEFAULT_MAX_OCTAVES_PER_PITCH_CLASS = 3
@@ -584,15 +584,12 @@
                         tokens.append(f"ABS_VOICE_{v}_{ev.pitch}")
                     prev_pitch[v] = ev.pitch
 
-                tokens.append(f"VOICE_{v}")
-                tokens.append(f"DUR_{ev.duration_tick}")
                 base_pitch = prev_pitch[v] if prev_pitch[v] is not None else ev.pitch
                 mel_int = compute_melodic_interval(
                     ev.pitch,
                     base_pitch,
                     qa_mode=True,
                 )
-                tokens.append(f"MEL_INT12_{format_signed_interval(mel_int)}")
 
                 harm_oct_tok, harm_cls_tok = harmonic_tokens_for_pitch(
                     ev.pitch,
@@ -600,8 +597,21 @@
                     qa_mode=qa_mode,
                     stats=interval_repairs,
                 )
-                tokens.append(harm_oct_tok)
-                tokens.append(harm_cls_tok)
+                harm_oct = None if harm_oct_tok.endswith("_NA") else int(harm_oct_tok.rsplit("_", 1)[1])
+                harm_class = (
+                    None if harm_cls_tok.endswith("_NA") else int(harm_cls_tok.rsplit("_", 1)[1])
+                )
+                tokens.extend(
+                    serialize_voice_event(
+                        VoiceEvent(
+                            voice=v,
+                            duration_ticks=ev.duration_tick,
+                            mel_int=mel_int,
+                            harm_oct=harm_oct,
+                            harm_class=harm_class,
+                        )
+                    )
+                )
 
                 prev_pitch[v] = ev.pitch
                 active_until[v] = t + ev.duration_tick
diff --git a/src/tokens/tokenizer.py b/src/tokens/tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..3d7839be70301fe3c5376ba858263c8c4f66da1b
--- /dev/null
+++ b/src/tokens/tokenizer.py
@@ -0,0 +1,219 @@
+from dataclasses import dataclass
+from typing import List, Sequence, Tuple, Union
+
+from src.tokens.intervals import format_signed_interval
+
+
+@dataclass(frozen=True)
+class VoiceEvent:
+    voice: int
+    duration_ticks: int = 0
+    rest_ticks: int = 0
+    mel_int: int = 0
+    harm_oct: Union[int, None] = None
+    harm_class: Union[int, None] = None
+    dup_count: Union[int, None] = None
+    string: Union[int, None] = None
+    fret: Union[int, None] = None
+
+    def __post_init__(self) -> None:
+        if self.voice < 0:
+            raise ValueError("voice must be >= 0")
+        if self.duration_ticks < 0:
+            raise ValueError("duration_ticks must be >= 0")
+        if self.rest_ticks < 0:
+            raise ValueError("rest_ticks must be >= 0")
+        if self.dup_count is not None and self.dup_count < 1:
+            raise ValueError("dup_count must be >= 1 when set")
+        if (self.string is None) ^ (self.fret is None):
+            raise ValueError("string and fret must both be set or both be None")
+
+        is_rest = self.rest_ticks > 0
+        is_pitched = self.duration_ticks > 0
+        if is_rest == is_pitched:
+            raise ValueError("voice event must be exactly one of rest or pitched")
+
+        if is_rest:
+            if self.dup_count is not None:
+                raise ValueError("rest event cannot set dup_count")
+            if self.harm_oct is not None or self.harm_class is not None:
+                raise ValueError("rest event cannot set harmonic interval fields")
+            if self.string is not None or self.fret is not None:
+                raise ValueError("rest event cannot set tab fields")
+            return
+
+        if (self.harm_oct is None) ^ (self.harm_class is None):
+            raise ValueError("harm_oct and harm_class must both be set or both be None")
+        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
+            raise ValueError("harm_class must be in [0, 11]")
+
+    @property
+    def is_rest(self) -> bool:
+        return self.rest_ticks > 0
+
+
+def tokenize_event_text(text: str) -> List[str]:
+    raw = text.replace("\n", ",")
+    tokens = [tok.strip() for tok in raw.split(",")]
+    return [tok for tok in tokens if tok]
+
+
+def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
+    if start_idx >= len(tokens):
+        raise ValueError("start_idx out of range")
+
+    voice_tok = tokens[start_idx]
+    if not voice_tok.startswith("VOICE_"):
+        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")
+
+    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
+    next_idx = start_idx + 1
+    if next_idx >= len(tokens):
+        raise ValueError(f"truncated VOICE event at index {start_idx}")
+
+    first_tok = tokens[next_idx]
+    if first_tok.startswith("REST_"):
+        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
+        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1
+
+    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
+    next_idx += 1
+
+    dup_count = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
+        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
+        next_idx += 1
+
+    if next_idx + 2 >= len(tokens):
+        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")
+
+    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
+    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
+    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
+    next_idx += 3
+
+    string = None
+    fret = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
+        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
+        next_idx += 1
+        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
+            raise ValueError(f"missing FRET token after STR at index {next_idx}")
+        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
+        next_idx += 1
+    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
+        raise ValueError(f"FRET token must follow STR token at index {next_idx}")
+
+    return (
+        VoiceEvent(
+            voice=voice,
+            duration_ticks=duration_ticks,
+            mel_int=mel_int,
+            harm_oct=harm_oct,
+            harm_class=harm_class,
+            dup_count=dup_count,
+            string=string,
+            fret=fret,
+        ),
+        next_idx,
+    )
+
+
+def serialize_voice_event(event: VoiceEvent) -> List[str]:
+    tokens = [f"VOICE_{event.voice}"]
+    if event.is_rest:
+        tokens.append(f"REST_{event.rest_ticks}")
+        return tokens
+
+    tokens.append(f"DUR_{event.duration_ticks}")
+    if event.dup_count is not None:
+        tokens.append(f"DUP_{event.dup_count}")
+
+    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
+    if event.harm_oct is None:
+        tokens.append("HARM_OCT_NA")
+        tokens.append("HARM_CLASS_NA")
+    else:
+        tokens.append(f"HARM_OCT_{event.harm_oct}")
+        tokens.append(f"HARM_CLASS_{event.harm_class}")
+
+    if event.string is not None:
+        tokens.append(f"STR_{event.string}")
+        tokens.append(f"FRET_{event.fret}")
+
+    return tokens
+
+
+def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
+    parsed: List[Union[str, VoiceEvent]] = []
+    idx = 0
+    while idx < len(tokens):
+        tok = tokens[idx]
+        if tok.startswith("VOICE_"):
+            event, idx = parse_voice_event(tokens, idx)
+            parsed.append(event)
+            continue
+        parsed.append(tok)
+        idx += 1
+    return parsed
+
+
+def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
+    tokens: List[str] = []
+    for item in items:
+        if isinstance(item, VoiceEvent):
+            tokens.extend(serialize_voice_event(item))
+        else:
+            tokens.append(item)
+    return tokens
+
+
+def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
+    return serialize_event_stream(parse_event_stream(tokens))
+
+
+def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
+    if not token.startswith(prefix):
+        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
+    value = token[len(prefix) :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc
+
+
+def _parse_signed_mel(token: str, idx: int) -> int:
+    if not token.startswith("MEL_INT12_"):
+        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
+    value = token[len("MEL_INT12_") :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_OCT_"):
+        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
+    value = token[len("HARM_OCT_") :]
+    if value == "NA":
+        return None
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_CLASS_"):
+        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
+    value = token[len("HARM_CLASS_") :]
+    if value == "NA":
+        return None
+    try:
+        parsed = int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
+    if not (0 <= parsed <= 11):
+        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
+    return parsed
diff --git a/tests/test_tokenizer.py b/tests/test_tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..4f4741f7ebc6a851a695b5f6081e10cfc2c03f03
--- /dev/null
+++ b/tests/test_tokenizer.py
@@ -0,0 +1,107 @@
+import sys
+from pathlib import Path
+
+import pytest
+
+sys.path.insert(0, str(Path(__file__).parents[1]))
+
+from src.tokens.tokenizer import (
+    VoiceEvent,
+    canonicalize_event_stream,
+    parse_event_stream,
+    parse_voice_event,
+    serialize_event_stream,
+    serialize_voice_event,
+    tokenize_event_text,
+)
+
+
+def test_parse_pitched_voice_event_with_dup_and_tab():
+    tokens = [
+        "VOICE_2",
+        "DUR_12",
+        "DUP_3",
+        "MEL_INT12_+7",
+        "HARM_OCT_1",
+        "HARM_CLASS_7",
+        "STR_4",
+        "FRET_2",
+        "POS_6",
+    ]
+
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 8
+    assert event.voice == 2
+    assert event.duration_ticks == 12
+    assert event.dup_count == 3
+    assert event.mel_int == 7
+    assert event.harm_oct == 1
+    assert event.harm_class == 7
+    assert event.string == 4
+    assert event.fret == 2
+
+
+def test_parse_rest_voice_event():
+    tokens = ["VOICE_1", "REST_18", "BAR"]
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 2
+    assert event.voice == 1
+    assert event.is_rest
+    assert event.rest_ticks == 18
+
+
+def test_serialize_voice_event_uses_canonical_order():
+    event = VoiceEvent(
+        voice=3,
+        duration_ticks=24,
+        dup_count=2,
+        mel_int=11,
+        harm_oct=0,
+        harm_class=11,
+    )
+    assert serialize_voice_event(event) == [
+        "VOICE_3",
+        "DUR_24",
+        "DUP_2",
+        "MEL_INT12_+11",
+        "HARM_OCT_0",
+        "HARM_CLASS_11",
+    ]
+
+
+def test_canonicalize_event_stream_normalizes_mel_sign():
+    tokens = [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+    assert canonicalize_event_stream(tokens) == [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_+2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+
+
+def test_parse_event_stream_roundtrip_and_text_tokenization():
+    text = "BAR, POS_0, VOICE_0, DUR_4, MEL_INT12_0, HARM_OCT_NA, HARM_CLASS_NA"
+    tokens = tokenize_event_text(text)
+    parsed = parse_event_stream(tokens)
+    assert serialize_event_stream(parsed) == tokens
+
+
+def test_parse_rejects_mixed_harm_na_state():
+    tokens = ["VOICE_0", "DUR_8", "MEL_INT12_0", "HARM_OCT_NA", "HARM_CLASS_2"]
+    with pytest.raises(ValueError, match="harm_oct and harm_class must both be set or both be None"):
+        parse_voice_event(tokens, 0)


thinking
**Running full test suite**
exec
/bin/bash -lc 'bash docs/skills/python-test-env/scripts/run_tests.sh' in /home/stephen-reilly/dev/bach_genfile update:
diff --git a/src/tokens/eventizer.py b/src/tokens/eventizer.py
index f7c666b524c3f03edbf62558e3a89e26917d8d43..53385718babf181fdc8367c1ca227731a1c1b799
--- a/src/tokens/eventizer.py
+++ b/src/tokens/eventizer.py
@@ -12,9 +12,9 @@
     IntervalRepairStats,
     compute_reference_pitch,
     compute_melodic_interval,
-    format_signed_interval,
     harmonic_tokens_for_pitch,
 )
+from src.tokens.tokenizer import VoiceEvent, serialize_voice_event
 
 DEFAULT_MAX_VOICES = 8
 DEFAULT_MAX_OCTAVES_PER_PITCH_CLASS = 3
@@ -584,15 +584,12 @@
                         tokens.append(f"ABS_VOICE_{v}_{ev.pitch}")
                     prev_pitch[v] = ev.pitch
 
-                tokens.append(f"VOICE_{v}")
-                tokens.append(f"DUR_{ev.duration_tick}")
                 base_pitch = prev_pitch[v] if prev_pitch[v] is not None else ev.pitch
                 mel_int = compute_melodic_interval(
                     ev.pitch,
                     base_pitch,
                     qa_mode=True,
                 )
-                tokens.append(f"MEL_INT12_{format_signed_interval(mel_int)}")
 
                 harm_oct_tok, harm_cls_tok = harmonic_tokens_for_pitch(
                     ev.pitch,
@@ -600,8 +597,21 @@
                     qa_mode=qa_mode,
                     stats=interval_repairs,
                 )
-                tokens.append(harm_oct_tok)
-                tokens.append(harm_cls_tok)
+                harm_oct = None if harm_oct_tok.endswith("_NA") else int(harm_oct_tok.rsplit("_", 1)[1])
+                harm_class = (
+                    None if harm_cls_tok.endswith("_NA") else int(harm_cls_tok.rsplit("_", 1)[1])
+                )
+                tokens.extend(
+                    serialize_voice_event(
+                        VoiceEvent(
+                            voice=v,
+                            duration_ticks=ev.duration_tick,
+                            mel_int=mel_int,
+                            harm_oct=harm_oct,
+                            harm_class=harm_class,
+                        )
+                    )
+                )
 
                 prev_pitch[v] = ev.pitch
                 active_until[v] = t + ev.duration_tick
diff --git a/src/tokens/tokenizer.py b/src/tokens/tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..3d7839be70301fe3c5376ba858263c8c4f66da1b
--- /dev/null
+++ b/src/tokens/tokenizer.py
@@ -0,0 +1,219 @@
+from dataclasses import dataclass
+from typing import List, Sequence, Tuple, Union
+
+from src.tokens.intervals import format_signed_interval
+
+
+@dataclass(frozen=True)
+class VoiceEvent:
+    voice: int
+    duration_ticks: int = 0
+    rest_ticks: int = 0
+    mel_int: int = 0
+    harm_oct: Union[int, None] = None
+    harm_class: Union[int, None] = None
+    dup_count: Union[int, None] = None
+    string: Union[int, None] = None
+    fret: Union[int, None] = None
+
+    def __post_init__(self) -> None:
+        if self.voice < 0:
+            raise ValueError("voice must be >= 0")
+        if self.duration_ticks < 0:
+            raise ValueError("duration_ticks must be >= 0")
+        if self.rest_ticks < 0:
+            raise ValueError("rest_ticks must be >= 0")
+        if self.dup_count is not None and self.dup_count < 1:
+            raise ValueError("dup_count must be >= 1 when set")
+        if (self.string is None) ^ (self.fret is None):
+            raise ValueError("string and fret must both be set or both be None")
+
+        is_rest = self.rest_ticks > 0
+        is_pitched = self.duration_ticks > 0
+        if is_rest == is_pitched:
+            raise ValueError("voice event must be exactly one of rest or pitched")
+
+        if is_rest:
+            if self.dup_count is not None:
+                raise ValueError("rest event cannot set dup_count")
+            if self.harm_oct is not None or self.harm_class is not None:
+                raise ValueError("rest event cannot set harmonic interval fields")
+            if self.string is not None or self.fret is not None:
+                raise ValueError("rest event cannot set tab fields")
+            return
+
+        if (self.harm_oct is None) ^ (self.harm_class is None):
+            raise ValueError("harm_oct and harm_class must both be set or both be None")
+        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
+            raise ValueError("harm_class must be in [0, 11]")
+
+    @property
+    def is_rest(self) -> bool:
+        return self.rest_ticks > 0
+
+
+def tokenize_event_text(text: str) -> List[str]:
+    raw = text.replace("\n", ",")
+    tokens = [tok.strip() for tok in raw.split(",")]
+    return [tok for tok in tokens if tok]
+
+
+def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
+    if start_idx >= len(tokens):
+        raise ValueError("start_idx out of range")
+
+    voice_tok = tokens[start_idx]
+    if not voice_tok.startswith("VOICE_"):
+        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")
+
+    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
+    next_idx = start_idx + 1
+    if next_idx >= len(tokens):
+        raise ValueError(f"truncated VOICE event at index {start_idx}")
+
+    first_tok = tokens[next_idx]
+    if first_tok.startswith("REST_"):
+        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
+        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1
+
+    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
+    next_idx += 1
+
+    dup_count = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
+        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
+        next_idx += 1
+
+    if next_idx + 2 >= len(tokens):
+        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")
+
+    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
+    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
+    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
+    next_idx += 3
+
+    string = None
+    fret = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
+        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
+        next_idx += 1
+        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
+            raise ValueError(f"missing FRET token after STR at index {next_idx}")
+        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
+        next_idx += 1
+    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
+        raise ValueError(f"FRET token must follow STR token at index {next_idx}")
+
+    return (
+        VoiceEvent(
+            voice=voice,
+            duration_ticks=duration_ticks,
+            mel_int=mel_int,
+            harm_oct=harm_oct,
+            harm_class=harm_class,
+            dup_count=dup_count,
+            string=string,
+            fret=fret,
+        ),
+        next_idx,
+    )
+
+
+def serialize_voice_event(event: VoiceEvent) -> List[str]:
+    tokens = [f"VOICE_{event.voice}"]
+    if event.is_rest:
+        tokens.append(f"REST_{event.rest_ticks}")
+        return tokens
+
+    tokens.append(f"DUR_{event.duration_ticks}")
+    if event.dup_count is not None:
+        tokens.append(f"DUP_{event.dup_count}")
+
+    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
+    if event.harm_oct is None:
+        tokens.append("HARM_OCT_NA")
+        tokens.append("HARM_CLASS_NA")
+    else:
+        tokens.append(f"HARM_OCT_{event.harm_oct}")
+        tokens.append(f"HARM_CLASS_{event.harm_class}")
+
+    if event.string is not None:
+        tokens.append(f"STR_{event.string}")
+        tokens.append(f"FRET_{event.fret}")
+
+    return tokens
+
+
+def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
+    parsed: List[Union[str, VoiceEvent]] = []
+    idx = 0
+    while idx < len(tokens):
+        tok = tokens[idx]
+        if tok.startswith("VOICE_"):
+            event, idx = parse_voice_event(tokens, idx)
+            parsed.append(event)
+            continue
+        parsed.append(tok)
+        idx += 1
+    return parsed
+
+
+def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
+    tokens: List[str] = []
+    for item in items:
+        if isinstance(item, VoiceEvent):
+            tokens.extend(serialize_voice_event(item))
+        else:
+            tokens.append(item)
+    return tokens
+
+
+def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
+    return serialize_event_stream(parse_event_stream(tokens))
+
+
+def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
+    if not token.startswith(prefix):
+        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
+    value = token[len(prefix) :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc
+
+
+def _parse_signed_mel(token: str, idx: int) -> int:
+    if not token.startswith("MEL_INT12_"):
+        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
+    value = token[len("MEL_INT12_") :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_OCT_"):
+        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
+    value = token[len("HARM_OCT_") :]
+    if value == "NA":
+        return None
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_CLASS_"):
+        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
+    value = token[len("HARM_CLASS_") :]
+    if value == "NA":
+        return None
+    try:
+        parsed = int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
+    if not (0 <= parsed <= 11):
+        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
+    return parsed
diff --git a/tests/test_tokenizer.py b/tests/test_tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..4f4741f7ebc6a851a695b5f6081e10cfc2c03f03
--- /dev/null
+++ b/tests/test_tokenizer.py
@@ -0,0 +1,107 @@
+import sys
+from pathlib import Path
+
+import pytest
+
+sys.path.insert(0, str(Path(__file__).parents[1]))
+
+from src.tokens.tokenizer import (
+    VoiceEvent,
+    canonicalize_event_stream,
+    parse_event_stream,
+    parse_voice_event,
+    serialize_event_stream,
+    serialize_voice_event,
+    tokenize_event_text,
+)
+
+
+def test_parse_pitched_voice_event_with_dup_and_tab():
+    tokens = [
+        "VOICE_2",
+        "DUR_12",
+        "DUP_3",
+        "MEL_INT12_+7",
+        "HARM_OCT_1",
+        "HARM_CLASS_7",
+        "STR_4",
+        "FRET_2",
+        "POS_6",
+    ]
+
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 8
+    assert event.voice == 2
+    assert event.duration_ticks == 12
+    assert event.dup_count == 3
+    assert event.mel_int == 7
+    assert event.harm_oct == 1
+    assert event.harm_class == 7
+    assert event.string == 4
+    assert event.fret == 2
+
+
+def test_parse_rest_voice_event():
+    tokens = ["VOICE_1", "REST_18", "BAR"]
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 2
+    assert event.voice == 1
+    assert event.is_rest
+    assert event.rest_ticks == 18
+
+
+def test_serialize_voice_event_uses_canonical_order():
+    event = VoiceEvent(
+        voice=3,
+        duration_ticks=24,
+        dup_count=2,
+        mel_int=11,
+        harm_oct=0,
+        harm_class=11,
+    )
+    assert serialize_voice_event(event) == [
+        "VOICE_3",
+        "DUR_24",
+        "DUP_2",
+        "MEL_INT12_+11",
+        "HARM_OCT_0",
+        "HARM_CLASS_11",
+    ]
+
+
+def test_canonicalize_event_stream_normalizes_mel_sign():
+    tokens = [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+    assert canonicalize_event_stream(tokens) == [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_+2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+
+
+def test_parse_event_stream_roundtrip_and_text_tokenization():
+    text = "BAR, POS_0, VOICE_0, DUR_4, MEL_INT12_0, HARM_OCT_NA, HARM_CLASS_NA"
+    tokens = tokenize_event_text(text)
+    parsed = parse_event_stream(tokens)
+    assert serialize_event_stream(parsed) == tokens
+
+
+def test_parse_rejects_mixed_harm_na_state():
+    tokens = ["VOICE_0", "DUR_8", "MEL_INT12_0", "HARM_OCT_NA", "HARM_CLASS_2"]
+    with pytest.raises(ValueError, match="harm_oct and harm_class must both be set or both be None"):
+        parse_voice_event(tokens, 0)


thinking
**Updating on process status**
codex
{"done":false,"progress_made":true,"summary":"Code and tests are in place; I’m running the full repository verify command now (`bash docs/skills/python-test-env/scripts/run_tests.sh`) to validate the change before updating TODO/PROGRESS.","tests_ran":true,"tests_status":"not_run","next_step":"Wait for test completion, then update TODO.md and PROGRESS.md.","stop_reason":""}
file update:
diff --git a/src/tokens/eventizer.py b/src/tokens/eventizer.py
index f7c666b524c3f03edbf62558e3a89e26917d8d43..53385718babf181fdc8367c1ca227731a1c1b799
--- a/src/tokens/eventizer.py
+++ b/src/tokens/eventizer.py
@@ -12,9 +12,9 @@
     IntervalRepairStats,
     compute_reference_pitch,
     compute_melodic_interval,
-    format_signed_interval,
     harmonic_tokens_for_pitch,
 )
+from src.tokens.tokenizer import VoiceEvent, serialize_voice_event
 
 DEFAULT_MAX_VOICES = 8
 DEFAULT_MAX_OCTAVES_PER_PITCH_CLASS = 3
@@ -584,15 +584,12 @@
                         tokens.append(f"ABS_VOICE_{v}_{ev.pitch}")
                     prev_pitch[v] = ev.pitch
 
-                tokens.append(f"VOICE_{v}")
-                tokens.append(f"DUR_{ev.duration_tick}")
                 base_pitch = prev_pitch[v] if prev_pitch[v] is not None else ev.pitch
                 mel_int = compute_melodic_interval(
                     ev.pitch,
                     base_pitch,
                     qa_mode=True,
                 )
-                tokens.append(f"MEL_INT12_{format_signed_interval(mel_int)}")
 
                 harm_oct_tok, harm_cls_tok = harmonic_tokens_for_pitch(
                     ev.pitch,
@@ -600,8 +597,21 @@
                     qa_mode=qa_mode,
                     stats=interval_repairs,
                 )
-                tokens.append(harm_oct_tok)
-                tokens.append(harm_cls_tok)
+                harm_oct = None if harm_oct_tok.endswith("_NA") else int(harm_oct_tok.rsplit("_", 1)[1])
+                harm_class = (
+                    None if harm_cls_tok.endswith("_NA") else int(harm_cls_tok.rsplit("_", 1)[1])
+                )
+                tokens.extend(
+                    serialize_voice_event(
+                        VoiceEvent(
+                            voice=v,
+                            duration_ticks=ev.duration_tick,
+                            mel_int=mel_int,
+                            harm_oct=harm_oct,
+                            harm_class=harm_class,
+                        )
+                    )
+                )
 
                 prev_pitch[v] = ev.pitch
                 active_until[v] = t + ev.duration_tick
diff --git a/src/tokens/tokenizer.py b/src/tokens/tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..3d7839be70301fe3c5376ba858263c8c4f66da1b
--- /dev/null
+++ b/src/tokens/tokenizer.py
@@ -0,0 +1,219 @@
+from dataclasses import dataclass
+from typing import List, Sequence, Tuple, Union
+
+from src.tokens.intervals import format_signed_interval
+
+
+@dataclass(frozen=True)
+class VoiceEvent:
+    voice: int
+    duration_ticks: int = 0
+    rest_ticks: int = 0
+    mel_int: int = 0
+    harm_oct: Union[int, None] = None
+    harm_class: Union[int, None] = None
+    dup_count: Union[int, None] = None
+    string: Union[int, None] = None
+    fret: Union[int, None] = None
+
+    def __post_init__(self) -> None:
+        if self.voice < 0:
+            raise ValueError("voice must be >= 0")
+        if self.duration_ticks < 0:
+            raise ValueError("duration_ticks must be >= 0")
+        if self.rest_ticks < 0:
+            raise ValueError("rest_ticks must be >= 0")
+        if self.dup_count is not None and self.dup_count < 1:
+            raise ValueError("dup_count must be >= 1 when set")
+        if (self.string is None) ^ (self.fret is None):
+            raise ValueError("string and fret must both be set or both be None")
+
+        is_rest = self.rest_ticks > 0
+        is_pitched = self.duration_ticks > 0
+        if is_rest == is_pitched:
+            raise ValueError("voice event must be exactly one of rest or pitched")
+
+        if is_rest:
+            if self.dup_count is not None:
+                raise ValueError("rest event cannot set dup_count")
+            if self.harm_oct is not None or self.harm_class is not None:
+                raise ValueError("rest event cannot set harmonic interval fields")
+            if self.string is not None or self.fret is not None:
+                raise ValueError("rest event cannot set tab fields")
+            return
+
+        if (self.harm_oct is None) ^ (self.harm_class is None):
+            raise ValueError("harm_oct and harm_class must both be set or both be None")
+        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
+            raise ValueError("harm_class must be in [0, 11]")
+
+    @property
+    def is_rest(self) -> bool:
+        return self.rest_ticks > 0
+
+
+def tokenize_event_text(text: str) -> List[str]:
+    raw = text.replace("\n", ",")
+    tokens = [tok.strip() for tok in raw.split(",")]
+    return [tok for tok in tokens if tok]
+
+
+def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
+    if start_idx >= len(tokens):
+        raise ValueError("start_idx out of range")
+
+    voice_tok = tokens[start_idx]
+    if not voice_tok.startswith("VOICE_"):
+        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")
+
+    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
+    next_idx = start_idx + 1
+    if next_idx >= len(tokens):
+        raise ValueError(f"truncated VOICE event at index {start_idx}")
+
+    first_tok = tokens[next_idx]
+    if first_tok.startswith("REST_"):
+        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
+        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1
+
+    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
+    next_idx += 1
+
+    dup_count = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
+        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
+        next_idx += 1
+
+    if next_idx + 2 >= len(tokens):
+        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")
+
+    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
+    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
+    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
+    next_idx += 3
+
+    string = None
+    fret = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
+        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
+        next_idx += 1
+        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
+            raise ValueError(f"missing FRET token after STR at index {next_idx}")
+        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
+        next_idx += 1
+    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
+        raise ValueError(f"FRET token must follow STR token at index {next_idx}")
+
+    return (
+        VoiceEvent(
+            voice=voice,
+            duration_ticks=duration_ticks,
+            mel_int=mel_int,
+            harm_oct=harm_oct,
+            harm_class=harm_class,
+            dup_count=dup_count,
+            string=string,
+            fret=fret,
+        ),
+        next_idx,
+    )
+
+
+def serialize_voice_event(event: VoiceEvent) -> List[str]:
+    tokens = [f"VOICE_{event.voice}"]
+    if event.is_rest:
+        tokens.append(f"REST_{event.rest_ticks}")
+        return tokens
+
+    tokens.append(f"DUR_{event.duration_ticks}")
+    if event.dup_count is not None:
+        tokens.append(f"DUP_{event.dup_count}")
+
+    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
+    if event.harm_oct is None:
+        tokens.append("HARM_OCT_NA")
+        tokens.append("HARM_CLASS_NA")
+    else:
+        tokens.append(f"HARM_OCT_{event.harm_oct}")
+        tokens.append(f"HARM_CLASS_{event.harm_class}")
+
+    if event.string is not None:
+        tokens.append(f"STR_{event.string}")
+        tokens.append(f"FRET_{event.fret}")
+
+    return tokens
+
+
+def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
+    parsed: List[Union[str, VoiceEvent]] = []
+    idx = 0
+    while idx < len(tokens):
+        tok = tokens[idx]
+        if tok.startswith("VOICE_"):
+            event, idx = parse_voice_event(tokens, idx)
+            parsed.append(event)
+            continue
+        parsed.append(tok)
+        idx += 1
+    return parsed
+
+
+def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
+    tokens: List[str] = []
+    for item in items:
+        if isinstance(item, VoiceEvent):
+            tokens.extend(serialize_voice_event(item))
+        else:
+            tokens.append(item)
+    return tokens
+
+
+def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
+    return serialize_event_stream(parse_event_stream(tokens))
+
+
+def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
+    if not token.startswith(prefix):
+        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
+    value = token[len(prefix) :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc
+
+
+def _parse_signed_mel(token: str, idx: int) -> int:
+    if not token.startswith("MEL_INT12_"):
+        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
+    value = token[len("MEL_INT12_") :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_OCT_"):
+        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
+    value = token[len("HARM_OCT_") :]
+    if value == "NA":
+        return None
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_CLASS_"):
+        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
+    value = token[len("HARM_CLASS_") :]
+    if value == "NA":
+        return None
+    try:
+        parsed = int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
+    if not (0 <= parsed <= 11):
+        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
+    return parsed
diff --git a/tests/test_tokenizer.py b/tests/test_tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..4f4741f7ebc6a851a695b5f6081e10cfc2c03f03
--- /dev/null
+++ b/tests/test_tokenizer.py
@@ -0,0 +1,107 @@
+import sys
+from pathlib import Path
+
+import pytest
+
+sys.path.insert(0, str(Path(__file__).parents[1]))
+
+from src.tokens.tokenizer import (
+    VoiceEvent,
+    canonicalize_event_stream,
+    parse_event_stream,
+    parse_voice_event,
+    serialize_event_stream,
+    serialize_voice_event,
+    tokenize_event_text,
+)
+
+
+def test_parse_pitched_voice_event_with_dup_and_tab():
+    tokens = [
+        "VOICE_2",
+        "DUR_12",
+        "DUP_3",
+        "MEL_INT12_+7",
+        "HARM_OCT_1",
+        "HARM_CLASS_7",
+        "STR_4",
+        "FRET_2",
+        "POS_6",
+    ]
+
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 8
+    assert event.voice == 2
+    assert event.duration_ticks == 12
+    assert event.dup_count == 3
+    assert event.mel_int == 7
+    assert event.harm_oct == 1
+    assert event.harm_class == 7
+    assert event.string == 4
+    assert event.fret == 2
+
+
+def test_parse_rest_voice_event():
+    tokens = ["VOICE_1", "REST_18", "BAR"]
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 2
+    assert event.voice == 1
+    assert event.is_rest
+    assert event.rest_ticks == 18
+
+
+def test_serialize_voice_event_uses_canonical_order():
+    event = VoiceEvent(
+        voice=3,
+        duration_ticks=24,
+        dup_count=2,
+        mel_int=11,
+        harm_oct=0,
+        harm_class=11,
+    )
+    assert serialize_voice_event(event) == [
+        "VOICE_3",
+        "DUR_24",
+        "DUP_2",
+        "MEL_INT12_+11",
+        "HARM_OCT_0",
+        "HARM_CLASS_11",
+    ]
+
+
+def test_canonicalize_event_stream_normalizes_mel_sign():
+    tokens = [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+    assert canonicalize_event_stream(tokens) == [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_+2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+
+
+def test_parse_event_stream_roundtrip_and_text_tokenization():
+    text = "BAR, POS_0, VOICE_0, DUR_4, MEL_INT12_0, HARM_OCT_NA, HARM_CLASS_NA"
+    tokens = tokenize_event_text(text)
+    parsed = parse_event_stream(tokens)
+    assert serialize_event_stream(parsed) == tokens
+
+
+def test_parse_rejects_mixed_harm_na_state():
+    tokens = ["VOICE_0", "DUR_8", "MEL_INT12_0", "HARM_OCT_NA", "HARM_CLASS_2"]
+    with pytest.raises(ValueError, match="harm_oct and harm_class must both be set or both be None"):
+        parse_voice_event(tokens, 0)


thinking
**Polling for completion**
file update:
diff --git a/src/tokens/eventizer.py b/src/tokens/eventizer.py
index f7c666b524c3f03edbf62558e3a89e26917d8d43..53385718babf181fdc8367c1ca227731a1c1b799
--- a/src/tokens/eventizer.py
+++ b/src/tokens/eventizer.py
@@ -12,9 +12,9 @@
     IntervalRepairStats,
     compute_reference_pitch,
     compute_melodic_interval,
-    format_signed_interval,
     harmonic_tokens_for_pitch,
 )
+from src.tokens.tokenizer import VoiceEvent, serialize_voice_event
 
 DEFAULT_MAX_VOICES = 8
 DEFAULT_MAX_OCTAVES_PER_PITCH_CLASS = 3
@@ -584,15 +584,12 @@
                         tokens.append(f"ABS_VOICE_{v}_{ev.pitch}")
                     prev_pitch[v] = ev.pitch
 
-                tokens.append(f"VOICE_{v}")
-                tokens.append(f"DUR_{ev.duration_tick}")
                 base_pitch = prev_pitch[v] if prev_pitch[v] is not None else ev.pitch
                 mel_int = compute_melodic_interval(
                     ev.pitch,
                     base_pitch,
                     qa_mode=True,
                 )
-                tokens.append(f"MEL_INT12_{format_signed_interval(mel_int)}")
 
                 harm_oct_tok, harm_cls_tok = harmonic_tokens_for_pitch(
                     ev.pitch,
@@ -600,8 +597,21 @@
                     qa_mode=qa_mode,
                     stats=interval_repairs,
                 )
-                tokens.append(harm_oct_tok)
-                tokens.append(harm_cls_tok)
+                harm_oct = None if harm_oct_tok.endswith("_NA") else int(harm_oct_tok.rsplit("_", 1)[1])
+                harm_class = (
+                    None if harm_cls_tok.endswith("_NA") else int(harm_cls_tok.rsplit("_", 1)[1])
+                )
+                tokens.extend(
+                    serialize_voice_event(
+                        VoiceEvent(
+                            voice=v,
+                            duration_ticks=ev.duration_tick,
+                            mel_int=mel_int,
+                            harm_oct=harm_oct,
+                            harm_class=harm_class,
+                        )
+                    )
+                )
 
                 prev_pitch[v] = ev.pitch
                 active_until[v] = t + ev.duration_tick
diff --git a/src/tokens/tokenizer.py b/src/tokens/tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..3d7839be70301fe3c5376ba858263c8c4f66da1b
--- /dev/null
+++ b/src/tokens/tokenizer.py
@@ -0,0 +1,219 @@
+from dataclasses import dataclass
+from typing import List, Sequence, Tuple, Union
+
+from src.tokens.intervals import format_signed_interval
+
+
+@dataclass(frozen=True)
+class VoiceEvent:
+    voice: int
+    duration_ticks: int = 0
+    rest_ticks: int = 0
+    mel_int: int = 0
+    harm_oct: Union[int, None] = None
+    harm_class: Union[int, None] = None
+    dup_count: Union[int, None] = None
+    string: Union[int, None] = None
+    fret: Union[int, None] = None
+
+    def __post_init__(self) -> None:
+        if self.voice < 0:
+            raise ValueError("voice must be >= 0")
+        if self.duration_ticks < 0:
+            raise ValueError("duration_ticks must be >= 0")
+        if self.rest_ticks < 0:
+            raise ValueError("rest_ticks must be >= 0")
+        if self.dup_count is not None and self.dup_count < 1:
+            raise ValueError("dup_count must be >= 1 when set")
+        if (self.string is None) ^ (self.fret is None):
+            raise ValueError("string and fret must both be set or both be None")
+
+        is_rest = self.rest_ticks > 0
+        is_pitched = self.duration_ticks > 0
+        if is_rest == is_pitched:
+            raise ValueError("voice event must be exactly one of rest or pitched")
+
+        if is_rest:
+            if self.dup_count is not None:
+                raise ValueError("rest event cannot set dup_count")
+            if self.harm_oct is not None or self.harm_class is not None:
+                raise ValueError("rest event cannot set harmonic interval fields")
+            if self.string is not None or self.fret is not None:
+                raise ValueError("rest event cannot set tab fields")
+            return
+
+        if (self.harm_oct is None) ^ (self.harm_class is None):
+            raise ValueError("harm_oct and harm_class must both be set or both be None")
+        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
+            raise ValueError("harm_class must be in [0, 11]")
+
+    @property
+    def is_rest(self) -> bool:
+        return self.rest_ticks > 0
+
+
+def tokenize_event_text(text: str) -> List[str]:
+    raw = text.replace("\n", ",")
+    tokens = [tok.strip() for tok in raw.split(",")]
+    return [tok for tok in tokens if tok]
+
+
+def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
+    if start_idx >= len(tokens):
+        raise ValueError("start_idx out of range")
+
+    voice_tok = tokens[start_idx]
+    if not voice_tok.startswith("VOICE_"):
+        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")
+
+    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
+    next_idx = start_idx + 1
+    if next_idx >= len(tokens):
+        raise ValueError(f"truncated VOICE event at index {start_idx}")
+
+    first_tok = tokens[next_idx]
+    if first_tok.startswith("REST_"):
+        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
+        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1
+
+    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
+    next_idx += 1
+
+    dup_count = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
+        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
+        next_idx += 1
+
+    if next_idx + 2 >= len(tokens):
+        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")
+
+    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
+    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
+    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
+    next_idx += 3
+
+    string = None
+    fret = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
+        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
+        next_idx += 1
+        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
+            raise ValueError(f"missing FRET token after STR at index {next_idx}")
+        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
+        next_idx += 1
+    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
+        raise ValueError(f"FRET token must follow STR token at index {next_idx}")
+
+    return (
+        VoiceEvent(
+            voice=voice,
+            duration_ticks=duration_ticks,
+            mel_int=mel_int,
+            harm_oct=harm_oct,
+            harm_class=harm_class,
+            dup_count=dup_count,
+            string=string,
+            fret=fret,
+        ),
+        next_idx,
+    )
+
+
+def serialize_voice_event(event: VoiceEvent) -> List[str]:
+    tokens = [f"VOICE_{event.voice}"]
+    if event.is_rest:
+        tokens.append(f"REST_{event.rest_ticks}")
+        return tokens
+
+    tokens.append(f"DUR_{event.duration_ticks}")
+    if event.dup_count is not None:
+        tokens.append(f"DUP_{event.dup_count}")
+
+    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
+    if event.harm_oct is None:
+        tokens.append("HARM_OCT_NA")
+        tokens.append("HARM_CLASS_NA")
+    else:
+        tokens.append(f"HARM_OCT_{event.harm_oct}")
+        tokens.append(f"HARM_CLASS_{event.harm_class}")
+
+    if event.string is not None:
+        tokens.append(f"STR_{event.string}")
+        tokens.append(f"FRET_{event.fret}")
+
+    return tokens
+
+
+def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
+    parsed: List[Union[str, VoiceEvent]] = []
+    idx = 0
+    while idx < len(tokens):
+        tok = tokens[idx]
+        if tok.startswith("VOICE_"):
+            event, idx = parse_voice_event(tokens, idx)
+            parsed.append(event)
+            continue
+        parsed.append(tok)
+        idx += 1
+    return parsed
+
+
+def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
+    tokens: List[str] = []
+    for item in items:
+        if isinstance(item, VoiceEvent):
+            tokens.extend(serialize_voice_event(item))
+        else:
+            tokens.append(item)
+    return tokens
+
+
+def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
+    return serialize_event_stream(parse_event_stream(tokens))
+
+
+def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
+    if not token.startswith(prefix):
+        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
+    value = token[len(prefix) :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc
+
+
+def _parse_signed_mel(token: str, idx: int) -> int:
+    if not token.startswith("MEL_INT12_"):
+        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
+    value = token[len("MEL_INT12_") :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_OCT_"):
+        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
+    value = token[len("HARM_OCT_") :]
+    if value == "NA":
+        return None
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_CLASS_"):
+        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
+    value = token[len("HARM_CLASS_") :]
+    if value == "NA":
+        return None
+    try:
+        parsed = int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
+    if not (0 <= parsed <= 11):
+        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
+    return parsed
diff --git a/tests/test_tokenizer.py b/tests/test_tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..4f4741f7ebc6a851a695b5f6081e10cfc2c03f03
--- /dev/null
+++ b/tests/test_tokenizer.py
@@ -0,0 +1,107 @@
+import sys
+from pathlib import Path
+
+import pytest
+
+sys.path.insert(0, str(Path(__file__).parents[1]))
+
+from src.tokens.tokenizer import (
+    VoiceEvent,
+    canonicalize_event_stream,
+    parse_event_stream,
+    parse_voice_event,
+    serialize_event_stream,
+    serialize_voice_event,
+    tokenize_event_text,
+)
+
+
+def test_parse_pitched_voice_event_with_dup_and_tab():
+    tokens = [
+        "VOICE_2",
+        "DUR_12",
+        "DUP_3",
+        "MEL_INT12_+7",
+        "HARM_OCT_1",
+        "HARM_CLASS_7",
+        "STR_4",
+        "FRET_2",
+        "POS_6",
+    ]
+
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 8
+    assert event.voice == 2
+    assert event.duration_ticks == 12
+    assert event.dup_count == 3
+    assert event.mel_int == 7
+    assert event.harm_oct == 1
+    assert event.harm_class == 7
+    assert event.string == 4
+    assert event.fret == 2
+
+
+def test_parse_rest_voice_event():
+    tokens = ["VOICE_1", "REST_18", "BAR"]
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 2
+    assert event.voice == 1
+    assert event.is_rest
+    assert event.rest_ticks == 18
+
+
+def test_serialize_voice_event_uses_canonical_order():
+    event = VoiceEvent(
+        voice=3,
+        duration_ticks=24,
+        dup_count=2,
+        mel_int=11,
+        harm_oct=0,
+        harm_class=11,
+    )
+    assert serialize_voice_event(event) == [
+        "VOICE_3",
+        "DUR_24",
+        "DUP_2",
+        "MEL_INT12_+11",
+        "HARM_OCT_0",
+        "HARM_CLASS_11",
+    ]
+
+
+def test_canonicalize_event_stream_normalizes_mel_sign():
+    tokens = [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+    assert canonicalize_event_stream(tokens) == [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_+2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+
+
+def test_parse_event_stream_roundtrip_and_text_tokenization():
+    text = "BAR, POS_0, VOICE_0, DUR_4, MEL_INT12_0, HARM_OCT_NA, HARM_CLASS_NA"
+    tokens = tokenize_event_text(text)
+    parsed = parse_event_stream(tokens)
+    assert serialize_event_stream(parsed) == tokens
+
+
+def test_parse_rejects_mixed_harm_na_state():
+    tokens = ["VOICE_0", "DUR_8", "MEL_INT12_0", "HARM_OCT_NA", "HARM_CLASS_2"]
+    with pytest.raises(ValueError, match="harm_oct and harm_class must both be set or both be None"):
+        parse_voice_event(tokens, 0)

file update:
diff --git a/src/tokens/eventizer.py b/src/tokens/eventizer.py
index f7c666b524c3f03edbf62558e3a89e26917d8d43..53385718babf181fdc8367c1ca227731a1c1b799
--- a/src/tokens/eventizer.py
+++ b/src/tokens/eventizer.py
@@ -12,9 +12,9 @@
     IntervalRepairStats,
     compute_reference_pitch,
     compute_melodic_interval,
-    format_signed_interval,
     harmonic_tokens_for_pitch,
 )
+from src.tokens.tokenizer import VoiceEvent, serialize_voice_event
 
 DEFAULT_MAX_VOICES = 8
 DEFAULT_MAX_OCTAVES_PER_PITCH_CLASS = 3
@@ -584,15 +584,12 @@
                         tokens.append(f"ABS_VOICE_{v}_{ev.pitch}")
                     prev_pitch[v] = ev.pitch
 
-                tokens.append(f"VOICE_{v}")
-                tokens.append(f"DUR_{ev.duration_tick}")
                 base_pitch = prev_pitch[v] if prev_pitch[v] is not None else ev.pitch
                 mel_int = compute_melodic_interval(
                     ev.pitch,
                     base_pitch,
                     qa_mode=True,
                 )
-                tokens.append(f"MEL_INT12_{format_signed_interval(mel_int)}")
 
                 harm_oct_tok, harm_cls_tok = harmonic_tokens_for_pitch(
                     ev.pitch,
@@ -600,8 +597,21 @@
                     qa_mode=qa_mode,
                     stats=interval_repairs,
                 )
-                tokens.append(harm_oct_tok)
-                tokens.append(harm_cls_tok)
+                harm_oct = None if harm_oct_tok.endswith("_NA") else int(harm_oct_tok.rsplit("_", 1)[1])
+                harm_class = (
+                    None if harm_cls_tok.endswith("_NA") else int(harm_cls_tok.rsplit("_", 1)[1])
+                )
+                tokens.extend(
+                    serialize_voice_event(
+                        VoiceEvent(
+                            voice=v,
+                            duration_ticks=ev.duration_tick,
+                            mel_int=mel_int,
+                            harm_oct=harm_oct,
+                            harm_class=harm_class,
+                        )
+                    )
+                )
 
                 prev_pitch[v] = ev.pitch
                 active_until[v] = t + ev.duration_tick
diff --git a/src/tokens/tokenizer.py b/src/tokens/tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..3d7839be70301fe3c5376ba858263c8c4f66da1b
--- /dev/null
+++ b/src/tokens/tokenizer.py
@@ -0,0 +1,219 @@
+from dataclasses import dataclass
+from typing import List, Sequence, Tuple, Union
+
+from src.tokens.intervals import format_signed_interval
+
+
+@dataclass(frozen=True)
+class VoiceEvent:
+    voice: int
+    duration_ticks: int = 0
+    rest_ticks: int = 0
+    mel_int: int = 0
+    harm_oct: Union[int, None] = None
+    harm_class: Union[int, None] = None
+    dup_count: Union[int, None] = None
+    string: Union[int, None] = None
+    fret: Union[int, None] = None
+
+    def __post_init__(self) -> None:
+        if self.voice < 0:
+            raise ValueError("voice must be >= 0")
+        if self.duration_ticks < 0:
+            raise ValueError("duration_ticks must be >= 0")
+        if self.rest_ticks < 0:
+            raise ValueError("rest_ticks must be >= 0")
+        if self.dup_count is not None and self.dup_count < 1:
+            raise ValueError("dup_count must be >= 1 when set")
+        if (self.string is None) ^ (self.fret is None):
+            raise ValueError("string and fret must both be set or both be None")
+
+        is_rest = self.rest_ticks > 0
+        is_pitched = self.duration_ticks > 0
+        if is_rest == is_pitched:
+            raise ValueError("voice event must be exactly one of rest or pitched")
+
+        if is_rest:
+            if self.dup_count is not None:
+                raise ValueError("rest event cannot set dup_count")
+            if self.harm_oct is not None or self.harm_class is not None:
+                raise ValueError("rest event cannot set harmonic interval fields")
+            if self.string is not None or self.fret is not None:
+                raise ValueError("rest event cannot set tab fields")
+            return
+
+        if (self.harm_oct is None) ^ (self.harm_class is None):
+            raise ValueError("harm_oct and harm_class must both be set or both be None")
+        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
+            raise ValueError("harm_class must be in [0, 11]")
+
+    @property
+    def is_rest(self) -> bool:
+        return self.rest_ticks > 0
+
+
+def tokenize_event_text(text: str) -> List[str]:
+    raw = text.replace("\n", ",")
+    tokens = [tok.strip() for tok in raw.split(",")]
+    return [tok for tok in tokens if tok]
+
+
+def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
+    if start_idx >= len(tokens):
+        raise ValueError("start_idx out of range")
+
+    voice_tok = tokens[start_idx]
+    if not voice_tok.startswith("VOICE_"):
+        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")
+
+    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
+    next_idx = start_idx + 1
+    if next_idx >= len(tokens):
+        raise ValueError(f"truncated VOICE event at index {start_idx}")
+
+    first_tok = tokens[next_idx]
+    if first_tok.startswith("REST_"):
+        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
+        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1
+
+    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
+    next_idx += 1
+
+    dup_count = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
+        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
+        next_idx += 1
+
+    if next_idx + 2 >= len(tokens):
+        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")
+
+    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
+    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
+    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
+    next_idx += 3
+
+    string = None
+    fret = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
+        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
+        next_idx += 1
+        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
+            raise ValueError(f"missing FRET token after STR at index {next_idx}")
+        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
+        next_idx += 1
+    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
+        raise ValueError(f"FRET token must follow STR token at index {next_idx}")
+
+    return (
+        VoiceEvent(
+            voice=voice,
+            duration_ticks=duration_ticks,
+            mel_int=mel_int,
+            harm_oct=harm_oct,
+            harm_class=harm_class,
+            dup_count=dup_count,
+            string=string,
+            fret=fret,
+        ),
+        next_idx,
+    )
+
+
+def serialize_voice_event(event: VoiceEvent) -> List[str]:
+    tokens = [f"VOICE_{event.voice}"]
+    if event.is_rest:
+        tokens.append(f"REST_{event.rest_ticks}")
+        return tokens
+
+    tokens.append(f"DUR_{event.duration_ticks}")
+    if event.dup_count is not None:
+        tokens.append(f"DUP_{event.dup_count}")
+
+    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
+    if event.harm_oct is None:
+        tokens.append("HARM_OCT_NA")
+        tokens.append("HARM_CLASS_NA")
+    else:
+        tokens.append(f"HARM_OCT_{event.harm_oct}")
+        tokens.append(f"HARM_CLASS_{event.harm_class}")
+
+    if event.string is not None:
+        tokens.append(f"STR_{event.string}")
+        tokens.append(f"FRET_{event.fret}")
+
+    return tokens
+
+
+def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
+    parsed: List[Union[str, VoiceEvent]] = []
+    idx = 0
+    while idx < len(tokens):
+        tok = tokens[idx]
+        if tok.startswith("VOICE_"):
+            event, idx = parse_voice_event(tokens, idx)
+            parsed.append(event)
+            continue
+        parsed.append(tok)
+        idx += 1
+    return parsed
+
+
+def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
+    tokens: List[str] = []
+    for item in items:
+        if isinstance(item, VoiceEvent):
+            tokens.extend(serialize_voice_event(item))
+        else:
+            tokens.append(item)
+    return tokens
+
+
+def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
+    return serialize_event_stream(parse_event_stream(tokens))
+
+
+def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
+    if not token.startswith(prefix):
+        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
+    value = token[len(prefix) :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc
+
+
+def _parse_signed_mel(token: str, idx: int) -> int:
+    if not token.startswith("MEL_INT12_"):
+        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
+    value = token[len("MEL_INT12_") :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_OCT_"):
+        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
+    value = token[len("HARM_OCT_") :]
+    if value == "NA":
+        return None
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_CLASS_"):
+        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
+    value = token[len("HARM_CLASS_") :]
+    if value == "NA":
+        return None
+    try:
+        parsed = int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
+    if not (0 <= parsed <= 11):
+        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
+    return parsed
diff --git a/tests/test_tokenizer.py b/tests/test_tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..4f4741f7ebc6a851a695b5f6081e10cfc2c03f03
--- /dev/null
+++ b/tests/test_tokenizer.py
@@ -0,0 +1,107 @@
+import sys
+from pathlib import Path
+
+import pytest
+
+sys.path.insert(0, str(Path(__file__).parents[1]))
+
+from src.tokens.tokenizer import (
+    VoiceEvent,
+    canonicalize_event_stream,
+    parse_event_stream,
+    parse_voice_event,
+    serialize_event_stream,
+    serialize_voice_event,
+    tokenize_event_text,
+)
+
+
+def test_parse_pitched_voice_event_with_dup_and_tab():
+    tokens = [
+        "VOICE_2",
+        "DUR_12",
+        "DUP_3",
+        "MEL_INT12_+7",
+        "HARM_OCT_1",
+        "HARM_CLASS_7",
+        "STR_4",
+        "FRET_2",
+        "POS_6",
+    ]
+
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 8
+    assert event.voice == 2
+    assert event.duration_ticks == 12
+    assert event.dup_count == 3
+    assert event.mel_int == 7
+    assert event.harm_oct == 1
+    assert event.harm_class == 7
+    assert event.string == 4
+    assert event.fret == 2
+
+
+def test_parse_rest_voice_event():
+    tokens = ["VOICE_1", "REST_18", "BAR"]
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 2
+    assert event.voice == 1
+    assert event.is_rest
+    assert event.rest_ticks == 18
+
+
+def test_serialize_voice_event_uses_canonical_order():
+    event = VoiceEvent(
+        voice=3,
+        duration_ticks=24,
+        dup_count=2,
+        mel_int=11,
+        harm_oct=0,
+        harm_class=11,
+    )
+    assert serialize_voice_event(event) == [
+        "VOICE_3",
+        "DUR_24",
+        "DUP_2",
+        "MEL_INT12_+11",
+        "HARM_OCT_0",
+        "HARM_CLASS_11",
+    ]
+
+
+def test_canonicalize_event_stream_normalizes_mel_sign():
+    tokens = [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+    assert canonicalize_event_stream(tokens) == [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_+2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+
+
+def test_parse_event_stream_roundtrip_and_text_tokenization():
+    text = "BAR, POS_0, VOICE_0, DUR_4, MEL_INT12_0, HARM_OCT_NA, HARM_CLASS_NA"
+    tokens = tokenize_event_text(text)
+    parsed = parse_event_stream(tokens)
+    assert serialize_event_stream(parsed) == tokens
+
+
+def test_parse_rejects_mixed_harm_na_state():
+    tokens = ["VOICE_0", "DUR_8", "MEL_INT12_0", "HARM_OCT_NA", "HARM_CLASS_2"]
+    with pytest.raises(ValueError, match="harm_oct and harm_class must both be set or both be None"):
+        parse_voice_event(tokens, 0)

file update:
diff --git a/src/tokens/eventizer.py b/src/tokens/eventizer.py
index f7c666b524c3f03edbf62558e3a89e26917d8d43..53385718babf181fdc8367c1ca227731a1c1b799
--- a/src/tokens/eventizer.py
+++ b/src/tokens/eventizer.py
@@ -12,9 +12,9 @@
     IntervalRepairStats,
     compute_reference_pitch,
     compute_melodic_interval,
-    format_signed_interval,
     harmonic_tokens_for_pitch,
 )
+from src.tokens.tokenizer import VoiceEvent, serialize_voice_event
 
 DEFAULT_MAX_VOICES = 8
 DEFAULT_MAX_OCTAVES_PER_PITCH_CLASS = 3
@@ -584,15 +584,12 @@
                         tokens.append(f"ABS_VOICE_{v}_{ev.pitch}")
                     prev_pitch[v] = ev.pitch
 
-                tokens.append(f"VOICE_{v}")
-                tokens.append(f"DUR_{ev.duration_tick}")
                 base_pitch = prev_pitch[v] if prev_pitch[v] is not None else ev.pitch
                 mel_int = compute_melodic_interval(
                     ev.pitch,
                     base_pitch,
                     qa_mode=True,
                 )
-                tokens.append(f"MEL_INT12_{format_signed_interval(mel_int)}")
 
                 harm_oct_tok, harm_cls_tok = harmonic_tokens_for_pitch(
                     ev.pitch,
@@ -600,8 +597,21 @@
                     qa_mode=qa_mode,
                     stats=interval_repairs,
                 )
-                tokens.append(harm_oct_tok)
-                tokens.append(harm_cls_tok)
+                harm_oct = None if harm_oct_tok.endswith("_NA") else int(harm_oct_tok.rsplit("_", 1)[1])
+                harm_class = (
+                    None if harm_cls_tok.endswith("_NA") else int(harm_cls_tok.rsplit("_", 1)[1])
+                )
+                tokens.extend(
+                    serialize_voice_event(
+                        VoiceEvent(
+                            voice=v,
+                            duration_ticks=ev.duration_tick,
+                            mel_int=mel_int,
+                            harm_oct=harm_oct,
+                            harm_class=harm_class,
+                        )
+                    )
+                )
 
                 prev_pitch[v] = ev.pitch
                 active_until[v] = t + ev.duration_tick
diff --git a/src/tokens/tokenizer.py b/src/tokens/tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..3d7839be70301fe3c5376ba858263c8c4f66da1b
--- /dev/null
+++ b/src/tokens/tokenizer.py
@@ -0,0 +1,219 @@
+from dataclasses import dataclass
+from typing import List, Sequence, Tuple, Union
+
+from src.tokens.intervals import format_signed_interval
+
+
+@dataclass(frozen=True)
+class VoiceEvent:
+    voice: int
+    duration_ticks: int = 0
+    rest_ticks: int = 0
+    mel_int: int = 0
+    harm_oct: Union[int, None] = None
+    harm_class: Union[int, None] = None
+    dup_count: Union[int, None] = None
+    string: Union[int, None] = None
+    fret: Union[int, None] = None
+
+    def __post_init__(self) -> None:
+        if self.voice < 0:
+            raise ValueError("voice must be >= 0")
+        if self.duration_ticks < 0:
+            raise ValueError("duration_ticks must be >= 0")
+        if self.rest_ticks < 0:
+            raise ValueError("rest_ticks must be >= 0")
+        if self.dup_count is not None and self.dup_count < 1:
+            raise ValueError("dup_count must be >= 1 when set")
+        if (self.string is None) ^ (self.fret is None):
+            raise ValueError("string and fret must both be set or both be None")
+
+        is_rest = self.rest_ticks > 0
+        is_pitched = self.duration_ticks > 0
+        if is_rest == is_pitched:
+            raise ValueError("voice event must be exactly one of rest or pitched")
+
+        if is_rest:
+            if self.dup_count is not None:
+                raise ValueError("rest event cannot set dup_count")
+            if self.harm_oct is not None or self.harm_class is not None:
+                raise ValueError("rest event cannot set harmonic interval fields")
+            if self.string is not None or self.fret is not None:
+                raise ValueError("rest event cannot set tab fields")
+            return
+
+        if (self.harm_oct is None) ^ (self.harm_class is None):
+            raise ValueError("harm_oct and harm_class must both be set or both be None")
+        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
+            raise ValueError("harm_class must be in [0, 11]")
+
+    @property
+    def is_rest(self) -> bool:
+        return self.rest_ticks > 0
+
+
+def tokenize_event_text(text: str) -> List[str]:
+    raw = text.replace("\n", ",")
+    tokens = [tok.strip() for tok in raw.split(",")]
+    return [tok for tok in tokens if tok]
+
+
+def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
+    if start_idx >= len(tokens):
+        raise ValueError("start_idx out of range")
+
+    voice_tok = tokens[start_idx]
+    if not voice_tok.startswith("VOICE_"):
+        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")
+
+    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
+    next_idx = start_idx + 1
+    if next_idx >= len(tokens):
+        raise ValueError(f"truncated VOICE event at index {start_idx}")
+
+    first_tok = tokens[next_idx]
+    if first_tok.startswith("REST_"):
+        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
+        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1
+
+    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
+    next_idx += 1
+
+    dup_count = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
+        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
+        next_idx += 1
+
+    if next_idx + 2 >= len(tokens):
+        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")
+
+    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
+    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
+    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
+    next_idx += 3
+
+    string = None
+    fret = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
+        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
+        next_idx += 1
+        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
+            raise ValueError(f"missing FRET token after STR at index {next_idx}")
+        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
+        next_idx += 1
+    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
+        raise ValueError(f"FRET token must follow STR token at index {next_idx}")
+
+    return (
+        VoiceEvent(
+            voice=voice,
+            duration_ticks=duration_ticks,
+            mel_int=mel_int,
+            harm_oct=harm_oct,
+            harm_class=harm_class,
+            dup_count=dup_count,
+            string=string,
+            fret=fret,
+        ),
+        next_idx,
+    )
+
+
+def serialize_voice_event(event: VoiceEvent) -> List[str]:
+    tokens = [f"VOICE_{event.voice}"]
+    if event.is_rest:
+        tokens.append(f"REST_{event.rest_ticks}")
+        return tokens
+
+    tokens.append(f"DUR_{event.duration_ticks}")
+    if event.dup_count is not None:
+        tokens.append(f"DUP_{event.dup_count}")
+
+    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
+    if event.harm_oct is None:
+        tokens.append("HARM_OCT_NA")
+        tokens.append("HARM_CLASS_NA")
+    else:
+        tokens.append(f"HARM_OCT_{event.harm_oct}")
+        tokens.append(f"HARM_CLASS_{event.harm_class}")
+
+    if event.string is not None:
+        tokens.append(f"STR_{event.string}")
+        tokens.append(f"FRET_{event.fret}")
+
+    return tokens
+
+
+def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
+    parsed: List[Union[str, VoiceEvent]] = []
+    idx = 0
+    while idx < len(tokens):
+        tok = tokens[idx]
+        if tok.startswith("VOICE_"):
+            event, idx = parse_voice_event(tokens, idx)
+            parsed.append(event)
+            continue
+        parsed.append(tok)
+        idx += 1
+    return parsed
+
+
+def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
+    tokens: List[str] = []
+    for item in items:
+        if isinstance(item, VoiceEvent):
+            tokens.extend(serialize_voice_event(item))
+        else:
+            tokens.append(item)
+    return tokens
+
+
+def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
+    return serialize_event_stream(parse_event_stream(tokens))
+
+
+def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
+    if not token.startswith(prefix):
+        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
+    value = token[len(prefix) :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc
+
+
+def _parse_signed_mel(token: str, idx: int) -> int:
+    if not token.startswith("MEL_INT12_"):
+        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
+    value = token[len("MEL_INT12_") :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_OCT_"):
+        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
+    value = token[len("HARM_OCT_") :]
+    if value == "NA":
+        return None
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_CLASS_"):
+        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
+    value = token[len("HARM_CLASS_") :]
+    if value == "NA":
+        return None
+    try:
+        parsed = int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
+    if not (0 <= parsed <= 11):
+        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
+    return parsed
diff --git a/tests/test_tokenizer.py b/tests/test_tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..4f4741f7ebc6a851a695b5f6081e10cfc2c03f03
--- /dev/null
+++ b/tests/test_tokenizer.py
@@ -0,0 +1,107 @@
+import sys
+from pathlib import Path
+
+import pytest
+
+sys.path.insert(0, str(Path(__file__).parents[1]))
+
+from src.tokens.tokenizer import (
+    VoiceEvent,
+    canonicalize_event_stream,
+    parse_event_stream,
+    parse_voice_event,
+    serialize_event_stream,
+    serialize_voice_event,
+    tokenize_event_text,
+)
+
+
+def test_parse_pitched_voice_event_with_dup_and_tab():
+    tokens = [
+        "VOICE_2",
+        "DUR_12",
+        "DUP_3",
+        "MEL_INT12_+7",
+        "HARM_OCT_1",
+        "HARM_CLASS_7",
+        "STR_4",
+        "FRET_2",
+        "POS_6",
+    ]
+
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 8
+    assert event.voice == 2
+    assert event.duration_ticks == 12
+    assert event.dup_count == 3
+    assert event.mel_int == 7
+    assert event.harm_oct == 1
+    assert event.harm_class == 7
+    assert event.string == 4
+    assert event.fret == 2
+
+
+def test_parse_rest_voice_event():
+    tokens = ["VOICE_1", "REST_18", "BAR"]
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 2
+    assert event.voice == 1
+    assert event.is_rest
+    assert event.rest_ticks == 18
+
+
+def test_serialize_voice_event_uses_canonical_order():
+    event = VoiceEvent(
+        voice=3,
+        duration_ticks=24,
+        dup_count=2,
+        mel_int=11,
+        harm_oct=0,
+        harm_class=11,
+    )
+    assert serialize_voice_event(event) == [
+        "VOICE_3",
+        "DUR_24",
+        "DUP_2",
+        "MEL_INT12_+11",
+        "HARM_OCT_0",
+        "HARM_CLASS_11",
+    ]
+
+
+def test_canonicalize_event_stream_normalizes_mel_sign():
+    tokens = [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+    assert canonicalize_event_stream(tokens) == [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_+2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+
+
+def test_parse_event_stream_roundtrip_and_text_tokenization():
+    text = "BAR, POS_0, VOICE_0, DUR_4, MEL_INT12_0, HARM_OCT_NA, HARM_CLASS_NA"
+    tokens = tokenize_event_text(text)
+    parsed = parse_event_stream(tokens)
+    assert serialize_event_stream(parsed) == tokens
+
+
+def test_parse_rejects_mixed_harm_na_state():
+    tokens = ["VOICE_0", "DUR_8", "MEL_INT12_0", "HARM_OCT_NA", "HARM_CLASS_2"]
+    with pytest.raises(ValueError, match="harm_oct and harm_class must both be set or both be None"):
+        parse_voice_event(tokens, 0)


thinking
**Adding periodic update logic**
codex
{"done":false,"progress_made":true,"summary":"Test run is still in progress; I’m waiting for completion output from the helper script.","tests_ran":true,"tests_status":"not_run","next_step":"Capture final test result and apply TODO/PROGRESS updates.","stop_reason":""}
file update:
diff --git a/src/tokens/eventizer.py b/src/tokens/eventizer.py
index f7c666b524c3f03edbf62558e3a89e26917d8d43..53385718babf181fdc8367c1ca227731a1c1b799
--- a/src/tokens/eventizer.py
+++ b/src/tokens/eventizer.py
@@ -12,9 +12,9 @@
     IntervalRepairStats,
     compute_reference_pitch,
     compute_melodic_interval,
-    format_signed_interval,
     harmonic_tokens_for_pitch,
 )
+from src.tokens.tokenizer import VoiceEvent, serialize_voice_event
 
 DEFAULT_MAX_VOICES = 8
 DEFAULT_MAX_OCTAVES_PER_PITCH_CLASS = 3
@@ -584,15 +584,12 @@
                         tokens.append(f"ABS_VOICE_{v}_{ev.pitch}")
                     prev_pitch[v] = ev.pitch
 
-                tokens.append(f"VOICE_{v}")
-                tokens.append(f"DUR_{ev.duration_tick}")
                 base_pitch = prev_pitch[v] if prev_pitch[v] is not None else ev.pitch
                 mel_int = compute_melodic_interval(
                     ev.pitch,
                     base_pitch,
                     qa_mode=True,
                 )
-                tokens.append(f"MEL_INT12_{format_signed_interval(mel_int)}")
 
                 harm_oct_tok, harm_cls_tok = harmonic_tokens_for_pitch(
                     ev.pitch,
@@ -600,8 +597,21 @@
                     qa_mode=qa_mode,
                     stats=interval_repairs,
                 )
-                tokens.append(harm_oct_tok)
-                tokens.append(harm_cls_tok)
+                harm_oct = None if harm_oct_tok.endswith("_NA") else int(harm_oct_tok.rsplit("_", 1)[1])
+                harm_class = (
+                    None if harm_cls_tok.endswith("_NA") else int(harm_cls_tok.rsplit("_", 1)[1])
+                )
+                tokens.extend(
+                    serialize_voice_event(
+                        VoiceEvent(
+                            voice=v,
+                            duration_ticks=ev.duration_tick,
+                            mel_int=mel_int,
+                            harm_oct=harm_oct,
+                            harm_class=harm_class,
+                        )
+                    )
+                )
 
                 prev_pitch[v] = ev.pitch
                 active_until[v] = t + ev.duration_tick
diff --git a/src/tokens/tokenizer.py b/src/tokens/tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..3d7839be70301fe3c5376ba858263c8c4f66da1b
--- /dev/null
+++ b/src/tokens/tokenizer.py
@@ -0,0 +1,219 @@
+from dataclasses import dataclass
+from typing import List, Sequence, Tuple, Union
+
+from src.tokens.intervals import format_signed_interval
+
+
+@dataclass(frozen=True)
+class VoiceEvent:
+    voice: int
+    duration_ticks: int = 0
+    rest_ticks: int = 0
+    mel_int: int = 0
+    harm_oct: Union[int, None] = None
+    harm_class: Union[int, None] = None
+    dup_count: Union[int, None] = None
+    string: Union[int, None] = None
+    fret: Union[int, None] = None
+
+    def __post_init__(self) -> None:
+        if self.voice < 0:
+            raise ValueError("voice must be >= 0")
+        if self.duration_ticks < 0:
+            raise ValueError("duration_ticks must be >= 0")
+        if self.rest_ticks < 0:
+            raise ValueError("rest_ticks must be >= 0")
+        if self.dup_count is not None and self.dup_count < 1:
+            raise ValueError("dup_count must be >= 1 when set")
+        if (self.string is None) ^ (self.fret is None):
+            raise ValueError("string and fret must both be set or both be None")
+
+        is_rest = self.rest_ticks > 0
+        is_pitched = self.duration_ticks > 0
+        if is_rest == is_pitched:
+            raise ValueError("voice event must be exactly one of rest or pitched")
+
+        if is_rest:
+            if self.dup_count is not None:
+                raise ValueError("rest event cannot set dup_count")
+            if self.harm_oct is not None or self.harm_class is not None:
+                raise ValueError("rest event cannot set harmonic interval fields")
+            if self.string is not None or self.fret is not None:
+                raise ValueError("rest event cannot set tab fields")
+            return
+
+        if (self.harm_oct is None) ^ (self.harm_class is None):
+            raise ValueError("harm_oct and harm_class must both be set or both be None")
+        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
+            raise ValueError("harm_class must be in [0, 11]")
+
+    @property
+    def is_rest(self) -> bool:
+        return self.rest_ticks > 0
+
+
+def tokenize_event_text(text: str) -> List[str]:
+    raw = text.replace("\n", ",")
+    tokens = [tok.strip() for tok in raw.split(",")]
+    return [tok for tok in tokens if tok]
+
+
+def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
+    if start_idx >= len(tokens):
+        raise ValueError("start_idx out of range")
+
+    voice_tok = tokens[start_idx]
+    if not voice_tok.startswith("VOICE_"):
+        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")
+
+    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
+    next_idx = start_idx + 1
+    if next_idx >= len(tokens):
+        raise ValueError(f"truncated VOICE event at index {start_idx}")
+
+    first_tok = tokens[next_idx]
+    if first_tok.startswith("REST_"):
+        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
+        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1
+
+    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
+    next_idx += 1
+
+    dup_count = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
+        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
+        next_idx += 1
+
+    if next_idx + 2 >= len(tokens):
+        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")
+
+    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
+    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
+    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
+    next_idx += 3
+
+    string = None
+    fret = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
+        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
+        next_idx += 1
+        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
+            raise ValueError(f"missing FRET token after STR at index {next_idx}")
+        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
+        next_idx += 1
+    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
+        raise ValueError(f"FRET token must follow STR token at index {next_idx}")
+
+    return (
+        VoiceEvent(
+            voice=voice,
+            duration_ticks=duration_ticks,
+            mel_int=mel_int,
+            harm_oct=harm_oct,
+            harm_class=harm_class,
+            dup_count=dup_count,
+            string=string,
+            fret=fret,
+        ),
+        next_idx,
+    )
+
+
+def serialize_voice_event(event: VoiceEvent) -> List[str]:
+    tokens = [f"VOICE_{event.voice}"]
+    if event.is_rest:
+        tokens.append(f"REST_{event.rest_ticks}")
+        return tokens
+
+    tokens.append(f"DUR_{event.duration_ticks}")
+    if event.dup_count is not None:
+        tokens.append(f"DUP_{event.dup_count}")
+
+    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
+    if event.harm_oct is None:
+        tokens.append("HARM_OCT_NA")
+        tokens.append("HARM_CLASS_NA")
+    else:
+        tokens.append(f"HARM_OCT_{event.harm_oct}")
+        tokens.append(f"HARM_CLASS_{event.harm_class}")
+
+    if event.string is not None:
+        tokens.append(f"STR_{event.string}")
+        tokens.append(f"FRET_{event.fret}")
+
+    return tokens
+
+
+def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
+    parsed: List[Union[str, VoiceEvent]] = []
+    idx = 0
+    while idx < len(tokens):
+        tok = tokens[idx]
+        if tok.startswith("VOICE_"):
+            event, idx = parse_voice_event(tokens, idx)
+            parsed.append(event)
+            continue
+        parsed.append(tok)
+        idx += 1
+    return parsed
+
+
+def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
+    tokens: List[str] = []
+    for item in items:
+        if isinstance(item, VoiceEvent):
+            tokens.extend(serialize_voice_event(item))
+        else:
+            tokens.append(item)
+    return tokens
+
+
+def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
+    return serialize_event_stream(parse_event_stream(tokens))
+
+
+def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
+    if not token.startswith(prefix):
+        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
+    value = token[len(prefix) :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc
+
+
+def _parse_signed_mel(token: str, idx: int) -> int:
+    if not token.startswith("MEL_INT12_"):
+        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
+    value = token[len("MEL_INT12_") :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_OCT_"):
+        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
+    value = token[len("HARM_OCT_") :]
+    if value == "NA":
+        return None
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_CLASS_"):
+        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
+    value = token[len("HARM_CLASS_") :]
+    if value == "NA":
+        return None
+    try:
+        parsed = int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
+    if not (0 <= parsed <= 11):
+        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
+    return parsed
diff --git a/tests/test_tokenizer.py b/tests/test_tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..4f4741f7ebc6a851a695b5f6081e10cfc2c03f03
--- /dev/null
+++ b/tests/test_tokenizer.py
@@ -0,0 +1,107 @@
+import sys
+from pathlib import Path
+
+import pytest
+
+sys.path.insert(0, str(Path(__file__).parents[1]))
+
+from src.tokens.tokenizer import (
+    VoiceEvent,
+    canonicalize_event_stream,
+    parse_event_stream,
+    parse_voice_event,
+    serialize_event_stream,
+    serialize_voice_event,
+    tokenize_event_text,
+)
+
+
+def test_parse_pitched_voice_event_with_dup_and_tab():
+    tokens = [
+        "VOICE_2",
+        "DUR_12",
+        "DUP_3",
+        "MEL_INT12_+7",
+        "HARM_OCT_1",
+        "HARM_CLASS_7",
+        "STR_4",
+        "FRET_2",
+        "POS_6",
+    ]
+
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 8
+    assert event.voice == 2
+    assert event.duration_ticks == 12
+    assert event.dup_count == 3
+    assert event.mel_int == 7
+    assert event.harm_oct == 1
+    assert event.harm_class == 7
+    assert event.string == 4
+    assert event.fret == 2
+
+
+def test_parse_rest_voice_event():
+    tokens = ["VOICE_1", "REST_18", "BAR"]
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 2
+    assert event.voice == 1
+    assert event.is_rest
+    assert event.rest_ticks == 18
+
+
+def test_serialize_voice_event_uses_canonical_order():
+    event = VoiceEvent(
+        voice=3,
+        duration_ticks=24,
+        dup_count=2,
+        mel_int=11,
+        harm_oct=0,
+        harm_class=11,
+    )
+    assert serialize_voice_event(event) == [
+        "VOICE_3",
+        "DUR_24",
+        "DUP_2",
+        "MEL_INT12_+11",
+        "HARM_OCT_0",
+        "HARM_CLASS_11",
+    ]
+
+
+def test_canonicalize_event_stream_normalizes_mel_sign():
+    tokens = [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+    assert canonicalize_event_stream(tokens) == [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_+2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+
+
+def test_parse_event_stream_roundtrip_and_text_tokenization():
+    text = "BAR, POS_0, VOICE_0, DUR_4, MEL_INT12_0, HARM_OCT_NA, HARM_CLASS_NA"
+    tokens = tokenize_event_text(text)
+    parsed = parse_event_stream(tokens)
+    assert serialize_event_stream(parsed) == tokens
+
+
+def test_parse_rejects_mixed_harm_na_state():
+    tokens = ["VOICE_0", "DUR_8", "MEL_INT12_0", "HARM_OCT_NA", "HARM_CLASS_2"]
+    with pytest.raises(ValueError, match="harm_oct and harm_class must both be set or both be None"):
+        parse_voice_event(tokens, 0)

file update:
diff --git a/src/tokens/eventizer.py b/src/tokens/eventizer.py
index f7c666b524c3f03edbf62558e3a89e26917d8d43..53385718babf181fdc8367c1ca227731a1c1b799
--- a/src/tokens/eventizer.py
+++ b/src/tokens/eventizer.py
@@ -12,9 +12,9 @@
     IntervalRepairStats,
     compute_reference_pitch,
     compute_melodic_interval,
-    format_signed_interval,
     harmonic_tokens_for_pitch,
 )
+from src.tokens.tokenizer import VoiceEvent, serialize_voice_event
 
 DEFAULT_MAX_VOICES = 8
 DEFAULT_MAX_OCTAVES_PER_PITCH_CLASS = 3
@@ -584,15 +584,12 @@
                         tokens.append(f"ABS_VOICE_{v}_{ev.pitch}")
                     prev_pitch[v] = ev.pitch
 
-                tokens.append(f"VOICE_{v}")
-                tokens.append(f"DUR_{ev.duration_tick}")
                 base_pitch = prev_pitch[v] if prev_pitch[v] is not None else ev.pitch
                 mel_int = compute_melodic_interval(
                     ev.pitch,
                     base_pitch,
                     qa_mode=True,
                 )
-                tokens.append(f"MEL_INT12_{format_signed_interval(mel_int)}")
 
                 harm_oct_tok, harm_cls_tok = harmonic_tokens_for_pitch(
                     ev.pitch,
@@ -600,8 +597,21 @@
                     qa_mode=qa_mode,
                     stats=interval_repairs,
                 )
-                tokens.append(harm_oct_tok)
-                tokens.append(harm_cls_tok)
+                harm_oct = None if harm_oct_tok.endswith("_NA") else int(harm_oct_tok.rsplit("_", 1)[1])
+                harm_class = (
+                    None if harm_cls_tok.endswith("_NA") else int(harm_cls_tok.rsplit("_", 1)[1])
+                )
+                tokens.extend(
+                    serialize_voice_event(
+                        VoiceEvent(
+                            voice=v,
+                            duration_ticks=ev.duration_tick,
+                            mel_int=mel_int,
+                            harm_oct=harm_oct,
+                            harm_class=harm_class,
+                        )
+                    )
+                )
 
                 prev_pitch[v] = ev.pitch
                 active_until[v] = t + ev.duration_tick
diff --git a/src/tokens/tokenizer.py b/src/tokens/tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..3d7839be70301fe3c5376ba858263c8c4f66da1b
--- /dev/null
+++ b/src/tokens/tokenizer.py
@@ -0,0 +1,219 @@
+from dataclasses import dataclass
+from typing import List, Sequence, Tuple, Union
+
+from src.tokens.intervals import format_signed_interval
+
+
+@dataclass(frozen=True)
+class VoiceEvent:
+    voice: int
+    duration_ticks: int = 0
+    rest_ticks: int = 0
+    mel_int: int = 0
+    harm_oct: Union[int, None] = None
+    harm_class: Union[int, None] = None
+    dup_count: Union[int, None] = None
+    string: Union[int, None] = None
+    fret: Union[int, None] = None
+
+    def __post_init__(self) -> None:
+        if self.voice < 0:
+            raise ValueError("voice must be >= 0")
+        if self.duration_ticks < 0:
+            raise ValueError("duration_ticks must be >= 0")
+        if self.rest_ticks < 0:
+            raise ValueError("rest_ticks must be >= 0")
+        if self.dup_count is not None and self.dup_count < 1:
+            raise ValueError("dup_count must be >= 1 when set")
+        if (self.string is None) ^ (self.fret is None):
+            raise ValueError("string and fret must both be set or both be None")
+
+        is_rest = self.rest_ticks > 0
+        is_pitched = self.duration_ticks > 0
+        if is_rest == is_pitched:
+            raise ValueError("voice event must be exactly one of rest or pitched")
+
+        if is_rest:
+            if self.dup_count is not None:
+                raise ValueError("rest event cannot set dup_count")
+            if self.harm_oct is not None or self.harm_class is not None:
+                raise ValueError("rest event cannot set harmonic interval fields")
+            if self.string is not None or self.fret is not None:
+                raise ValueError("rest event cannot set tab fields")
+            return
+
+        if (self.harm_oct is None) ^ (self.harm_class is None):
+            raise ValueError("harm_oct and harm_class must both be set or both be None")
+        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
+            raise ValueError("harm_class must be in [0, 11]")
+
+    @property
+    def is_rest(self) -> bool:
+        return self.rest_ticks > 0
+
+
+def tokenize_event_text(text: str) -> List[str]:
+    raw = text.replace("\n", ",")
+    tokens = [tok.strip() for tok in raw.split(",")]
+    return [tok for tok in tokens if tok]
+
+
+def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
+    if start_idx >= len(tokens):
+        raise ValueError("start_idx out of range")
+
+    voice_tok = tokens[start_idx]
+    if not voice_tok.startswith("VOICE_"):
+        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")
+
+    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
+    next_idx = start_idx + 1
+    if next_idx >= len(tokens):
+        raise ValueError(f"truncated VOICE event at index {start_idx}")
+
+    first_tok = tokens[next_idx]
+    if first_tok.startswith("REST_"):
+        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
+        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1
+
+    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
+    next_idx += 1
+
+    dup_count = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
+        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
+        next_idx += 1
+
+    if next_idx + 2 >= len(tokens):
+        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")
+
+    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
+    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
+    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
+    next_idx += 3
+
+    string = None
+    fret = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
+        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
+        next_idx += 1
+        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
+            raise ValueError(f"missing FRET token after STR at index {next_idx}")
+        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
+        next_idx += 1
+    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
+        raise ValueError(f"FRET token must follow STR token at index {next_idx}")
+
+    return (
+        VoiceEvent(
+            voice=voice,
+            duration_ticks=duration_ticks,
+            mel_int=mel_int,
+            harm_oct=harm_oct,
+            harm_class=harm_class,
+            dup_count=dup_count,
+            string=string,
+            fret=fret,
+        ),
+        next_idx,
+    )
+
+
+def serialize_voice_event(event: VoiceEvent) -> List[str]:
+    tokens = [f"VOICE_{event.voice}"]
+    if event.is_rest:
+        tokens.append(f"REST_{event.rest_ticks}")
+        return tokens
+
+    tokens.append(f"DUR_{event.duration_ticks}")
+    if event.dup_count is not None:
+        tokens.append(f"DUP_{event.dup_count}")
+
+    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
+    if event.harm_oct is None:
+        tokens.append("HARM_OCT_NA")
+        tokens.append("HARM_CLASS_NA")
+    else:
+        tokens.append(f"HARM_OCT_{event.harm_oct}")
+        tokens.append(f"HARM_CLASS_{event.harm_class}")
+
+    if event.string is not None:
+        tokens.append(f"STR_{event.string}")
+        tokens.append(f"FRET_{event.fret}")
+
+    return tokens
+
+
+def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
+    parsed: List[Union[str, VoiceEvent]] = []
+    idx = 0
+    while idx < len(tokens):
+        tok = tokens[idx]
+        if tok.startswith("VOICE_"):
+            event, idx = parse_voice_event(tokens, idx)
+            parsed.append(event)
+            continue
+        parsed.append(tok)
+        idx += 1
+    return parsed
+
+
+def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
+    tokens: List[str] = []
+    for item in items:
+        if isinstance(item, VoiceEvent):
+            tokens.extend(serialize_voice_event(item))
+        else:
+            tokens.append(item)
+    return tokens
+
+
+def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
+    return serialize_event_stream(parse_event_stream(tokens))
+
+
+def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
+    if not token.startswith(prefix):
+        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
+    value = token[len(prefix) :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc
+
+
+def _parse_signed_mel(token: str, idx: int) -> int:
+    if not token.startswith("MEL_INT12_"):
+        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
+    value = token[len("MEL_INT12_") :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_OCT_"):
+        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
+    value = token[len("HARM_OCT_") :]
+    if value == "NA":
+        return None
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_CLASS_"):
+        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
+    value = token[len("HARM_CLASS_") :]
+    if value == "NA":
+        return None
+    try:
+        parsed = int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
+    if not (0 <= parsed <= 11):
+        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
+    return parsed
diff --git a/tests/test_tokenizer.py b/tests/test_tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..4f4741f7ebc6a851a695b5f6081e10cfc2c03f03
--- /dev/null
+++ b/tests/test_tokenizer.py
@@ -0,0 +1,107 @@
+import sys
+from pathlib import Path
+
+import pytest
+
+sys.path.insert(0, str(Path(__file__).parents[1]))
+
+from src.tokens.tokenizer import (
+    VoiceEvent,
+    canonicalize_event_stream,
+    parse_event_stream,
+    parse_voice_event,
+    serialize_event_stream,
+    serialize_voice_event,
+    tokenize_event_text,
+)
+
+
+def test_parse_pitched_voice_event_with_dup_and_tab():
+    tokens = [
+        "VOICE_2",
+        "DUR_12",
+        "DUP_3",
+        "MEL_INT12_+7",
+        "HARM_OCT_1",
+        "HARM_CLASS_7",
+        "STR_4",
+        "FRET_2",
+        "POS_6",
+    ]
+
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 8
+    assert event.voice == 2
+    assert event.duration_ticks == 12
+    assert event.dup_count == 3
+    assert event.mel_int == 7
+    assert event.harm_oct == 1
+    assert event.harm_class == 7
+    assert event.string == 4
+    assert event.fret == 2
+
+
+def test_parse_rest_voice_event():
+    tokens = ["VOICE_1", "REST_18", "BAR"]
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 2
+    assert event.voice == 1
+    assert event.is_rest
+    assert event.rest_ticks == 18
+
+
+def test_serialize_voice_event_uses_canonical_order():
+    event = VoiceEvent(
+        voice=3,
+        duration_ticks=24,
+        dup_count=2,
+        mel_int=11,
+        harm_oct=0,
+        harm_class=11,
+    )
+    assert serialize_voice_event(event) == [
+        "VOICE_3",
+        "DUR_24",
+        "DUP_2",
+        "MEL_INT12_+11",
+        "HARM_OCT_0",
+        "HARM_CLASS_11",
+    ]
+
+
+def test_canonicalize_event_stream_normalizes_mel_sign():
+    tokens = [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+    assert canonicalize_event_stream(tokens) == [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_+2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+
+
+def test_parse_event_stream_roundtrip_and_text_tokenization():
+    text = "BAR, POS_0, VOICE_0, DUR_4, MEL_INT12_0, HARM_OCT_NA, HARM_CLASS_NA"
+    tokens = tokenize_event_text(text)
+    parsed = parse_event_stream(tokens)
+    assert serialize_event_stream(parsed) == tokens
+
+
+def test_parse_rejects_mixed_harm_na_state():
+    tokens = ["VOICE_0", "DUR_8", "MEL_INT12_0", "HARM_OCT_NA", "HARM_CLASS_2"]
+    with pytest.raises(ValueError, match="harm_oct and harm_class must both be set or both be None"):
+        parse_voice_event(tokens, 0)

file update:
diff --git a/src/tokens/eventizer.py b/src/tokens/eventizer.py
index f7c666b524c3f03edbf62558e3a89e26917d8d43..53385718babf181fdc8367c1ca227731a1c1b799
--- a/src/tokens/eventizer.py
+++ b/src/tokens/eventizer.py
@@ -12,9 +12,9 @@
     IntervalRepairStats,
     compute_reference_pitch,
     compute_melodic_interval,
-    format_signed_interval,
     harmonic_tokens_for_pitch,
 )
+from src.tokens.tokenizer import VoiceEvent, serialize_voice_event
 
 DEFAULT_MAX_VOICES = 8
 DEFAULT_MAX_OCTAVES_PER_PITCH_CLASS = 3
@@ -584,15 +584,12 @@
                         tokens.append(f"ABS_VOICE_{v}_{ev.pitch}")
                     prev_pitch[v] = ev.pitch
 
-                tokens.append(f"VOICE_{v}")
-                tokens.append(f"DUR_{ev.duration_tick}")
                 base_pitch = prev_pitch[v] if prev_pitch[v] is not None else ev.pitch
                 mel_int = compute_melodic_interval(
                     ev.pitch,
                     base_pitch,
                     qa_mode=True,
                 )
-                tokens.append(f"MEL_INT12_{format_signed_interval(mel_int)}")
 
                 harm_oct_tok, harm_cls_tok = harmonic_tokens_for_pitch(
                     ev.pitch,
@@ -600,8 +597,21 @@
                     qa_mode=qa_mode,
                     stats=interval_repairs,
                 )
-                tokens.append(harm_oct_tok)
-                tokens.append(harm_cls_tok)
+                harm_oct = None if harm_oct_tok.endswith("_NA") else int(harm_oct_tok.rsplit("_", 1)[1])
+                harm_class = (
+                    None if harm_cls_tok.endswith("_NA") else int(harm_cls_tok.rsplit("_", 1)[1])
+                )
+                tokens.extend(
+                    serialize_voice_event(
+                        VoiceEvent(
+                            voice=v,
+                            duration_ticks=ev.duration_tick,
+                            mel_int=mel_int,
+                            harm_oct=harm_oct,
+                            harm_class=harm_class,
+                        )
+                    )
+                )
 
                 prev_pitch[v] = ev.pitch
                 active_until[v] = t + ev.duration_tick
diff --git a/src/tokens/tokenizer.py b/src/tokens/tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..3d7839be70301fe3c5376ba858263c8c4f66da1b
--- /dev/null
+++ b/src/tokens/tokenizer.py
@@ -0,0 +1,219 @@
+from dataclasses import dataclass
+from typing import List, Sequence, Tuple, Union
+
+from src.tokens.intervals import format_signed_interval
+
+
+@dataclass(frozen=True)
+class VoiceEvent:
+    voice: int
+    duration_ticks: int = 0
+    rest_ticks: int = 0
+    mel_int: int = 0
+    harm_oct: Union[int, None] = None
+    harm_class: Union[int, None] = None
+    dup_count: Union[int, None] = None
+    string: Union[int, None] = None
+    fret: Union[int, None] = None
+
+    def __post_init__(self) -> None:
+        if self.voice < 0:
+            raise ValueError("voice must be >= 0")
+        if self.duration_ticks < 0:
+            raise ValueError("duration_ticks must be >= 0")
+        if self.rest_ticks < 0:
+            raise ValueError("rest_ticks must be >= 0")
+        if self.dup_count is not None and self.dup_count < 1:
+            raise ValueError("dup_count must be >= 1 when set")
+        if (self.string is None) ^ (self.fret is None):
+            raise ValueError("string and fret must both be set or both be None")
+
+        is_rest = self.rest_ticks > 0
+        is_pitched = self.duration_ticks > 0
+        if is_rest == is_pitched:
+            raise ValueError("voice event must be exactly one of rest or pitched")
+
+        if is_rest:
+            if self.dup_count is not None:
+                raise ValueError("rest event cannot set dup_count")
+            if self.harm_oct is not None or self.harm_class is not None:
+                raise ValueError("rest event cannot set harmonic interval fields")
+            if self.string is not None or self.fret is not None:
+                raise ValueError("rest event cannot set tab fields")
+            return
+
+        if (self.harm_oct is None) ^ (self.harm_class is None):
+            raise ValueError("harm_oct and harm_class must both be set or both be None")
+        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
+            raise ValueError("harm_class must be in [0, 11]")
+
+    @property
+    def is_rest(self) -> bool:
+        return self.rest_ticks > 0
+
+
+def tokenize_event_text(text: str) -> List[str]:
+    raw = text.replace("\n", ",")
+    tokens = [tok.strip() for tok in raw.split(",")]
+    return [tok for tok in tokens if tok]
+
+
+def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
+    if start_idx >= len(tokens):
+        raise ValueError("start_idx out of range")
+
+    voice_tok = tokens[start_idx]
+    if not voice_tok.startswith("VOICE_"):
+        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")
+
+    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
+    next_idx = start_idx + 1
+    if next_idx >= len(tokens):
+        raise ValueError(f"truncated VOICE event at index {start_idx}")
+
+    first_tok = tokens[next_idx]
+    if first_tok.startswith("REST_"):
+        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
+        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1
+
+    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
+    next_idx += 1
+
+    dup_count = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
+        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
+        next_idx += 1
+
+    if next_idx + 2 >= len(tokens):
+        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")
+
+    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
+    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
+    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
+    next_idx += 3
+
+    string = None
+    fret = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
+        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
+        next_idx += 1
+        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
+            raise ValueError(f"missing FRET token after STR at index {next_idx}")
+        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
+        next_idx += 1
+    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
+        raise ValueError(f"FRET token must follow STR token at index {next_idx}")
+
+    return (
+        VoiceEvent(
+            voice=voice,
+            duration_ticks=duration_ticks,
+            mel_int=mel_int,
+            harm_oct=harm_oct,
+            harm_class=harm_class,
+            dup_count=dup_count,
+            string=string,
+            fret=fret,
+        ),
+        next_idx,
+    )
+
+
+def serialize_voice_event(event: VoiceEvent) -> List[str]:
+    tokens = [f"VOICE_{event.voice}"]
+    if event.is_rest:
+        tokens.append(f"REST_{event.rest_ticks}")
+        return tokens
+
+    tokens.append(f"DUR_{event.duration_ticks}")
+    if event.dup_count is not None:
+        tokens.append(f"DUP_{event.dup_count}")
+
+    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
+    if event.harm_oct is None:
+        tokens.append("HARM_OCT_NA")
+        tokens.append("HARM_CLASS_NA")
+    else:
+        tokens.append(f"HARM_OCT_{event.harm_oct}")
+        tokens.append(f"HARM_CLASS_{event.harm_class}")
+
+    if event.string is not None:
+        tokens.append(f"STR_{event.string}")
+        tokens.append(f"FRET_{event.fret}")
+
+    return tokens
+
+
+def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
+    parsed: List[Union[str, VoiceEvent]] = []
+    idx = 0
+    while idx < len(tokens):
+        tok = tokens[idx]
+        if tok.startswith("VOICE_"):
+            event, idx = parse_voice_event(tokens, idx)
+            parsed.append(event)
+            continue
+        parsed.append(tok)
+        idx += 1
+    return parsed
+
+
+def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
+    tokens: List[str] = []
+    for item in items:
+        if isinstance(item, VoiceEvent):
+            tokens.extend(serialize_voice_event(item))
+        else:
+            tokens.append(item)
+    return tokens
+
+
+def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
+    return serialize_event_stream(parse_event_stream(tokens))
+
+
+def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
+    if not token.startswith(prefix):
+        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
+    value = token[len(prefix) :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc
+
+
+def _parse_signed_mel(token: str, idx: int) -> int:
+    if not token.startswith("MEL_INT12_"):
+        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
+    value = token[len("MEL_INT12_") :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_OCT_"):
+        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
+    value = token[len("HARM_OCT_") :]
+    if value == "NA":
+        return None
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_CLASS_"):
+        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
+    value = token[len("HARM_CLASS_") :]
+    if value == "NA":
+        return None
+    try:
+        parsed = int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
+    if not (0 <= parsed <= 11):
+        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
+    return parsed
diff --git a/tests/test_tokenizer.py b/tests/test_tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..4f4741f7ebc6a851a695b5f6081e10cfc2c03f03
--- /dev/null
+++ b/tests/test_tokenizer.py
@@ -0,0 +1,107 @@
+import sys
+from pathlib import Path
+
+import pytest
+
+sys.path.insert(0, str(Path(__file__).parents[1]))
+
+from src.tokens.tokenizer import (
+    VoiceEvent,
+    canonicalize_event_stream,
+    parse_event_stream,
+    parse_voice_event,
+    serialize_event_stream,
+    serialize_voice_event,
+    tokenize_event_text,
+)
+
+
+def test_parse_pitched_voice_event_with_dup_and_tab():
+    tokens = [
+        "VOICE_2",
+        "DUR_12",
+        "DUP_3",
+        "MEL_INT12_+7",
+        "HARM_OCT_1",
+        "HARM_CLASS_7",
+        "STR_4",
+        "FRET_2",
+        "POS_6",
+    ]
+
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 8
+    assert event.voice == 2
+    assert event.duration_ticks == 12
+    assert event.dup_count == 3
+    assert event.mel_int == 7
+    assert event.harm_oct == 1
+    assert event.harm_class == 7
+    assert event.string == 4
+    assert event.fret == 2
+
+
+def test_parse_rest_voice_event():
+    tokens = ["VOICE_1", "REST_18", "BAR"]
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 2
+    assert event.voice == 1
+    assert event.is_rest
+    assert event.rest_ticks == 18
+
+
+def test_serialize_voice_event_uses_canonical_order():
+    event = VoiceEvent(
+        voice=3,
+        duration_ticks=24,
+        dup_count=2,
+        mel_int=11,
+        harm_oct=0,
+        harm_class=11,
+    )
+    assert serialize_voice_event(event) == [
+        "VOICE_3",
+        "DUR_24",
+        "DUP_2",
+        "MEL_INT12_+11",
+        "HARM_OCT_0",
+        "HARM_CLASS_11",
+    ]
+
+
+def test_canonicalize_event_stream_normalizes_mel_sign():
+    tokens = [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+    assert canonicalize_event_stream(tokens) == [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_+2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+
+
+def test_parse_event_stream_roundtrip_and_text_tokenization():
+    text = "BAR, POS_0, VOICE_0, DUR_4, MEL_INT12_0, HARM_OCT_NA, HARM_CLASS_NA"
+    tokens = tokenize_event_text(text)
+    parsed = parse_event_stream(tokens)
+    assert serialize_event_stream(parsed) == tokens
+
+
+def test_parse_rejects_mixed_harm_na_state():
+    tokens = ["VOICE_0", "DUR_8", "MEL_INT12_0", "HARM_OCT_NA", "HARM_CLASS_2"]
+    with pytest.raises(ValueError, match="harm_oct and harm_class must both be set or both be None"):
+        parse_voice_event(tokens, 0)

 succeeded in 50.06s:
...............s............................................             [100%]
59 passed, 1 skipped in 47.77s


thinking
**Synchronizing TODO and progress**
codex
{"done":false,"progress_made":true,"summary":"Full test suite passed (`59 passed, 1 skipped`). I’m now updating planning/status files: mark the completed ExecPlan/TODO item and append a PROGRESS entry for this change set.","tests_ran":true,"tests_status":"pass","next_step":"Edit `docs/exec/EXECPLAN_stage1.md`, `TODO.md`, and `PROGRESS.md`.","stop_reason":""}

thinking
**Updating TODO and docs consistently**
file update
M /home/stephen-reilly/dev/bach_gen/docs/exec/EXECPLAN_stage1.md
@@ -13,3 +13,3 @@
 - [x] Implement interval math helpers (melodic + harmonic ref) with QA vs production behavior.
-- [ ] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering.
+- [x] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering.
 - [ ] Add round-trip tests for pitch reconstruction and `HARM_*` consistency.
apply_patch(auto_approved=true) exited 0 in 29ms:
Success. Updated the following files:
M docs/exec/EXECPLAN_stage1.md
file update:
diff --git a/docs/exec/EXECPLAN_stage1.md b/docs/exec/EXECPLAN_stage1.md
index 7f2647e15e5b673df54c0e21549951d68753a8c6..b8ee585fca521d0ea892fa6b21d13bb879078f5d
--- a/docs/exec/EXECPLAN_stage1.md
+++ b/docs/exec/EXECPLAN_stage1.md
@@ -11,7 +11,7 @@
 ## Checklist (one item per loop)
 - [x] Define EventSpec + DescriptorSpec in `src/tokens/schema.py` with version `remi_tab_v1` and `tpq=24`.
 - [x] Implement interval math helpers (melodic + harmonic ref) with QA vs production behavior.
-- [ ] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering.
+- [x] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering.
 - [ ] Add round-trip tests for pitch reconstruction and `HARM_*` consistency.
 - [ ] Implement `scripts/make_dataset.py` basic pipeline (parse, normalize, collapse doublings, track assignment, tokenize, stats).
 - [ ] Produce dataset artifacts in `data/processed/` and document counts in `stats.json`.
diff --git a/src/tokens/eventizer.py b/src/tokens/eventizer.py
index f7c666b524c3f03edbf62558e3a89e26917d8d43..53385718babf181fdc8367c1ca227731a1c1b799
--- a/src/tokens/eventizer.py
+++ b/src/tokens/eventizer.py
@@ -12,9 +12,9 @@
     IntervalRepairStats,
     compute_reference_pitch,
     compute_melodic_interval,
-    format_signed_interval,
     harmonic_tokens_for_pitch,
 )
+from src.tokens.tokenizer import VoiceEvent, serialize_voice_event
 
 DEFAULT_MAX_VOICES = 8
 DEFAULT_MAX_OCTAVES_PER_PITCH_CLASS = 3
@@ -584,15 +584,12 @@
                         tokens.append(f"ABS_VOICE_{v}_{ev.pitch}")
                     prev_pitch[v] = ev.pitch
 
-                tokens.append(f"VOICE_{v}")
-                tokens.append(f"DUR_{ev.duration_tick}")
                 base_pitch = prev_pitch[v] if prev_pitch[v] is not None else ev.pitch
                 mel_int = compute_melodic_interval(
                     ev.pitch,
                     base_pitch,
                     qa_mode=True,
                 )
-                tokens.append(f"MEL_INT12_{format_signed_interval(mel_int)}")
 
                 harm_oct_tok, harm_cls_tok = harmonic_tokens_for_pitch(
                     ev.pitch,
@@ -600,8 +597,21 @@
                     qa_mode=qa_mode,
                     stats=interval_repairs,
                 )
-                tokens.append(harm_oct_tok)
-                tokens.append(harm_cls_tok)
+                harm_oct = None if harm_oct_tok.endswith("_NA") else int(harm_oct_tok.rsplit("_", 1)[1])
+                harm_class = (
+                    None if harm_cls_tok.endswith("_NA") else int(harm_cls_tok.rsplit("_", 1)[1])
+                )
+                tokens.extend(
+                    serialize_voice_event(
+                        VoiceEvent(
+                            voice=v,
+                            duration_ticks=ev.duration_tick,
+                            mel_int=mel_int,
+                            harm_oct=harm_oct,
+                            harm_class=harm_class,
+                        )
+                    )
+                )
 
                 prev_pitch[v] = ev.pitch
                 active_until[v] = t + ev.duration_tick
diff --git a/src/tokens/tokenizer.py b/src/tokens/tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..3d7839be70301fe3c5376ba858263c8c4f66da1b
--- /dev/null
+++ b/src/tokens/tokenizer.py
@@ -0,0 +1,219 @@
+from dataclasses import dataclass
+from typing import List, Sequence, Tuple, Union
+
+from src.tokens.intervals import format_signed_interval
+
+
+@dataclass(frozen=True)
+class VoiceEvent:
+    voice: int
+    duration_ticks: int = 0
+    rest_ticks: int = 0
+    mel_int: int = 0
+    harm_oct: Union[int, None] = None
+    harm_class: Union[int, None] = None
+    dup_count: Union[int, None] = None
+    string: Union[int, None] = None
+    fret: Union[int, None] = None
+
+    def __post_init__(self) -> None:
+        if self.voice < 0:
+            raise ValueError("voice must be >= 0")
+        if self.duration_ticks < 0:
+            raise ValueError("duration_ticks must be >= 0")
+        if self.rest_ticks < 0:
+            raise ValueError("rest_ticks must be >= 0")
+        if self.dup_count is not None and self.dup_count < 1:
+            raise ValueError("dup_count must be >= 1 when set")
+        if (self.string is None) ^ (self.fret is None):
+            raise ValueError("string and fret must both be set or both be None")
+
+        is_rest = self.rest_ticks > 0
+        is_pitched = self.duration_ticks > 0
+        if is_rest == is_pitched:
+            raise ValueError("voice event must be exactly one of rest or pitched")
+
+        if is_rest:
+            if self.dup_count is not None:
+                raise ValueError("rest event cannot set dup_count")
+            if self.harm_oct is not None or self.harm_class is not None:
+                raise ValueError("rest event cannot set harmonic interval fields")
+            if self.string is not None or self.fret is not None:
+                raise ValueError("rest event cannot set tab fields")
+            return
+
+        if (self.harm_oct is None) ^ (self.harm_class is None):
+            raise ValueError("harm_oct and harm_class must both be set or both be None")
+        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
+            raise ValueError("harm_class must be in [0, 11]")
+
+    @property
+    def is_rest(self) -> bool:
+        return self.rest_ticks > 0
+
+
+def tokenize_event_text(text: str) -> List[str]:
+    raw = text.replace("\n", ",")
+    tokens = [tok.strip() for tok in raw.split(",")]
+    return [tok for tok in tokens if tok]
+
+
+def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
+    if start_idx >= len(tokens):
+        raise ValueError("start_idx out of range")
+
+    voice_tok = tokens[start_idx]
+    if not voice_tok.startswith("VOICE_"):
+        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")
+
+    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
+    next_idx = start_idx + 1
+    if next_idx >= len(tokens):
+        raise ValueError(f"truncated VOICE event at index {start_idx}")
+
+    first_tok = tokens[next_idx]
+    if first_tok.startswith("REST_"):
+        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
+        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1
+
+    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
+    next_idx += 1
+
+    dup_count = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
+        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
+        next_idx += 1
+
+    if next_idx + 2 >= len(tokens):
+        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")
+
+    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
+    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
+    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
+    next_idx += 3
+
+    string = None
+    fret = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
+        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
+        next_idx += 1
+        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
+            raise ValueError(f"missing FRET token after STR at index {next_idx}")
+        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
+        next_idx += 1
+    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
+        raise ValueError(f"FRET token must follow STR token at index {next_idx}")
+
+    return (
+        VoiceEvent(
+            voice=voice,
+            duration_ticks=duration_ticks,
+            mel_int=mel_int,
+            harm_oct=harm_oct,
+            harm_class=harm_class,
+            dup_count=dup_count,
+            string=string,
+            fret=fret,
+        ),
+        next_idx,
+    )
+
+
+def serialize_voice_event(event: VoiceEvent) -> List[str]:
+    tokens = [f"VOICE_{event.voice}"]
+    if event.is_rest:
+        tokens.append(f"REST_{event.rest_ticks}")
+        return tokens
+
+    tokens.append(f"DUR_{event.duration_ticks}")
+    if event.dup_count is not None:
+        tokens.append(f"DUP_{event.dup_count}")
+
+    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
+    if event.harm_oct is None:
+        tokens.append("HARM_OCT_NA")
+        tokens.append("HARM_CLASS_NA")
+    else:
+        tokens.append(f"HARM_OCT_{event.harm_oct}")
+        tokens.append(f"HARM_CLASS_{event.harm_class}")
+
+    if event.string is not None:
+        tokens.append(f"STR_{event.string}")
+        tokens.append(f"FRET_{event.fret}")
+
+    return tokens
+
+
+def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
+    parsed: List[Union[str, VoiceEvent]] = []
+    idx = 0
+    while idx < len(tokens):
+        tok = tokens[idx]
+        if tok.startswith("VOICE_"):
+            event, idx = parse_voice_event(tokens, idx)
+            parsed.append(event)
+            continue
+        parsed.append(tok)
+        idx += 1
+    return parsed
+
+
+def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
+    tokens: List[str] = []
+    for item in items:
+        if isinstance(item, VoiceEvent):
+            tokens.extend(serialize_voice_event(item))
+        else:
+            tokens.append(item)
+    return tokens
+
+
+def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
+    return serialize_event_stream(parse_event_stream(tokens))
+
+
+def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
+    if not token.startswith(prefix):
+        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
+    value = token[len(prefix) :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc
+
+
+def _parse_signed_mel(token: str, idx: int) -> int:
+    if not token.startswith("MEL_INT12_"):
+        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
+    value = token[len("MEL_INT12_") :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_OCT_"):
+        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
+    value = token[len("HARM_OCT_") :]
+    if value == "NA":
+        return None
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_CLASS_"):
+        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
+    value = token[len("HARM_CLASS_") :]
+    if value == "NA":
+        return None
+    try:
+        parsed = int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
+    if not (0 <= parsed <= 11):
+        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
+    return parsed
diff --git a/tests/test_tokenizer.py b/tests/test_tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..4f4741f7ebc6a851a695b5f6081e10cfc2c03f03
--- /dev/null
+++ b/tests/test_tokenizer.py
@@ -0,0 +1,107 @@
+import sys
+from pathlib import Path
+
+import pytest
+
+sys.path.insert(0, str(Path(__file__).parents[1]))
+
+from src.tokens.tokenizer import (
+    VoiceEvent,
+    canonicalize_event_stream,
+    parse_event_stream,
+    parse_voice_event,
+    serialize_event_stream,
+    serialize_voice_event,
+    tokenize_event_text,
+)
+
+
+def test_parse_pitched_voice_event_with_dup_and_tab():
+    tokens = [
+        "VOICE_2",
+        "DUR_12",
+        "DUP_3",
+        "MEL_INT12_+7",
+        "HARM_OCT_1",
+        "HARM_CLASS_7",
+        "STR_4",
+        "FRET_2",
+        "POS_6",
+    ]
+
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 8
+    assert event.voice == 2
+    assert event.duration_ticks == 12
+    assert event.dup_count == 3
+    assert event.mel_int == 7
+    assert event.harm_oct == 1
+    assert event.harm_class == 7
+    assert event.string == 4
+    assert event.fret == 2
+
+
+def test_parse_rest_voice_event():
+    tokens = ["VOICE_1", "REST_18", "BAR"]
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 2
+    assert event.voice == 1
+    assert event.is_rest
+    assert event.rest_ticks == 18
+
+
+def test_serialize_voice_event_uses_canonical_order():
+    event = VoiceEvent(
+        voice=3,
+        duration_ticks=24,
+        dup_count=2,
+        mel_int=11,
+        harm_oct=0,
+        harm_class=11,
+    )
+    assert serialize_voice_event(event) == [
+        "VOICE_3",
+        "DUR_24",
+        "DUP_2",
+        "MEL_INT12_+11",
+        "HARM_OCT_0",
+        "HARM_CLASS_11",
+    ]
+
+
+def test_canonicalize_event_stream_normalizes_mel_sign():
+    tokens = [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+    assert canonicalize_event_stream(tokens) == [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_+2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+
+
+def test_parse_event_stream_roundtrip_and_text_tokenization():
+    text = "BAR, POS_0, VOICE_0, DUR_4, MEL_INT12_0, HARM_OCT_NA, HARM_CLASS_NA"
+    tokens = tokenize_event_text(text)
+    parsed = parse_event_stream(tokens)
+    assert serialize_event_stream(parsed) == tokens
+
+
+def test_parse_rejects_mixed_harm_na_state():
+    tokens = ["VOICE_0", "DUR_8", "MEL_INT12_0", "HARM_OCT_NA", "HARM_CLASS_2"]
+    with pytest.raises(ValueError, match="harm_oct and harm_class must both be set or both be None"):
+        parse_voice_event(tokens, 0)

file update:
diff --git a/docs/exec/EXECPLAN_stage1.md b/docs/exec/EXECPLAN_stage1.md
index 7f2647e15e5b673df54c0e21549951d68753a8c6..b8ee585fca521d0ea892fa6b21d13bb879078f5d
--- a/docs/exec/EXECPLAN_stage1.md
+++ b/docs/exec/EXECPLAN_stage1.md
@@ -11,7 +11,7 @@
 ## Checklist (one item per loop)
 - [x] Define EventSpec + DescriptorSpec in `src/tokens/schema.py` with version `remi_tab_v1` and `tpq=24`.
 - [x] Implement interval math helpers (melodic + harmonic ref) with QA vs production behavior.
-- [ ] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering.
+- [x] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering.
 - [ ] Add round-trip tests for pitch reconstruction and `HARM_*` consistency.
 - [ ] Implement `scripts/make_dataset.py` basic pipeline (parse, normalize, collapse doublings, track assignment, tokenize, stats).
 - [ ] Produce dataset artifacts in `data/processed/` and document counts in `stats.json`.
diff --git a/src/tokens/eventizer.py b/src/tokens/eventizer.py
index f7c666b524c3f03edbf62558e3a89e26917d8d43..53385718babf181fdc8367c1ca227731a1c1b799
--- a/src/tokens/eventizer.py
+++ b/src/tokens/eventizer.py
@@ -12,9 +12,9 @@
     IntervalRepairStats,
     compute_reference_pitch,
     compute_melodic_interval,
-    format_signed_interval,
     harmonic_tokens_for_pitch,
 )
+from src.tokens.tokenizer import VoiceEvent, serialize_voice_event
 
 DEFAULT_MAX_VOICES = 8
 DEFAULT_MAX_OCTAVES_PER_PITCH_CLASS = 3
@@ -584,15 +584,12 @@
                         tokens.append(f"ABS_VOICE_{v}_{ev.pitch}")
                     prev_pitch[v] = ev.pitch
 
-                tokens.append(f"VOICE_{v}")
-                tokens.append(f"DUR_{ev.duration_tick}")
                 base_pitch = prev_pitch[v] if prev_pitch[v] is not None else ev.pitch
                 mel_int = compute_melodic_interval(
                     ev.pitch,
                     base_pitch,
                     qa_mode=True,
                 )
-                tokens.append(f"MEL_INT12_{format_signed_interval(mel_int)}")
 
                 harm_oct_tok, harm_cls_tok = harmonic_tokens_for_pitch(
                     ev.pitch,
@@ -600,8 +597,21 @@
                     qa_mode=qa_mode,
                     stats=interval_repairs,
                 )
-                tokens.append(harm_oct_tok)
-                tokens.append(harm_cls_tok)
+                harm_oct = None if harm_oct_tok.endswith("_NA") else int(harm_oct_tok.rsplit("_", 1)[1])
+                harm_class = (
+                    None if harm_cls_tok.endswith("_NA") else int(harm_cls_tok.rsplit("_", 1)[1])
+                )
+                tokens.extend(
+                    serialize_voice_event(
+                        VoiceEvent(
+                            voice=v,
+                            duration_ticks=ev.duration_tick,
+                            mel_int=mel_int,
+                            harm_oct=harm_oct,
+                            harm_class=harm_class,
+                        )
+                    )
+                )
 
                 prev_pitch[v] = ev.pitch
                 active_until[v] = t + ev.duration_tick
diff --git a/src/tokens/tokenizer.py b/src/tokens/tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..3d7839be70301fe3c5376ba858263c8c4f66da1b
--- /dev/null
+++ b/src/tokens/tokenizer.py
@@ -0,0 +1,219 @@
+from dataclasses import dataclass
+from typing import List, Sequence, Tuple, Union
+
+from src.tokens.intervals import format_signed_interval
+
+
+@dataclass(frozen=True)
+class VoiceEvent:
+    voice: int
+    duration_ticks: int = 0
+    rest_ticks: int = 0
+    mel_int: int = 0
+    harm_oct: Union[int, None] = None
+    harm_class: Union[int, None] = None
+    dup_count: Union[int, None] = None
+    string: Union[int, None] = None
+    fret: Union[int, None] = None
+
+    def __post_init__(self) -> None:
+        if self.voice < 0:
+            raise ValueError("voice must be >= 0")
+        if self.duration_ticks < 0:
+            raise ValueError("duration_ticks must be >= 0")
+        if self.rest_ticks < 0:
+            raise ValueError("rest_ticks must be >= 0")
+        if self.dup_count is not None and self.dup_count < 1:
+            raise ValueError("dup_count must be >= 1 when set")
+        if (self.string is None) ^ (self.fret is None):
+            raise ValueError("string and fret must both be set or both be None")
+
+        is_rest = self.rest_ticks > 0
+        is_pitched = self.duration_ticks > 0
+        if is_rest == is_pitched:
+            raise ValueError("voice event must be exactly one of rest or pitched")
+
+        if is_rest:
+            if self.dup_count is not None:
+                raise ValueError("rest event cannot set dup_count")
+            if self.harm_oct is not None or self.harm_class is not None:
+                raise ValueError("rest event cannot set harmonic interval fields")
+            if self.string is not None or self.fret is not None:
+                raise ValueError("rest event cannot set tab fields")
+            return
+
+        if (self.harm_oct is None) ^ (self.harm_class is None):
+            raise ValueError("harm_oct and harm_class must both be set or both be None")
+        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
+            raise ValueError("harm_class must be in [0, 11]")
+
+    @property
+    def is_rest(self) -> bool:
+        return self.rest_ticks > 0
+
+
+def tokenize_event_text(text: str) -> List[str]:
+    raw = text.replace("\n", ",")
+    tokens = [tok.strip() for tok in raw.split(",")]
+    return [tok for tok in tokens if tok]
+
+
+def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
+    if start_idx >= len(tokens):
+        raise ValueError("start_idx out of range")
+
+    voice_tok = tokens[start_idx]
+    if not voice_tok.startswith("VOICE_"):
+        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")
+
+    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
+    next_idx = start_idx + 1
+    if next_idx >= len(tokens):
+        raise ValueError(f"truncated VOICE event at index {start_idx}")
+
+    first_tok = tokens[next_idx]
+    if first_tok.startswith("REST_"):
+        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
+        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1
+
+    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
+    next_idx += 1
+
+    dup_count = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
+        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
+        next_idx += 1
+
+    if next_idx + 2 >= len(tokens):
+        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")
+
+    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
+    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
+    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
+    next_idx += 3
+
+    string = None
+    fret = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
+        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
+        next_idx += 1
+        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
+            raise ValueError(f"missing FRET token after STR at index {next_idx}")
+        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
+        next_idx += 1
+    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
+        raise ValueError(f"FRET token must follow STR token at index {next_idx}")
+
+    return (
+        VoiceEvent(
+            voice=voice,
+            duration_ticks=duration_ticks,
+            mel_int=mel_int,
+            harm_oct=harm_oct,
+            harm_class=harm_class,
+            dup_count=dup_count,
+            string=string,
+            fret=fret,
+        ),
+        next_idx,
+    )
+
+
+def serialize_voice_event(event: VoiceEvent) -> List[str]:
+    tokens = [f"VOICE_{event.voice}"]
+    if event.is_rest:
+        tokens.append(f"REST_{event.rest_ticks}")
+        return tokens
+
+    tokens.append(f"DUR_{event.duration_ticks}")
+    if event.dup_count is not None:
+        tokens.append(f"DUP_{event.dup_count}")
+
+    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
+    if event.harm_oct is None:
+        tokens.append("HARM_OCT_NA")
+        tokens.append("HARM_CLASS_NA")
+    else:
+        tokens.append(f"HARM_OCT_{event.harm_oct}")
+        tokens.append(f"HARM_CLASS_{event.harm_class}")
+
+    if event.string is not None:
+        tokens.append(f"STR_{event.string}")
+        tokens.append(f"FRET_{event.fret}")
+
+    return tokens
+
+
+def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
+    parsed: List[Union[str, VoiceEvent]] = []
+    idx = 0
+    while idx < len(tokens):
+        tok = tokens[idx]
+        if tok.startswith("VOICE_"):
+            event, idx = parse_voice_event(tokens, idx)
+            parsed.append(event)
+            continue
+        parsed.append(tok)
+        idx += 1
+    return parsed
+
+
+def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
+    tokens: List[str] = []
+    for item in items:
+        if isinstance(item, VoiceEvent):
+            tokens.extend(serialize_voice_event(item))
+        else:
+            tokens.append(item)
+    return tokens
+
+
+def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
+    return serialize_event_stream(parse_event_stream(tokens))
+
+
+def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
+    if not token.startswith(prefix):
+        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
+    value = token[len(prefix) :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc
+
+
+def _parse_signed_mel(token: str, idx: int) -> int:
+    if not token.startswith("MEL_INT12_"):
+        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
+    value = token[len("MEL_INT12_") :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_OCT_"):
+        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
+    value = token[len("HARM_OCT_") :]
+    if value == "NA":
+        return None
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_CLASS_"):
+        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
+    value = token[len("HARM_CLASS_") :]
+    if value == "NA":
+        return None
+    try:
+        parsed = int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
+    if not (0 <= parsed <= 11):
+        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
+    return parsed
diff --git a/tests/test_tokenizer.py b/tests/test_tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..4f4741f7ebc6a851a695b5f6081e10cfc2c03f03
--- /dev/null
+++ b/tests/test_tokenizer.py
@@ -0,0 +1,107 @@
+import sys
+from pathlib import Path
+
+import pytest
+
+sys.path.insert(0, str(Path(__file__).parents[1]))
+
+from src.tokens.tokenizer import (
+    VoiceEvent,
+    canonicalize_event_stream,
+    parse_event_stream,
+    parse_voice_event,
+    serialize_event_stream,
+    serialize_voice_event,
+    tokenize_event_text,
+)
+
+
+def test_parse_pitched_voice_event_with_dup_and_tab():
+    tokens = [
+        "VOICE_2",
+        "DUR_12",
+        "DUP_3",
+        "MEL_INT12_+7",
+        "HARM_OCT_1",
+        "HARM_CLASS_7",
+        "STR_4",
+        "FRET_2",
+        "POS_6",
+    ]
+
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 8
+    assert event.voice == 2
+    assert event.duration_ticks == 12
+    assert event.dup_count == 3
+    assert event.mel_int == 7
+    assert event.harm_oct == 1
+    assert event.harm_class == 7
+    assert event.string == 4
+    assert event.fret == 2
+
+
+def test_parse_rest_voice_event():
+    tokens = ["VOICE_1", "REST_18", "BAR"]
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 2
+    assert event.voice == 1
+    assert event.is_rest
+    assert event.rest_ticks == 18
+
+
+def test_serialize_voice_event_uses_canonical_order():
+    event = VoiceEvent(
+        voice=3,
+        duration_ticks=24,
+        dup_count=2,
+        mel_int=11,
+        harm_oct=0,
+        harm_class=11,
+    )
+    assert serialize_voice_event(event) == [
+        "VOICE_3",
+        "DUR_24",
+        "DUP_2",
+        "MEL_INT12_+11",
+        "HARM_OCT_0",
+        "HARM_CLASS_11",
+    ]
+
+
+def test_canonicalize_event_stream_normalizes_mel_sign():
+    tokens = [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+    assert canonicalize_event_stream(tokens) == [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_+2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+
+
+def test_parse_event_stream_roundtrip_and_text_tokenization():
+    text = "BAR, POS_0, VOICE_0, DUR_4, MEL_INT12_0, HARM_OCT_NA, HARM_CLASS_NA"
+    tokens = tokenize_event_text(text)
+    parsed = parse_event_stream(tokens)
+    assert serialize_event_stream(parsed) == tokens
+
+
+def test_parse_rejects_mixed_harm_na_state():
+    tokens = ["VOICE_0", "DUR_8", "MEL_INT12_0", "HARM_OCT_NA", "HARM_CLASS_2"]
+    with pytest.raises(ValueError, match="harm_oct and harm_class must both be set or both be None"):
+        parse_voice_event(tokens, 0)

file update
M /home/stephen-reilly/dev/bach_gen/TODO.md
@@ -5,4 +5,5 @@
 - [x] Implement tokenizer interval math + round-trip tests
+- [x] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering
 - [ ] Implement `scripts/make_dataset.py` basic pipeline
 - [ ] Produce `data/processed/{events.parquet,barplans.parquet,stats.json}`
-- [ ] CI: `pytest -q` passes
+- [x] CI: `pytest -q` passes
apply_patch(auto_approved=true) exited 0 in 20ms:
Success. Updated the following files:
M TODO.md
file update:
diff --git a/TODO.md b/TODO.md
index da217dc3c318707990396dec3407998602311621..f34ae3002cbb8b57163085ffe73aebe53b830893
--- a/TODO.md
+++ b/TODO.md
@@ -3,6 +3,7 @@
 ## Milestone: Stage-1 Tokenizer & Dataset
 - [x] Implement `src/tokens/schema.py` (EventSpec + DescriptorSpec, versioned)
 - [x] Implement tokenizer interval math + round-trip tests
+- [x] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering
 - [ ] Implement `scripts/make_dataset.py` basic pipeline
 - [ ] Produce `data/processed/{events.parquet,barplans.parquet,stats.json}`
-- [ ] CI: `pytest -q` passes
+- [x] CI: `pytest -q` passes
diff --git a/docs/exec/EXECPLAN_stage1.md b/docs/exec/EXECPLAN_stage1.md
index 7f2647e15e5b673df54c0e21549951d68753a8c6..b8ee585fca521d0ea892fa6b21d13bb879078f5d
--- a/docs/exec/EXECPLAN_stage1.md
+++ b/docs/exec/EXECPLAN_stage1.md
@@ -11,7 +11,7 @@
 ## Checklist (one item per loop)
 - [x] Define EventSpec + DescriptorSpec in `src/tokens/schema.py` with version `remi_tab_v1` and `tpq=24`.
 - [x] Implement interval math helpers (melodic + harmonic ref) with QA vs production behavior.
-- [ ] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering.
+- [x] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering.
 - [ ] Add round-trip tests for pitch reconstruction and `HARM_*` consistency.
 - [ ] Implement `scripts/make_dataset.py` basic pipeline (parse, normalize, collapse doublings, track assignment, tokenize, stats).
 - [ ] Produce dataset artifacts in `data/processed/` and document counts in `stats.json`.
diff --git a/src/tokens/eventizer.py b/src/tokens/eventizer.py
index f7c666b524c3f03edbf62558e3a89e26917d8d43..53385718babf181fdc8367c1ca227731a1c1b799
--- a/src/tokens/eventizer.py
+++ b/src/tokens/eventizer.py
@@ -12,9 +12,9 @@
     IntervalRepairStats,
     compute_reference_pitch,
     compute_melodic_interval,
-    format_signed_interval,
     harmonic_tokens_for_pitch,
 )
+from src.tokens.tokenizer import VoiceEvent, serialize_voice_event
 
 DEFAULT_MAX_VOICES = 8
 DEFAULT_MAX_OCTAVES_PER_PITCH_CLASS = 3
@@ -584,15 +584,12 @@
                         tokens.append(f"ABS_VOICE_{v}_{ev.pitch}")
                     prev_pitch[v] = ev.pitch
 
-                tokens.append(f"VOICE_{v}")
-                tokens.append(f"DUR_{ev.duration_tick}")
                 base_pitch = prev_pitch[v] if prev_pitch[v] is not None else ev.pitch
                 mel_int = compute_melodic_interval(
                     ev.pitch,
                     base_pitch,
                     qa_mode=True,
                 )
-                tokens.append(f"MEL_INT12_{format_signed_interval(mel_int)}")
 
                 harm_oct_tok, harm_cls_tok = harmonic_tokens_for_pitch(
                     ev.pitch,
@@ -600,8 +597,21 @@
                     qa_mode=qa_mode,
                     stats=interval_repairs,
                 )
-                tokens.append(harm_oct_tok)
-                tokens.append(harm_cls_tok)
+                harm_oct = None if harm_oct_tok.endswith("_NA") else int(harm_oct_tok.rsplit("_", 1)[1])
+                harm_class = (
+                    None if harm_cls_tok.endswith("_NA") else int(harm_cls_tok.rsplit("_", 1)[1])
+                )
+                tokens.extend(
+                    serialize_voice_event(
+                        VoiceEvent(
+                            voice=v,
+                            duration_ticks=ev.duration_tick,
+                            mel_int=mel_int,
+                            harm_oct=harm_oct,
+                            harm_class=harm_class,
+                        )
+                    )
+                )
 
                 prev_pitch[v] = ev.pitch
                 active_until[v] = t + ev.duration_tick
diff --git a/src/tokens/tokenizer.py b/src/tokens/tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..3d7839be70301fe3c5376ba858263c8c4f66da1b
--- /dev/null
+++ b/src/tokens/tokenizer.py
@@ -0,0 +1,219 @@
+from dataclasses import dataclass
+from typing import List, Sequence, Tuple, Union
+
+from src.tokens.intervals import format_signed_interval
+
+
+@dataclass(frozen=True)
+class VoiceEvent:
+    voice: int
+    duration_ticks: int = 0
+    rest_ticks: int = 0
+    mel_int: int = 0
+    harm_oct: Union[int, None] = None
+    harm_class: Union[int, None] = None
+    dup_count: Union[int, None] = None
+    string: Union[int, None] = None
+    fret: Union[int, None] = None
+
+    def __post_init__(self) -> None:
+        if self.voice < 0:
+            raise ValueError("voice must be >= 0")
+        if self.duration_ticks < 0:
+            raise ValueError("duration_ticks must be >= 0")
+        if self.rest_ticks < 0:
+            raise ValueError("rest_ticks must be >= 0")
+        if self.dup_count is not None and self.dup_count < 1:
+            raise ValueError("dup_count must be >= 1 when set")
+        if (self.string is None) ^ (self.fret is None):
+            raise ValueError("string and fret must both be set or both be None")
+
+        is_rest = self.rest_ticks > 0
+        is_pitched = self.duration_ticks > 0
+        if is_rest == is_pitched:
+            raise ValueError("voice event must be exactly one of rest or pitched")
+
+        if is_rest:
+            if self.dup_count is not None:
+                raise ValueError("rest event cannot set dup_count")
+            if self.harm_oct is not None or self.harm_class is not None:
+                raise ValueError("rest event cannot set harmonic interval fields")
+            if self.string is not None or self.fret is not None:
+                raise ValueError("rest event cannot set tab fields")
+            return
+
+        if (self.harm_oct is None) ^ (self.harm_class is None):
+            raise ValueError("harm_oct and harm_class must both be set or both be None")
+        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
+            raise ValueError("harm_class must be in [0, 11]")
+
+    @property
+    def is_rest(self) -> bool:
+        return self.rest_ticks > 0
+
+
+def tokenize_event_text(text: str) -> List[str]:
+    raw = text.replace("\n", ",")
+    tokens = [tok.strip() for tok in raw.split(",")]
+    return [tok for tok in tokens if tok]
+
+
+def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
+    if start_idx >= len(tokens):
+        raise ValueError("start_idx out of range")
+
+    voice_tok = tokens[start_idx]
+    if not voice_tok.startswith("VOICE_"):
+        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")
+
+    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
+    next_idx = start_idx + 1
+    if next_idx >= len(tokens):
+        raise ValueError(f"truncated VOICE event at index {start_idx}")
+
+    first_tok = tokens[next_idx]
+    if first_tok.startswith("REST_"):
+        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
+        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1
+
+    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
+    next_idx += 1
+
+    dup_count = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
+        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
+        next_idx += 1
+
+    if next_idx + 2 >= len(tokens):
+        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")
+
+    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
+    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
+    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
+    next_idx += 3
+
+    string = None
+    fret = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
+        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
+        next_idx += 1
+        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
+            raise ValueError(f"missing FRET token after STR at index {next_idx}")
+        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
+        next_idx += 1
+    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
+        raise ValueError(f"FRET token must follow STR token at index {next_idx}")
+
+    return (
+        VoiceEvent(
+            voice=voice,
+            duration_ticks=duration_ticks,
+            mel_int=mel_int,
+            harm_oct=harm_oct,
+            harm_class=harm_class,
+            dup_count=dup_count,
+            string=string,
+            fret=fret,
+        ),
+        next_idx,
+    )
+
+
+def serialize_voice_event(event: VoiceEvent) -> List[str]:
+    tokens = [f"VOICE_{event.voice}"]
+    if event.is_rest:
+        tokens.append(f"REST_{event.rest_ticks}")
+        return tokens
+
+    tokens.append(f"DUR_{event.duration_ticks}")
+    if event.dup_count is not None:
+        tokens.append(f"DUP_{event.dup_count}")
+
+    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
+    if event.harm_oct is None:
+        tokens.append("HARM_OCT_NA")
+        tokens.append("HARM_CLASS_NA")
+    else:
+        tokens.append(f"HARM_OCT_{event.harm_oct}")
+        tokens.append(f"HARM_CLASS_{event.harm_class}")
+
+    if event.string is not None:
+        tokens.append(f"STR_{event.string}")
+        tokens.append(f"FRET_{event.fret}")
+
+    return tokens
+
+
+def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
+    parsed: List[Union[str, VoiceEvent]] = []
+    idx = 0
+    while idx < len(tokens):
+        tok = tokens[idx]
+        if tok.startswith("VOICE_"):
+            event, idx = parse_voice_event(tokens, idx)
+            parsed.append(event)
+            continue
+        parsed.append(tok)
+        idx += 1
+    return parsed
+
+
+def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
+    tokens: List[str] = []
+    for item in items:
+        if isinstance(item, VoiceEvent):
+            tokens.extend(serialize_voice_event(item))
+        else:
+            tokens.append(item)
+    return tokens
+
+
+def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
+    return serialize_event_stream(parse_event_stream(tokens))
+
+
+def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
+    if not token.startswith(prefix):
+        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
+    value = token[len(prefix) :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc
+
+
+def _parse_signed_mel(token: str, idx: int) -> int:
+    if not token.startswith("MEL_INT12_"):
+        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
+    value = token[len("MEL_INT12_") :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_OCT_"):
+        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
+    value = token[len("HARM_OCT_") :]
+    if value == "NA":
+        return None
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_CLASS_"):
+        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
+    value = token[len("HARM_CLASS_") :]
+    if value == "NA":
+        return None
+    try:
+        parsed = int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
+    if not (0 <= parsed <= 11):
+        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
+    return parsed
diff --git a/tests/test_tokenizer.py b/tests/test_tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..4f4741f7ebc6a851a695b5f6081e10cfc2c03f03
--- /dev/null
+++ b/tests/test_tokenizer.py
@@ -0,0 +1,107 @@
+import sys
+from pathlib import Path
+
+import pytest
+
+sys.path.insert(0, str(Path(__file__).parents[1]))
+
+from src.tokens.tokenizer import (
+    VoiceEvent,
+    canonicalize_event_stream,
+    parse_event_stream,
+    parse_voice_event,
+    serialize_event_stream,
+    serialize_voice_event,
+    tokenize_event_text,
+)
+
+
+def test_parse_pitched_voice_event_with_dup_and_tab():
+    tokens = [
+        "VOICE_2",
+        "DUR_12",
+        "DUP_3",
+        "MEL_INT12_+7",
+        "HARM_OCT_1",
+        "HARM_CLASS_7",
+        "STR_4",
+        "FRET_2",
+        "POS_6",
+    ]
+
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 8
+    assert event.voice == 2
+    assert event.duration_ticks == 12
+    assert event.dup_count == 3
+    assert event.mel_int == 7
+    assert event.harm_oct == 1
+    assert event.harm_class == 7
+    assert event.string == 4
+    assert event.fret == 2
+
+
+def test_parse_rest_voice_event():
+    tokens = ["VOICE_1", "REST_18", "BAR"]
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 2
+    assert event.voice == 1
+    assert event.is_rest
+    assert event.rest_ticks == 18
+
+
+def test_serialize_voice_event_uses_canonical_order():
+    event = VoiceEvent(
+        voice=3,
+        duration_ticks=24,
+        dup_count=2,
+        mel_int=11,
+        harm_oct=0,
+        harm_class=11,
+    )
+    assert serialize_voice_event(event) == [
+        "VOICE_3",
+        "DUR_24",
+        "DUP_2",
+        "MEL_INT12_+11",
+        "HARM_OCT_0",
+        "HARM_CLASS_11",
+    ]
+
+
+def test_canonicalize_event_stream_normalizes_mel_sign():
+    tokens = [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+    assert canonicalize_event_stream(tokens) == [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_+2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+
+
+def test_parse_event_stream_roundtrip_and_text_tokenization():
+    text = "BAR, POS_0, VOICE_0, DUR_4, MEL_INT12_0, HARM_OCT_NA, HARM_CLASS_NA"
+    tokens = tokenize_event_text(text)
+    parsed = parse_event_stream(tokens)
+    assert serialize_event_stream(parsed) == tokens
+
+
+def test_parse_rejects_mixed_harm_na_state():
+    tokens = ["VOICE_0", "DUR_8", "MEL_INT12_0", "HARM_OCT_NA", "HARM_CLASS_2"]
+    with pytest.raises(ValueError, match="harm_oct and harm_class must both be set or both be None"):
+        parse_voice_event(tokens, 0)

file update:
diff --git a/TODO.md b/TODO.md
index da217dc3c318707990396dec3407998602311621..f34ae3002cbb8b57163085ffe73aebe53b830893
--- a/TODO.md
+++ b/TODO.md
@@ -3,6 +3,7 @@
 ## Milestone: Stage-1 Tokenizer & Dataset
 - [x] Implement `src/tokens/schema.py` (EventSpec + DescriptorSpec, versioned)
 - [x] Implement tokenizer interval math + round-trip tests
+- [x] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering
 - [ ] Implement `scripts/make_dataset.py` basic pipeline
 - [ ] Produce `data/processed/{events.parquet,barplans.parquet,stats.json}`
-- [ ] CI: `pytest -q` passes
+- [x] CI: `pytest -q` passes
diff --git a/docs/exec/EXECPLAN_stage1.md b/docs/exec/EXECPLAN_stage1.md
index 7f2647e15e5b673df54c0e21549951d68753a8c6..b8ee585fca521d0ea892fa6b21d13bb879078f5d
--- a/docs/exec/EXECPLAN_stage1.md
+++ b/docs/exec/EXECPLAN_stage1.md
@@ -11,7 +11,7 @@
 ## Checklist (one item per loop)
 - [x] Define EventSpec + DescriptorSpec in `src/tokens/schema.py` with version `remi_tab_v1` and `tpq=24`.
 - [x] Implement interval math helpers (melodic + harmonic ref) with QA vs production behavior.
-- [ ] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering.
+- [x] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering.
 - [ ] Add round-trip tests for pitch reconstruction and `HARM_*` consistency.
 - [ ] Implement `scripts/make_dataset.py` basic pipeline (parse, normalize, collapse doublings, track assignment, tokenize, stats).
 - [ ] Produce dataset artifacts in `data/processed/` and document counts in `stats.json`.
diff --git a/src/tokens/eventizer.py b/src/tokens/eventizer.py
index f7c666b524c3f03edbf62558e3a89e26917d8d43..53385718babf181fdc8367c1ca227731a1c1b799
--- a/src/tokens/eventizer.py
+++ b/src/tokens/eventizer.py
@@ -12,9 +12,9 @@
     IntervalRepairStats,
     compute_reference_pitch,
     compute_melodic_interval,
-    format_signed_interval,
     harmonic_tokens_for_pitch,
 )
+from src.tokens.tokenizer import VoiceEvent, serialize_voice_event
 
 DEFAULT_MAX_VOICES = 8
 DEFAULT_MAX_OCTAVES_PER_PITCH_CLASS = 3
@@ -584,15 +584,12 @@
                         tokens.append(f"ABS_VOICE_{v}_{ev.pitch}")
                     prev_pitch[v] = ev.pitch
 
-                tokens.append(f"VOICE_{v}")
-                tokens.append(f"DUR_{ev.duration_tick}")
                 base_pitch = prev_pitch[v] if prev_pitch[v] is not None else ev.pitch
                 mel_int = compute_melodic_interval(
                     ev.pitch,
                     base_pitch,
                     qa_mode=True,
                 )
-                tokens.append(f"MEL_INT12_{format_signed_interval(mel_int)}")
 
                 harm_oct_tok, harm_cls_tok = harmonic_tokens_for_pitch(
                     ev.pitch,
@@ -600,8 +597,21 @@
                     qa_mode=qa_mode,
                     stats=interval_repairs,
                 )
-                tokens.append(harm_oct_tok)
-                tokens.append(harm_cls_tok)
+                harm_oct = None if harm_oct_tok.endswith("_NA") else int(harm_oct_tok.rsplit("_", 1)[1])
+                harm_class = (
+                    None if harm_cls_tok.endswith("_NA") else int(harm_cls_tok.rsplit("_", 1)[1])
+                )
+                tokens.extend(
+                    serialize_voice_event(
+                        VoiceEvent(
+                            voice=v,
+                            duration_ticks=ev.duration_tick,
+                            mel_int=mel_int,
+                            harm_oct=harm_oct,
+                            harm_class=harm_class,
+                        )
+                    )
+                )
 
                 prev_pitch[v] = ev.pitch
                 active_until[v] = t + ev.duration_tick
diff --git a/src/tokens/tokenizer.py b/src/tokens/tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..3d7839be70301fe3c5376ba858263c8c4f66da1b
--- /dev/null
+++ b/src/tokens/tokenizer.py
@@ -0,0 +1,219 @@
+from dataclasses import dataclass
+from typing import List, Sequence, Tuple, Union
+
+from src.tokens.intervals import format_signed_interval
+
+
+@dataclass(frozen=True)
+class VoiceEvent:
+    voice: int
+    duration_ticks: int = 0
+    rest_ticks: int = 0
+    mel_int: int = 0
+    harm_oct: Union[int, None] = None
+    harm_class: Union[int, None] = None
+    dup_count: Union[int, None] = None
+    string: Union[int, None] = None
+    fret: Union[int, None] = None
+
+    def __post_init__(self) -> None:
+        if self.voice < 0:
+            raise ValueError("voice must be >= 0")
+        if self.duration_ticks < 0:
+            raise ValueError("duration_ticks must be >= 0")
+        if self.rest_ticks < 0:
+            raise ValueError("rest_ticks must be >= 0")
+        if self.dup_count is not None and self.dup_count < 1:
+            raise ValueError("dup_count must be >= 1 when set")
+        if (self.string is None) ^ (self.fret is None):
+            raise ValueError("string and fret must both be set or both be None")
+
+        is_rest = self.rest_ticks > 0
+        is_pitched = self.duration_ticks > 0
+        if is_rest == is_pitched:
+            raise ValueError("voice event must be exactly one of rest or pitched")
+
+        if is_rest:
+            if self.dup_count is not None:
+                raise ValueError("rest event cannot set dup_count")
+            if self.harm_oct is not None or self.harm_class is not None:
+                raise ValueError("rest event cannot set harmonic interval fields")
+            if self.string is not None or self.fret is not None:
+                raise ValueError("rest event cannot set tab fields")
+            return
+
+        if (self.harm_oct is None) ^ (self.harm_class is None):
+            raise ValueError("harm_oct and harm_class must both be set or both be None")
+        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
+            raise ValueError("harm_class must be in [0, 11]")
+
+    @property
+    def is_rest(self) -> bool:
+        return self.rest_ticks > 0
+
+
+def tokenize_event_text(text: str) -> List[str]:
+    raw = text.replace("\n", ",")
+    tokens = [tok.strip() for tok in raw.split(",")]
+    return [tok for tok in tokens if tok]
+
+
+def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
+    if start_idx >= len(tokens):
+        raise ValueError("start_idx out of range")
+
+    voice_tok = tokens[start_idx]
+    if not voice_tok.startswith("VOICE_"):
+        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")
+
+    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
+    next_idx = start_idx + 1
+    if next_idx >= len(tokens):
+        raise ValueError(f"truncated VOICE event at index {start_idx}")
+
+    first_tok = tokens[next_idx]
+    if first_tok.startswith("REST_"):
+        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
+        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1
+
+    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
+    next_idx += 1
+
+    dup_count = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
+        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
+        next_idx += 1
+
+    if next_idx + 2 >= len(tokens):
+        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")
+
+    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
+    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
+    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
+    next_idx += 3
+
+    string = None
+    fret = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
+        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
+        next_idx += 1
+        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
+            raise ValueError(f"missing FRET token after STR at index {next_idx}")
+        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
+        next_idx += 1
+    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
+        raise ValueError(f"FRET token must follow STR token at index {next_idx}")
+
+    return (
+        VoiceEvent(
+            voice=voice,
+            duration_ticks=duration_ticks,
+            mel_int=mel_int,
+            harm_oct=harm_oct,
+            harm_class=harm_class,
+            dup_count=dup_count,
+            string=string,
+            fret=fret,
+        ),
+        next_idx,
+    )
+
+
+def serialize_voice_event(event: VoiceEvent) -> List[str]:
+    tokens = [f"VOICE_{event.voice}"]
+    if event.is_rest:
+        tokens.append(f"REST_{event.rest_ticks}")
+        return tokens
+
+    tokens.append(f"DUR_{event.duration_ticks}")
+    if event.dup_count is not None:
+        tokens.append(f"DUP_{event.dup_count}")
+
+    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
+    if event.harm_oct is None:
+        tokens.append("HARM_OCT_NA")
+        tokens.append("HARM_CLASS_NA")
+    else:
+        tokens.append(f"HARM_OCT_{event.harm_oct}")
+        tokens.append(f"HARM_CLASS_{event.harm_class}")
+
+    if event.string is not None:
+        tokens.append(f"STR_{event.string}")
+        tokens.append(f"FRET_{event.fret}")
+
+    return tokens
+
+
+def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
+    parsed: List[Union[str, VoiceEvent]] = []
+    idx = 0
+    while idx < len(tokens):
+        tok = tokens[idx]
+        if tok.startswith("VOICE_"):
+            event, idx = parse_voice_event(tokens, idx)
+            parsed.append(event)
+            continue
+        parsed.append(tok)
+        idx += 1
+    return parsed
+
+
+def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
+    tokens: List[str] = []
+    for item in items:
+        if isinstance(item, VoiceEvent):
+            tokens.extend(serialize_voice_event(item))
+        else:
+            tokens.append(item)
+    return tokens
+
+
+def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
+    return serialize_event_stream(parse_event_stream(tokens))
+
+
+def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
+    if not token.startswith(prefix):
+        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
+    value = token[len(prefix) :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc
+
+
+def _parse_signed_mel(token: str, idx: int) -> int:
+    if not token.startswith("MEL_INT12_"):
+        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
+    value = token[len("MEL_INT12_") :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_OCT_"):
+        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
+    value = token[len("HARM_OCT_") :]
+    if value == "NA":
+        return None
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_CLASS_"):
+        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
+    value = token[len("HARM_CLASS_") :]
+    if value == "NA":
+        return None
+    try:
+        parsed = int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
+    if not (0 <= parsed <= 11):
+        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
+    return parsed
diff --git a/tests/test_tokenizer.py b/tests/test_tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..4f4741f7ebc6a851a695b5f6081e10cfc2c03f03
--- /dev/null
+++ b/tests/test_tokenizer.py
@@ -0,0 +1,107 @@
+import sys
+from pathlib import Path
+
+import pytest
+
+sys.path.insert(0, str(Path(__file__).parents[1]))
+
+from src.tokens.tokenizer import (
+    VoiceEvent,
+    canonicalize_event_stream,
+    parse_event_stream,
+    parse_voice_event,
+    serialize_event_stream,
+    serialize_voice_event,
+    tokenize_event_text,
+)
+
+
+def test_parse_pitched_voice_event_with_dup_and_tab():
+    tokens = [
+        "VOICE_2",
+        "DUR_12",
+        "DUP_3",
+        "MEL_INT12_+7",
+        "HARM_OCT_1",
+        "HARM_CLASS_7",
+        "STR_4",
+        "FRET_2",
+        "POS_6",
+    ]
+
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 8
+    assert event.voice == 2
+    assert event.duration_ticks == 12
+    assert event.dup_count == 3
+    assert event.mel_int == 7
+    assert event.harm_oct == 1
+    assert event.harm_class == 7
+    assert event.string == 4
+    assert event.fret == 2
+
+
+def test_parse_rest_voice_event():
+    tokens = ["VOICE_1", "REST_18", "BAR"]
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 2
+    assert event.voice == 1
+    assert event.is_rest
+    assert event.rest_ticks == 18
+
+
+def test_serialize_voice_event_uses_canonical_order():
+    event = VoiceEvent(
+        voice=3,
+        duration_ticks=24,
+        dup_count=2,
+        mel_int=11,
+        harm_oct=0,
+        harm_class=11,
+    )
+    assert serialize_voice_event(event) == [
+        "VOICE_3",
+        "DUR_24",
+        "DUP_2",
+        "MEL_INT12_+11",
+        "HARM_OCT_0",
+        "HARM_CLASS_11",
+    ]
+
+
+def test_canonicalize_event_stream_normalizes_mel_sign():
+    tokens = [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+    assert canonicalize_event_stream(tokens) == [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_+2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+
+
+def test_parse_event_stream_roundtrip_and_text_tokenization():
+    text = "BAR, POS_0, VOICE_0, DUR_4, MEL_INT12_0, HARM_OCT_NA, HARM_CLASS_NA"
+    tokens = tokenize_event_text(text)
+    parsed = parse_event_stream(tokens)
+    assert serialize_event_stream(parsed) == tokens
+
+
+def test_parse_rejects_mixed_harm_na_state():
+    tokens = ["VOICE_0", "DUR_8", "MEL_INT12_0", "HARM_OCT_NA", "HARM_CLASS_2"]
+    with pytest.raises(ValueError, match="harm_oct and harm_class must both be set or both be None"):
+        parse_voice_event(tokens, 0)


thinking
**Appending dated test results**
file update
M /home/stephen-reilly/dev/bach_gen/PROGRESS.md
@@ -9 +9,2 @@
 - 2026-02-24: Completed Stage-1 ExecPlan item 2 by adding `src/tokens/intervals.py` (melodic/harmonic helpers with QA vs production behavior), wiring repairs into `eventize_musicxml`, updating harmonic validation to shared math helpers, and adding `tests/test_intervals.py`; verify run: `53 passed, 1 skipped`.
+- 2026-02-24: Completed Stage-1 ExecPlan item 3 by adding `src/tokens/tokenizer.py` (voice-event parsing/serialization, stream canonicalization helpers), wiring `eventize_musicxml` pitched-onset emission through tokenizer serialization for canonical ordering, and adding `tests/test_tokenizer.py`; verify run: `59 passed, 1 skipped`.
apply_patch(auto_approved=true) exited 0 in 27ms:
Success. Updated the following files:
M PROGRESS.md
file update:
diff --git a/PROGRESS.md b/PROGRESS.md
index 551ac82b1a8e3c568478d8e3046e4875e6badff8..11f763e2dbb4256baeb84ecbe0f89f7a55bf2222
--- a/PROGRESS.md
+++ b/PROGRESS.md
@@ -7,3 +7,4 @@
 - 2026-02-24: Updated loop to use `--full-auto` on newer Codex CLI builds where explicit approval flags are unavailable.
 - 2026-02-24: Completed Stage-1 ExecPlan item 1 by defining versioned `EventSpec`/`DescriptorSpec` in `src/tokens/schema.py` (`remi_tab_v1`, `tpq=24`) and added `tests/test_schema.py`.
 - 2026-02-24: Completed Stage-1 ExecPlan item 2 by adding `src/tokens/intervals.py` (melodic/harmonic helpers with QA vs production behavior), wiring repairs into `eventize_musicxml`, updating harmonic validation to shared math helpers, and adding `tests/test_intervals.py`; verify run: `53 passed, 1 skipped`.
+- 2026-02-24: Completed Stage-1 ExecPlan item 3 by adding `src/tokens/tokenizer.py` (voice-event parsing/serialization, stream canonicalization helpers), wiring `eventize_musicxml` pitched-onset emission through tokenizer serialization for canonical ordering, and adding `tests/test_tokenizer.py`; verify run: `59 passed, 1 skipped`.
diff --git a/TODO.md b/TODO.md
index da217dc3c318707990396dec3407998602311621..f34ae3002cbb8b57163085ffe73aebe53b830893
--- a/TODO.md
+++ b/TODO.md
@@ -3,6 +3,7 @@
 ## Milestone: Stage-1 Tokenizer & Dataset
 - [x] Implement `src/tokens/schema.py` (EventSpec + DescriptorSpec, versioned)
 - [x] Implement tokenizer interval math + round-trip tests
+- [x] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering
 - [ ] Implement `scripts/make_dataset.py` basic pipeline
 - [ ] Produce `data/processed/{events.parquet,barplans.parquet,stats.json}`
-- [ ] CI: `pytest -q` passes
+- [x] CI: `pytest -q` passes
diff --git a/docs/exec/EXECPLAN_stage1.md b/docs/exec/EXECPLAN_stage1.md
index 7f2647e15e5b673df54c0e21549951d68753a8c6..b8ee585fca521d0ea892fa6b21d13bb879078f5d
--- a/docs/exec/EXECPLAN_stage1.md
+++ b/docs/exec/EXECPLAN_stage1.md
@@ -11,7 +11,7 @@
 ## Checklist (one item per loop)
 - [x] Define EventSpec + DescriptorSpec in `src/tokens/schema.py` with version `remi_tab_v1` and `tpq=24`.
 - [x] Implement interval math helpers (melodic + harmonic ref) with QA vs production behavior.
-- [ ] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering.
+- [x] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering.
 - [ ] Add round-trip tests for pitch reconstruction and `HARM_*` consistency.
 - [ ] Implement `scripts/make_dataset.py` basic pipeline (parse, normalize, collapse doublings, track assignment, tokenize, stats).
 - [ ] Produce dataset artifacts in `data/processed/` and document counts in `stats.json`.
diff --git a/src/tokens/eventizer.py b/src/tokens/eventizer.py
index f7c666b524c3f03edbf62558e3a89e26917d8d43..53385718babf181fdc8367c1ca227731a1c1b799
--- a/src/tokens/eventizer.py
+++ b/src/tokens/eventizer.py
@@ -12,9 +12,9 @@
     IntervalRepairStats,
     compute_reference_pitch,
     compute_melodic_interval,
-    format_signed_interval,
     harmonic_tokens_for_pitch,
 )
+from src.tokens.tokenizer import VoiceEvent, serialize_voice_event
 
 DEFAULT_MAX_VOICES = 8
 DEFAULT_MAX_OCTAVES_PER_PITCH_CLASS = 3
@@ -584,15 +584,12 @@
                         tokens.append(f"ABS_VOICE_{v}_{ev.pitch}")
                     prev_pitch[v] = ev.pitch
 
-                tokens.append(f"VOICE_{v}")
-                tokens.append(f"DUR_{ev.duration_tick}")
                 base_pitch = prev_pitch[v] if prev_pitch[v] is not None else ev.pitch
                 mel_int = compute_melodic_interval(
                     ev.pitch,
                     base_pitch,
                     qa_mode=True,
                 )
-                tokens.append(f"MEL_INT12_{format_signed_interval(mel_int)}")
 
                 harm_oct_tok, harm_cls_tok = harmonic_tokens_for_pitch(
                     ev.pitch,
@@ -600,8 +597,21 @@
                     qa_mode=qa_mode,
                     stats=interval_repairs,
                 )
-                tokens.append(harm_oct_tok)
-                tokens.append(harm_cls_tok)
+                harm_oct = None if harm_oct_tok.endswith("_NA") else int(harm_oct_tok.rsplit("_", 1)[1])
+                harm_class = (
+                    None if harm_cls_tok.endswith("_NA") else int(harm_cls_tok.rsplit("_", 1)[1])
+                )
+                tokens.extend(
+                    serialize_voice_event(
+                        VoiceEvent(
+                            voice=v,
+                            duration_ticks=ev.duration_tick,
+                            mel_int=mel_int,
+                            harm_oct=harm_oct,
+                            harm_class=harm_class,
+                        )
+                    )
+                )
 
                 prev_pitch[v] = ev.pitch
                 active_until[v] = t + ev.duration_tick
diff --git a/src/tokens/tokenizer.py b/src/tokens/tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..3d7839be70301fe3c5376ba858263c8c4f66da1b
--- /dev/null
+++ b/src/tokens/tokenizer.py
@@ -0,0 +1,219 @@
+from dataclasses import dataclass
+from typing import List, Sequence, Tuple, Union
+
+from src.tokens.intervals import format_signed_interval
+
+
+@dataclass(frozen=True)
+class VoiceEvent:
+    voice: int
+    duration_ticks: int = 0
+    rest_ticks: int = 0
+    mel_int: int = 0
+    harm_oct: Union[int, None] = None
+    harm_class: Union[int, None] = None
+    dup_count: Union[int, None] = None
+    string: Union[int, None] = None
+    fret: Union[int, None] = None
+
+    def __post_init__(self) -> None:
+        if self.voice < 0:
+            raise ValueError("voice must be >= 0")
+        if self.duration_ticks < 0:
+            raise ValueError("duration_ticks must be >= 0")
+        if self.rest_ticks < 0:
+            raise ValueError("rest_ticks must be >= 0")
+        if self.dup_count is not None and self.dup_count < 1:
+            raise ValueError("dup_count must be >= 1 when set")
+        if (self.string is None) ^ (self.fret is None):
+            raise ValueError("string and fret must both be set or both be None")
+
+        is_rest = self.rest_ticks > 0
+        is_pitched = self.duration_ticks > 0
+        if is_rest == is_pitched:
+            raise ValueError("voice event must be exactly one of rest or pitched")
+
+        if is_rest:
+            if self.dup_count is not None:
+                raise ValueError("rest event cannot set dup_count")
+            if self.harm_oct is not None or self.harm_class is not None:
+                raise ValueError("rest event cannot set harmonic interval fields")
+            if self.string is not None or self.fret is not None:
+                raise ValueError("rest event cannot set tab fields")
+            return
+
+        if (self.harm_oct is None) ^ (self.harm_class is None):
+            raise ValueError("harm_oct and harm_class must both be set or both be None")
+        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
+            raise ValueError("harm_class must be in [0, 11]")
+
+    @property
+    def is_rest(self) -> bool:
+        return self.rest_ticks > 0
+
+
+def tokenize_event_text(text: str) -> List[str]:
+    raw = text.replace("\n", ",")
+    tokens = [tok.strip() for tok in raw.split(",")]
+    return [tok for tok in tokens if tok]
+
+
+def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
+    if start_idx >= len(tokens):
+        raise ValueError("start_idx out of range")
+
+    voice_tok = tokens[start_idx]
+    if not voice_tok.startswith("VOICE_"):
+        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")
+
+    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
+    next_idx = start_idx + 1
+    if next_idx >= len(tokens):
+        raise ValueError(f"truncated VOICE event at index {start_idx}")
+
+    first_tok = tokens[next_idx]
+    if first_tok.startswith("REST_"):
+        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
+        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1
+
+    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
+    next_idx += 1
+
+    dup_count = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
+        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
+        next_idx += 1
+
+    if next_idx + 2 >= len(tokens):
+        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")
+
+    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
+    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
+    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
+    next_idx += 3
+
+    string = None
+    fret = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
+        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
+        next_idx += 1
+        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
+            raise ValueError(f"missing FRET token after STR at index {next_idx}")
+        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
+        next_idx += 1
+    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
+        raise ValueError(f"FRET token must follow STR token at index {next_idx}")
+
+    return (
+        VoiceEvent(
+            voice=voice,
+            duration_ticks=duration_ticks,
+            mel_int=mel_int,
+            harm_oct=harm_oct,
+            harm_class=harm_class,
+            dup_count=dup_count,
+            string=string,
+            fret=fret,
+        ),
+        next_idx,
+    )
+
+
+def serialize_voice_event(event: VoiceEvent) -> List[str]:
+    tokens = [f"VOICE_{event.voice}"]
+    if event.is_rest:
+        tokens.append(f"REST_{event.rest_ticks}")
+        return tokens
+
+    tokens.append(f"DUR_{event.duration_ticks}")
+    if event.dup_count is not None:
+        tokens.append(f"DUP_{event.dup_count}")
+
+    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
+    if event.harm_oct is None:
+        tokens.append("HARM_OCT_NA")
+        tokens.append("HARM_CLASS_NA")
+    else:
+        tokens.append(f"HARM_OCT_{event.harm_oct}")
+        tokens.append(f"HARM_CLASS_{event.harm_class}")
+
+    if event.string is not None:
+        tokens.append(f"STR_{event.string}")
+        tokens.append(f"FRET_{event.fret}")
+
+    return tokens
+
+
+def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
+    parsed: List[Union[str, VoiceEvent]] = []
+    idx = 0
+    while idx < len(tokens):
+        tok = tokens[idx]
+        if tok.startswith("VOICE_"):
+            event, idx = parse_voice_event(tokens, idx)
+            parsed.append(event)
+            continue
+        parsed.append(tok)
+        idx += 1
+    return parsed
+
+
+def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
+    tokens: List[str] = []
+    for item in items:
+        if isinstance(item, VoiceEvent):
+            tokens.extend(serialize_voice_event(item))
+        else:
+            tokens.append(item)
+    return tokens
+
+
+def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
+    return serialize_event_stream(parse_event_stream(tokens))
+
+
+def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
+    if not token.startswith(prefix):
+        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
+    value = token[len(prefix) :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc
+
+
+def _parse_signed_mel(token: str, idx: int) -> int:
+    if not token.startswith("MEL_INT12_"):
+        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
+    value = token[len("MEL_INT12_") :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_OCT_"):
+        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
+    value = token[len("HARM_OCT_") :]
+    if value == "NA":
+        return None
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_CLASS_"):
+        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
+    value = token[len("HARM_CLASS_") :]
+    if value == "NA":
+        return None
+    try:
+        parsed = int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
+    if not (0 <= parsed <= 11):
+        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
+    return parsed
diff --git a/tests/test_tokenizer.py b/tests/test_tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..4f4741f7ebc6a851a695b5f6081e10cfc2c03f03
--- /dev/null
+++ b/tests/test_tokenizer.py
@@ -0,0 +1,107 @@
+import sys
+from pathlib import Path
+
+import pytest
+
+sys.path.insert(0, str(Path(__file__).parents[1]))
+
+from src.tokens.tokenizer import (
+    VoiceEvent,
+    canonicalize_event_stream,
+    parse_event_stream,
+    parse_voice_event,
+    serialize_event_stream,
+    serialize_voice_event,
+    tokenize_event_text,
+)
+
+
+def test_parse_pitched_voice_event_with_dup_and_tab():
+    tokens = [
+        "VOICE_2",
+        "DUR_12",
+        "DUP_3",
+        "MEL_INT12_+7",
+        "HARM_OCT_1",
+        "HARM_CLASS_7",
+        "STR_4",
+        "FRET_2",
+        "POS_6",
+    ]
+
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 8
+    assert event.voice == 2
+    assert event.duration_ticks == 12
+    assert event.dup_count == 3
+    assert event.mel_int == 7
+    assert event.harm_oct == 1
+    assert event.harm_class == 7
+    assert event.string == 4
+    assert event.fret == 2
+
+
+def test_parse_rest_voice_event():
+    tokens = ["VOICE_1", "REST_18", "BAR"]
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 2
+    assert event.voice == 1
+    assert event.is_rest
+    assert event.rest_ticks == 18
+
+
+def test_serialize_voice_event_uses_canonical_order():
+    event = VoiceEvent(
+        voice=3,
+        duration_ticks=24,
+        dup_count=2,
+        mel_int=11,
+        harm_oct=0,
+        harm_class=11,
+    )
+    assert serialize_voice_event(event) == [
+        "VOICE_3",
+        "DUR_24",
+        "DUP_2",
+        "MEL_INT12_+11",
+        "HARM_OCT_0",
+        "HARM_CLASS_11",
+    ]
+
+
+def test_canonicalize_event_stream_normalizes_mel_sign():
+    tokens = [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+    assert canonicalize_event_stream(tokens) == [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_+2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+
+
+def test_parse_event_stream_roundtrip_and_text_tokenization():
+    text = "BAR, POS_0, VOICE_0, DUR_4, MEL_INT12_0, HARM_OCT_NA, HARM_CLASS_NA"
+    tokens = tokenize_event_text(text)
+    parsed = parse_event_stream(tokens)
+    assert serialize_event_stream(parsed) == tokens
+
+
+def test_parse_rejects_mixed_harm_na_state():
+    tokens = ["VOICE_0", "DUR_8", "MEL_INT12_0", "HARM_OCT_NA", "HARM_CLASS_2"]
+    with pytest.raises(ValueError, match="harm_oct and harm_class must both be set or both be None"):
+        parse_voice_event(tokens, 0)

file update:
diff --git a/PROGRESS.md b/PROGRESS.md
index 551ac82b1a8e3c568478d8e3046e4875e6badff8..11f763e2dbb4256baeb84ecbe0f89f7a55bf2222
--- a/PROGRESS.md
+++ b/PROGRESS.md
@@ -7,3 +7,4 @@
 - 2026-02-24: Updated loop to use `--full-auto` on newer Codex CLI builds where explicit approval flags are unavailable.
 - 2026-02-24: Completed Stage-1 ExecPlan item 1 by defining versioned `EventSpec`/`DescriptorSpec` in `src/tokens/schema.py` (`remi_tab_v1`, `tpq=24`) and added `tests/test_schema.py`.
 - 2026-02-24: Completed Stage-1 ExecPlan item 2 by adding `src/tokens/intervals.py` (melodic/harmonic helpers with QA vs production behavior), wiring repairs into `eventize_musicxml`, updating harmonic validation to shared math helpers, and adding `tests/test_intervals.py`; verify run: `53 passed, 1 skipped`.
+- 2026-02-24: Completed Stage-1 ExecPlan item 3 by adding `src/tokens/tokenizer.py` (voice-event parsing/serialization, stream canonicalization helpers), wiring `eventize_musicxml` pitched-onset emission through tokenizer serialization for canonical ordering, and adding `tests/test_tokenizer.py`; verify run: `59 passed, 1 skipped`.
diff --git a/TODO.md b/TODO.md
index da217dc3c318707990396dec3407998602311621..f34ae3002cbb8b57163085ffe73aebe53b830893
--- a/TODO.md
+++ b/TODO.md
@@ -3,6 +3,7 @@
 ## Milestone: Stage-1 Tokenizer & Dataset
 - [x] Implement `src/tokens/schema.py` (EventSpec + DescriptorSpec, versioned)
 - [x] Implement tokenizer interval math + round-trip tests
+- [x] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering
 - [ ] Implement `scripts/make_dataset.py` basic pipeline
 - [ ] Produce `data/processed/{events.parquet,barplans.parquet,stats.json}`
-- [ ] CI: `pytest -q` passes
+- [x] CI: `pytest -q` passes
diff --git a/docs/exec/EXECPLAN_stage1.md b/docs/exec/EXECPLAN_stage1.md
index 7f2647e15e5b673df54c0e21549951d68753a8c6..b8ee585fca521d0ea892fa6b21d13bb879078f5d
--- a/docs/exec/EXECPLAN_stage1.md
+++ b/docs/exec/EXECPLAN_stage1.md
@@ -11,7 +11,7 @@
 ## Checklist (one item per loop)
 - [x] Define EventSpec + DescriptorSpec in `src/tokens/schema.py` with version `remi_tab_v1` and `tpq=24`.
 - [x] Implement interval math helpers (melodic + harmonic ref) with QA vs production behavior.
-- [ ] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering.
+- [x] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering.
 - [ ] Add round-trip tests for pitch reconstruction and `HARM_*` consistency.
 - [ ] Implement `scripts/make_dataset.py` basic pipeline (parse, normalize, collapse doublings, track assignment, tokenize, stats).
 - [ ] Produce dataset artifacts in `data/processed/` and document counts in `stats.json`.
diff --git a/src/tokens/eventizer.py b/src/tokens/eventizer.py
index f7c666b524c3f03edbf62558e3a89e26917d8d43..53385718babf181fdc8367c1ca227731a1c1b799
--- a/src/tokens/eventizer.py
+++ b/src/tokens/eventizer.py
@@ -12,9 +12,9 @@
     IntervalRepairStats,
     compute_reference_pitch,
     compute_melodic_interval,
-    format_signed_interval,
     harmonic_tokens_for_pitch,
 )
+from src.tokens.tokenizer import VoiceEvent, serialize_voice_event
 
 DEFAULT_MAX_VOICES = 8
 DEFAULT_MAX_OCTAVES_PER_PITCH_CLASS = 3
@@ -584,15 +584,12 @@
                         tokens.append(f"ABS_VOICE_{v}_{ev.pitch}")
                     prev_pitch[v] = ev.pitch
 
-                tokens.append(f"VOICE_{v}")
-                tokens.append(f"DUR_{ev.duration_tick}")
                 base_pitch = prev_pitch[v] if prev_pitch[v] is not None else ev.pitch
                 mel_int = compute_melodic_interval(
                     ev.pitch,
                     base_pitch,
                     qa_mode=True,
                 )
-                tokens.append(f"MEL_INT12_{format_signed_interval(mel_int)}")
 
                 harm_oct_tok, harm_cls_tok = harmonic_tokens_for_pitch(
                     ev.pitch,
@@ -600,8 +597,21 @@
                     qa_mode=qa_mode,
                     stats=interval_repairs,
                 )
-                tokens.append(harm_oct_tok)
-                tokens.append(harm_cls_tok)
+                harm_oct = None if harm_oct_tok.endswith("_NA") else int(harm_oct_tok.rsplit("_", 1)[1])
+                harm_class = (
+                    None if harm_cls_tok.endswith("_NA") else int(harm_cls_tok.rsplit("_", 1)[1])
+                )
+                tokens.extend(
+                    serialize_voice_event(
+                        VoiceEvent(
+                            voice=v,
+                            duration_ticks=ev.duration_tick,
+                            mel_int=mel_int,
+                            harm_oct=harm_oct,
+                            harm_class=harm_class,
+                        )
+                    )
+                )
 
                 prev_pitch[v] = ev.pitch
                 active_until[v] = t + ev.duration_tick
diff --git a/src/tokens/tokenizer.py b/src/tokens/tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..3d7839be70301fe3c5376ba858263c8c4f66da1b
--- /dev/null
+++ b/src/tokens/tokenizer.py
@@ -0,0 +1,219 @@
+from dataclasses import dataclass
+from typing import List, Sequence, Tuple, Union
+
+from src.tokens.intervals import format_signed_interval
+
+
+@dataclass(frozen=True)
+class VoiceEvent:
+    voice: int
+    duration_ticks: int = 0
+    rest_ticks: int = 0
+    mel_int: int = 0
+    harm_oct: Union[int, None] = None
+    harm_class: Union[int, None] = None
+    dup_count: Union[int, None] = None
+    string: Union[int, None] = None
+    fret: Union[int, None] = None
+
+    def __post_init__(self) -> None:
+        if self.voice < 0:
+            raise ValueError("voice must be >= 0")
+        if self.duration_ticks < 0:
+            raise ValueError("duration_ticks must be >= 0")
+        if self.rest_ticks < 0:
+            raise ValueError("rest_ticks must be >= 0")
+        if self.dup_count is not None and self.dup_count < 1:
+            raise ValueError("dup_count must be >= 1 when set")
+        if (self.string is None) ^ (self.fret is None):
+            raise ValueError("string and fret must both be set or both be None")
+
+        is_rest = self.rest_ticks > 0
+        is_pitched = self.duration_ticks > 0
+        if is_rest == is_pitched:
+            raise ValueError("voice event must be exactly one of rest or pitched")
+
+        if is_rest:
+            if self.dup_count is not None:
+                raise ValueError("rest event cannot set dup_count")
+            if self.harm_oct is not None or self.harm_class is not None:
+                raise ValueError("rest event cannot set harmonic interval fields")
+            if self.string is not None or self.fret is not None:
+                raise ValueError("rest event cannot set tab fields")
+            return
+
+        if (self.harm_oct is None) ^ (self.harm_class is None):
+            raise ValueError("harm_oct and harm_class must both be set or both be None")
+        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
+            raise ValueError("harm_class must be in [0, 11]")
+
+    @property
+    def is_rest(self) -> bool:
+        return self.rest_ticks > 0
+
+
+def tokenize_event_text(text: str) -> List[str]:
+    raw = text.replace("\n", ",")
+    tokens = [tok.strip() for tok in raw.split(",")]
+    return [tok for tok in tokens if tok]
+
+
+def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
+    if start_idx >= len(tokens):
+        raise ValueError("start_idx out of range")
+
+    voice_tok = tokens[start_idx]
+    if not voice_tok.startswith("VOICE_"):
+        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")
+
+    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
+    next_idx = start_idx + 1
+    if next_idx >= len(tokens):
+        raise ValueError(f"truncated VOICE event at index {start_idx}")
+
+    first_tok = tokens[next_idx]
+    if first_tok.startswith("REST_"):
+        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
+        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1
+
+    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
+    next_idx += 1
+
+    dup_count = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
+        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
+        next_idx += 1
+
+    if next_idx + 2 >= len(tokens):
+        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")
+
+    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
+    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
+    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
+    next_idx += 3
+
+    string = None
+    fret = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
+        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
+        next_idx += 1
+        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
+            raise ValueError(f"missing FRET token after STR at index {next_idx}")
+        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
+        next_idx += 1
+    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
+        raise ValueError(f"FRET token must follow STR token at index {next_idx}")
+
+    return (
+        VoiceEvent(
+            voice=voice,
+            duration_ticks=duration_ticks,
+            mel_int=mel_int,
+            harm_oct=harm_oct,
+            harm_class=harm_class,
+            dup_count=dup_count,
+            string=string,
+            fret=fret,
+        ),
+        next_idx,
+    )
+
+
+def serialize_voice_event(event: VoiceEvent) -> List[str]:
+    tokens = [f"VOICE_{event.voice}"]
+    if event.is_rest:
+        tokens.append(f"REST_{event.rest_ticks}")
+        return tokens
+
+    tokens.append(f"DUR_{event.duration_ticks}")
+    if event.dup_count is not None:
+        tokens.append(f"DUP_{event.dup_count}")
+
+    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
+    if event.harm_oct is None:
+        tokens.append("HARM_OCT_NA")
+        tokens.append("HARM_CLASS_NA")
+    else:
+        tokens.append(f"HARM_OCT_{event.harm_oct}")
+        tokens.append(f"HARM_CLASS_{event.harm_class}")
+
+    if event.string is not None:
+        tokens.append(f"STR_{event.string}")
+        tokens.append(f"FRET_{event.fret}")
+
+    return tokens
+
+
+def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
+    parsed: List[Union[str, VoiceEvent]] = []
+    idx = 0
+    while idx < len(tokens):
+        tok = tokens[idx]
+        if tok.startswith("VOICE_"):
+            event, idx = parse_voice_event(tokens, idx)
+            parsed.append(event)
+            continue
+        parsed.append(tok)
+        idx += 1
+    return parsed
+
+
+def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
+    tokens: List[str] = []
+    for item in items:
+        if isinstance(item, VoiceEvent):
+            tokens.extend(serialize_voice_event(item))
+        else:
+            tokens.append(item)
+    return tokens
+
+
+def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
+    return serialize_event_stream(parse_event_stream(tokens))
+
+
+def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
+    if not token.startswith(prefix):
+        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
+    value = token[len(prefix) :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc
+
+
+def _parse_signed_mel(token: str, idx: int) -> int:
+    if not token.startswith("MEL_INT12_"):
+        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
+    value = token[len("MEL_INT12_") :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_OCT_"):
+        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
+    value = token[len("HARM_OCT_") :]
+    if value == "NA":
+        return None
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_CLASS_"):
+        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
+    value = token[len("HARM_CLASS_") :]
+    if value == "NA":
+        return None
+    try:
+        parsed = int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
+    if not (0 <= parsed <= 11):
+        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
+    return parsed
diff --git a/tests/test_tokenizer.py b/tests/test_tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..4f4741f7ebc6a851a695b5f6081e10cfc2c03f03
--- /dev/null
+++ b/tests/test_tokenizer.py
@@ -0,0 +1,107 @@
+import sys
+from pathlib import Path
+
+import pytest
+
+sys.path.insert(0, str(Path(__file__).parents[1]))
+
+from src.tokens.tokenizer import (
+    VoiceEvent,
+    canonicalize_event_stream,
+    parse_event_stream,
+    parse_voice_event,
+    serialize_event_stream,
+    serialize_voice_event,
+    tokenize_event_text,
+)
+
+
+def test_parse_pitched_voice_event_with_dup_and_tab():
+    tokens = [
+        "VOICE_2",
+        "DUR_12",
+        "DUP_3",
+        "MEL_INT12_+7",
+        "HARM_OCT_1",
+        "HARM_CLASS_7",
+        "STR_4",
+        "FRET_2",
+        "POS_6",
+    ]
+
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 8
+    assert event.voice == 2
+    assert event.duration_ticks == 12
+    assert event.dup_count == 3
+    assert event.mel_int == 7
+    assert event.harm_oct == 1
+    assert event.harm_class == 7
+    assert event.string == 4
+    assert event.fret == 2
+
+
+def test_parse_rest_voice_event():
+    tokens = ["VOICE_1", "REST_18", "BAR"]
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 2
+    assert event.voice == 1
+    assert event.is_rest
+    assert event.rest_ticks == 18
+
+
+def test_serialize_voice_event_uses_canonical_order():
+    event = VoiceEvent(
+        voice=3,
+        duration_ticks=24,
+        dup_count=2,
+        mel_int=11,
+        harm_oct=0,
+        harm_class=11,
+    )
+    assert serialize_voice_event(event) == [
+        "VOICE_3",
+        "DUR_24",
+        "DUP_2",
+        "MEL_INT12_+11",
+        "HARM_OCT_0",
+        "HARM_CLASS_11",
+    ]
+
+
+def test_canonicalize_event_stream_normalizes_mel_sign():
+    tokens = [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+    assert canonicalize_event_stream(tokens) == [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_+2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+
+
+def test_parse_event_stream_roundtrip_and_text_tokenization():
+    text = "BAR, POS_0, VOICE_0, DUR_4, MEL_INT12_0, HARM_OCT_NA, HARM_CLASS_NA"
+    tokens = tokenize_event_text(text)
+    parsed = parse_event_stream(tokens)
+    assert serialize_event_stream(parsed) == tokens
+
+
+def test_parse_rejects_mixed_harm_na_state():
+    tokens = ["VOICE_0", "DUR_8", "MEL_INT12_0", "HARM_OCT_NA", "HARM_CLASS_2"]
+    with pytest.raises(ValueError, match="harm_oct and harm_class must both be set or both be None"):
+        parse_voice_event(tokens, 0)


thinking
**Inspecting documentation changes**
exec
/bin/bash -lc 'git diff -- src/tokens/tokenizer.py src/tokens/eventizer.py tests/test_tokenizer.py docs/exec/EXECPLAN_stage1.md TODO.md PROGRESS.md' in /home/stephen-reilly/dev/bach_genexec
/bin/bash -lc 'git status --short' in /home/stephen-reilly/dev/bach_gen succeeded in 51ms:
 M PROGRESS.md
 M TODO.md
 M docs/exec/EXECPLAN_stage1.md
 M src/tokens/eventizer.py
?? .codex/loop_runs/20260224_030212/context_3.md
?? .codex/loop_runs/20260224_030212/iter_3.err
?? .codex/loop_runs/20260224_030212/iter_3.out
?? .codex/loop_runs/20260224_030212/prompt_3.md
?? src/tokens/tokenizer.py
?? tests/test_tokenizer.py
 succeeded in 52ms:
diff --git a/PROGRESS.md b/PROGRESS.md
index 551ac82..11f763e 100644
--- a/PROGRESS.md
+++ b/PROGRESS.md
@@ -7,3 +7,4 @@
 - 2026-02-24: Updated loop to use `--full-auto` on newer Codex CLI builds where explicit approval flags are unavailable.
 - 2026-02-24: Completed Stage-1 ExecPlan item 1 by defining versioned `EventSpec`/`DescriptorSpec` in `src/tokens/schema.py` (`remi_tab_v1`, `tpq=24`) and added `tests/test_schema.py`.
 - 2026-02-24: Completed Stage-1 ExecPlan item 2 by adding `src/tokens/intervals.py` (melodic/harmonic helpers with QA vs production behavior), wiring repairs into `eventize_musicxml`, updating harmonic validation to shared math helpers, and adding `tests/test_intervals.py`; verify run: `53 passed, 1 skipped`.
+- 2026-02-24: Completed Stage-1 ExecPlan item 3 by adding `src/tokens/tokenizer.py` (voice-event parsing/serialization, stream canonicalization helpers), wiring `eventize_musicxml` pitched-onset emission through tokenizer serialization for canonical ordering, and adding `tests/test_tokenizer.py`; verify run: `59 passed, 1 skipped`.
diff --git a/TODO.md b/TODO.md
index da217dc..f34ae30 100644
--- a/TODO.md
+++ b/TODO.md
@@ -3,6 +3,7 @@
 ## Milestone: Stage-1 Tokenizer & Dataset
 - [x] Implement `src/tokens/schema.py` (EventSpec + DescriptorSpec, versioned)
 - [x] Implement tokenizer interval math + round-trip tests
+- [x] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering
 - [ ] Implement `scripts/make_dataset.py` basic pipeline
 - [ ] Produce `data/processed/{events.parquet,barplans.parquet,stats.json}`
file update:
diff --git a/PROGRESS.md b/PROGRESS.md
index 551ac82b1a8e3c568478d8e3046e4875e6badff8..11f763e2dbb4256baeb84ecbe0f89f7a55bf2222
--- a/PROGRESS.md
+++ b/PROGRESS.md
@@ -7,3 +7,4 @@
 - 2026-02-24: Updated loop to use `--full-auto` on newer Codex CLI builds where explicit approval flags are unavailable.
 - 2026-02-24: Completed Stage-1 ExecPlan item 1 by defining versioned `EventSpec`/`DescriptorSpec` in `src/tokens/schema.py` (`remi_tab_v1`, `tpq=24`) and added `tests/test_schema.py`.
 - 2026-02-24: Completed Stage-1 ExecPlan item 2 by adding `src/tokens/intervals.py` (melodic/harmonic helpers with QA vs production behavior), wiring repairs into `eventize_musicxml`, updating harmonic validation to shared math helpers, and adding `tests/test_intervals.py`; verify run: `53 passed, 1 skipped`.
+- 2026-02-24: Completed Stage-1 ExecPlan item 3 by adding `src/tokens/tokenizer.py` (voice-event parsing/serialization, stream canonicalization helpers), wiring `eventize_musicxml` pitched-onset emission through tokenizer serialization for canonical ordering, and adding `tests/test_tokenizer.py`; verify run: `59 passed, 1 skipped`.
diff --git a/TODO.md b/TODO.md
index da217dc3c318707990396dec3407998602311621..f34ae3002cbb8b57163085ffe73aebe53b830893
--- a/TODO.md
+++ b/TODO.md
@@ -3,6 +3,7 @@
 ## Milestone: Stage-1 Tokenizer & Dataset
 - [x] Implement `src/tokens/schema.py` (EventSpec + DescriptorSpec, versioned)
 - [x] Implement tokenizer interval math + round-trip tests
+- [x] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering
 - [ ] Implement `scripts/make_dataset.py` basic pipeline
 - [ ] Produce `data/processed/{events.parquet,barplans.parquet,stats.json}`
-- [ ] CI: `pytest -q` passes
+- [x] CI: `pytest -q` passes
diff --git a/docs/exec/EXECPLAN_stage1.md b/docs/exec/EXECPLAN_stage1.md
index 7f2647e15e5b673df54c0e21549951d68753a8c6..b8ee585fca521d0ea892fa6b21d13bb879078f5d
--- a/docs/exec/EXECPLAN_stage1.md
+++ b/docs/exec/EXECPLAN_stage1.md
@@ -11,7 +11,7 @@
 ## Checklist (one item per loop)
 - [x] Define EventSpec + DescriptorSpec in `src/tokens/schema.py` with version `remi_tab_v1` and `tpq=24`.
 - [x] Implement interval math helpers (melodic + harmonic ref) with QA vs production behavior.
-- [ ] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering.
+- [x] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering.
 - [ ] Add round-trip tests for pitch reconstruction and `HARM_*` consistency.
 - [ ] Implement `scripts/make_dataset.py` basic pipeline (parse, normalize, collapse doublings, track assignment, tokenize, stats).
 - [ ] Produce dataset artifacts in `data/processed/` and document counts in `stats.json`.
diff --git a/src/tokens/eventizer.py b/src/tokens/eventizer.py
index f7c666b524c3f03edbf62558e3a89e26917d8d43..53385718babf181fdc8367c1ca227731a1c1b799
--- a/src/tokens/eventizer.py
+++ b/src/tokens/eventizer.py
@@ -12,9 +12,9 @@
     IntervalRepairStats,
     compute_reference_pitch,
     compute_melodic_interval,
-    format_signed_interval,
     harmonic_tokens_for_pitch,
 )
+from src.tokens.tokenizer import VoiceEvent, serialize_voice_event
 
 DEFAULT_MAX_VOICES = 8
 DEFAULT_MAX_OCTAVES_PER_PITCH_CLASS = 3
@@ -584,15 +584,12 @@
                         tokens.append(f"ABS_VOICE_{v}_{ev.pitch}")
                     prev_pitch[v] = ev.pitch
 
-                tokens.append(f"VOICE_{v}")
-                tokens.append(f"DUR_{ev.duration_tick}")
                 base_pitch = prev_pitch[v] if prev_pitch[v] is not None else ev.pitch
                 mel_int = compute_melodic_interval(
                     ev.pitch,
                     base_pitch,
                     qa_mode=True,
                 )
-                tokens.append(f"MEL_INT12_{format_signed_interval(mel_int)}")
 
                 harm_oct_tok, harm_cls_tok = harmonic_tokens_for_pitch(
                     ev.pitch,
@@ -600,8 +597,21 @@
                     qa_mode=qa_mode,
                     stats=interval_repairs,
                 )
-                tokens.append(harm_oct_tok)
-                tokens.append(harm_cls_tok)
+                harm_oct = None if harm_oct_tok.endswith("_NA") else int(harm_oct_tok.rsplit("_", 1)[1])
+                harm_class = (
+                    None if harm_cls_tok.endswith("_NA") else int(harm_cls_tok.rsplit("_", 1)[1])
+                )
+                tokens.extend(
+                    serialize_voice_event(
+                        VoiceEvent(
+                            voice=v,
+                            duration_ticks=ev.duration_tick,
+                            mel_int=mel_int,
+                            harm_oct=harm_oct,
+                            harm_class=harm_class,
+                        )
+                    )
+                )
 
                 prev_pitch[v] = ev.pitch
                 active_until[v] = t + ev.duration_tick
diff --git a/src/tokens/tokenizer.py b/src/tokens/tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..3d7839be70301fe3c5376ba858263c8c4f66da1b
--- /dev/null
+++ b/src/tokens/tokenizer.py
@@ -0,0 +1,219 @@
+from dataclasses import dataclass
+from typing import List, Sequence, Tuple, Union
+
+from src.tokens.intervals import format_signed_interval
+
+
+@dataclass(frozen=True)
+class VoiceEvent:
+    voice: int
+    duration_ticks: int = 0
+    rest_ticks: int = 0
+    mel_int: int = 0
+    harm_oct: Union[int, None] = None
+    harm_class: Union[int, None] = None
+    dup_count: Union[int, None] = None
+    string: Union[int, None] = None
+    fret: Union[int, None] = None
+
+    def __post_init__(self) -> None:
+        if self.voice < 0:
+            raise ValueError("voice must be >= 0")
+        if self.duration_ticks < 0:
+            raise ValueError("duration_ticks must be >= 0")
+        if self.rest_ticks < 0:
+            raise ValueError("rest_ticks must be >= 0")
+        if self.dup_count is not None and self.dup_count < 1:
+            raise ValueError("dup_count must be >= 1 when set")
+        if (self.string is None) ^ (self.fret is None):
+            raise ValueError("string and fret must both be set or both be None")
+
+        is_rest = self.rest_ticks > 0
+        is_pitched = self.duration_ticks > 0
+        if is_rest == is_pitched:
+            raise ValueError("voice event must be exactly one of rest or pitched")
+
+        if is_rest:
+            if self.dup_count is not None:
+                raise ValueError("rest event cannot set dup_count")
+            if self.harm_oct is not None or self.harm_class is not None:
+                raise ValueError("rest event cannot set harmonic interval fields")
+            if self.string is not None or self.fret is not None:
+                raise ValueError("rest event cannot set tab fields")
+            return
+
+        if (self.harm_oct is None) ^ (self.harm_class is None):
+            raise ValueError("harm_oct and harm_class must both be set or both be None")
+        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
+            raise ValueError("harm_class must be in [0, 11]")
+
+    @property
+    def is_rest(self) -> bool:
+        return self.rest_ticks > 0
+
+
+def tokenize_event_text(text: str) -> List[str]:
+    raw = text.replace("\n", ",")
+    tokens = [tok.strip() for tok in raw.split(",")]
+    return [tok for tok in tokens if tok]
+
+
+def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
+    if start_idx >= len(tokens):
+        raise ValueError("start_idx out of range")
+
+    voice_tok = tokens[start_idx]
+    if not voice_tok.startswith("VOICE_"):
+        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")
+
+    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
+    next_idx = start_idx + 1
+    if next_idx >= len(tokens):
+        raise ValueError(f"truncated VOICE event at index {start_idx}")
+
+    first_tok = tokens[next_idx]
+    if first_tok.startswith("REST_"):
+        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
+        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1
+
+    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
+    next_idx += 1
+
+    dup_count = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
+        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
+        next_idx += 1
+
+    if next_idx + 2 >= len(tokens):
+        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")
+
+    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
+    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
+    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
+    next_idx += 3
+
+    string = None
+    fret = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
+        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
+        next_idx += 1
+        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
+            raise ValueError(f"missing FRET token after STR at index {next_idx}")
+        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
+        next_idx += 1
+    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
+        raise ValueError(f"FRET token must follow STR token at index {next_idx}")
+
+    return (
+        VoiceEvent(
+            voice=voice,
+            duration_ticks=duration_ticks,
+            mel_int=mel_int,
+            harm_oct=harm_oct,
+            harm_class=harm_class,
+            dup_count=dup_count,
+            string=string,
+            fret=fret,
+        ),
+        next_idx,
+    )
+
+
+def serialize_voice_event(event: VoiceEvent) -> List[str]:
+    tokens = [f"VOICE_{event.voice}"]
+    if event.is_rest:
+        tokens.append(f"REST_{event.rest_ticks}")
+        return tokens
+
+    tokens.append(f"DUR_{event.duration_ticks}")
+    if event.dup_count is not None:
+        tokens.append(f"DUP_{event.dup_count}")
+
+    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
+    if event.harm_oct is None:
+        tokens.append("HARM_OCT_NA")
+        tokens.append("HARM_CLASS_NA")
+    else:
+        tokens.append(f"HARM_OCT_{event.harm_oct}")
+        tokens.append(f"HARM_CLASS_{event.harm_class}")
+
+    if event.string is not None:
+        tokens.append(f"STR_{event.string}")
+        tokens.append(f"FRET_{event.fret}")
+
+    return tokens
+
+
+def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
+    parsed: List[Union[str, VoiceEvent]] = []
+    idx = 0
+    while idx < len(tokens):
+        tok = tokens[idx]
+        if tok.startswith("VOICE_"):
+            event, idx = parse_voice_event(tokens, idx)
+            parsed.append(event)
+            continue
+        parsed.append(tok)
+        idx += 1
+    return parsed
+
+
+def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
+    tokens: List[str] = []
+    for item in items:
+        if isinstance(item, VoiceEvent):
+            tokens.extend(serialize_voice_event(item))
+        else:
+            tokens.append(item)
+    return tokens
+
+
+def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
+    return serialize_event_stream(parse_event_stream(tokens))
+
+
+def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
+    if not token.startswith(prefix):
+        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
+    value = token[len(prefix) :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc
+
+
+def _parse_signed_mel(token: str, idx: int) -> int:
+    if not token.startswith("MEL_INT12_"):
+        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
+    value = token[len("MEL_INT12_") :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_OCT_"):
+        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
+    value = token[len("HARM_OCT_") :]
+    if value == "NA":
+        return None
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_CLASS_"):
+        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
+    value = token[len("HARM_CLASS_") :]
+    if value == "NA":
+        return None
+    try:
+        parsed = int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
+    if not (0 <= parsed <= 11):
+        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
+    return parsed
diff --git a/tests/test_tokenizer.py b/tests/test_tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..4f4741f7ebc6a851a695b5f6081e10cfc2c03f03
--- /dev/null
+++ b/tests/test_tokenizer.py
@@ -0,0 +1,107 @@
+import sys
+from pathlib import Path
+
+import pytest
+
+sys.path.insert(0, str(Path(__file__).parents[1]))
+
+from src.tokens.tokenizer import (
+    VoiceEvent,
+    canonicalize_event_stream,
+    parse_event_stream,
+    parse_voice_event,
+    serialize_event_stream,
+    serialize_voice_event,
+    tokenize_event_text,
+)
+
+
+def test_parse_pitched_voice_event_with_dup_and_tab():
+    tokens = [
+        "VOICE_2",
+        "DUR_12",
+        "DUP_3",
+        "MEL_INT12_+7",
+        "HARM_OCT_1",
+        "HARM_CLASS_7",
+        "STR_4",
+        "FRET_2",
+        "POS_6",
+    ]
+
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 8
+    assert event.voice == 2
+    assert event.duration_ticks == 12
+    assert event.dup_count == 3
+    assert event.mel_int == 7
+    assert event.harm_oct == 1
+    assert event.harm_class == 7
+    assert event.string == 4
+    assert event.fret == 2
+
+
+def test_parse_rest_voice_event():
+    tokens = ["VOICE_1", "REST_18", "BAR"]
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 2
+    assert event.voice == 1
+    assert event.is_rest
+    assert event.rest_ticks == 18
+
+
+def test_serialize_voice_event_uses_canonical_order():
+    event = VoiceEvent(
+        voice=3,
+        duration_ticks=24,
+        dup_count=2,
+        mel_int=11,
+        harm_oct=0,
+        harm_class=11,
+    )
+    assert serialize_voice_event(event) == [
+        "VOICE_3",
+        "DUR_24",
+        "DUP_2",
+        "MEL_INT12_+11",
+        "HARM_OCT_0",
+        "HARM_CLASS_11",
+    ]
+
+
+def test_canonicalize_event_stream_normalizes_mel_sign():
+    tokens = [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+    assert canonicalize_event_stream(tokens) == [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_+2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+
+
+def test_parse_event_stream_roundtrip_and_text_tokenization():
+    text = "BAR, POS_0, VOICE_0, DUR_4, MEL_INT12_0, HARM_OCT_NA, HARM_CLASS_NA"
+    tokens = tokenize_event_text(text)
+    parsed = parse_event_stream(tokens)
+    assert serialize_event_stream(parsed) == tokens
+
+
+def test_parse_rejects_mixed_harm_na_state():
+    tokens = ["VOICE_0", "DUR_8", "MEL_INT12_0", "HARM_OCT_NA", "HARM_CLASS_2"]
+    with pytest.raises(ValueError, match="harm_oct and harm_class must both be set or both be None"):
+        parse_voice_event(tokens, 0)


thinking
**Planning tokenization implementation**
exec
/bin/bash -lc "sed -n '1,360p' src/tokens/tokenizer.py" in /home/stephen-reilly/dev/bach_genexec
/bin/bash -lc "sed -n '1,260p' tests/test_tokenizer.py" in /home/stephen-reilly/dev/bach_gen succeeded in 51ms:
from dataclasses import dataclass
from typing import List, Sequence, Tuple, Union

from src.tokens.intervals import format_signed_interval


@dataclass(frozen=True)
class VoiceEvent:
    voice: int
    duration_ticks: int = 0
    rest_ticks: int = 0
    mel_int: int = 0
    harm_oct: Union[int, None] = None
    harm_class: Union[int, None] = None
    dup_count: Union[int, None] = None
    string: Union[int, None] = None
    fret: Union[int, None] = None

    def __post_init__(self) -> None:
        if self.voice < 0:
 succeeded in 50ms:
import sys
from pathlib import Path

import pytest

sys.path.insert(0, str(Path(__file__).parents[1]))

from src.tokens.tokenizer import (
    VoiceEvent,
    canonicalize_event_stream,
    parse_event_stream,
    parse_voice_event,
    serialize_event_stream,
    serialize_voice_event,
    tokenize_event_text,
)


def test_parse_pitched_voice_event_with_dup_and_tab():
    tokens = [
file update:
diff --git a/PROGRESS.md b/PROGRESS.md
index 551ac82b1a8e3c568478d8e3046e4875e6badff8..11f763e2dbb4256baeb84ecbe0f89f7a55bf2222
--- a/PROGRESS.md
+++ b/PROGRESS.md
@@ -7,3 +7,4 @@
 - 2026-02-24: Updated loop to use `--full-auto` on newer Codex CLI builds where explicit approval flags are unavailable.
 - 2026-02-24: Completed Stage-1 ExecPlan item 1 by defining versioned `EventSpec`/`DescriptorSpec` in `src/tokens/schema.py` (`remi_tab_v1`, `tpq=24`) and added `tests/test_schema.py`.
 - 2026-02-24: Completed Stage-1 ExecPlan item 2 by adding `src/tokens/intervals.py` (melodic/harmonic helpers with QA vs production behavior), wiring repairs into `eventize_musicxml`, updating harmonic validation to shared math helpers, and adding `tests/test_intervals.py`; verify run: `53 passed, 1 skipped`.
+- 2026-02-24: Completed Stage-1 ExecPlan item 3 by adding `src/tokens/tokenizer.py` (voice-event parsing/serialization, stream canonicalization helpers), wiring `eventize_musicxml` pitched-onset emission through tokenizer serialization for canonical ordering, and adding `tests/test_tokenizer.py`; verify run: `59 passed, 1 skipped`.
diff --git a/TODO.md b/TODO.md
index da217dc3c318707990396dec3407998602311621..f34ae3002cbb8b57163085ffe73aebe53b830893
--- a/TODO.md
+++ b/TODO.md
@@ -3,6 +3,7 @@
 ## Milestone: Stage-1 Tokenizer & Dataset
 - [x] Implement `src/tokens/schema.py` (EventSpec + DescriptorSpec, versioned)
 - [x] Implement tokenizer interval math + round-trip tests
+- [x] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering
 - [ ] Implement `scripts/make_dataset.py` basic pipeline
 - [ ] Produce `data/processed/{events.parquet,barplans.parquet,stats.json}`
-- [ ] CI: `pytest -q` passes
+- [x] CI: `pytest -q` passes
diff --git a/docs/exec/EXECPLAN_stage1.md b/docs/exec/EXECPLAN_stage1.md
index 7f2647e15e5b673df54c0e21549951d68753a8c6..b8ee585fca521d0ea892fa6b21d13bb879078f5d
--- a/docs/exec/EXECPLAN_stage1.md
+++ b/docs/exec/EXECPLAN_stage1.md
@@ -11,7 +11,7 @@
 ## Checklist (one item per loop)
 - [x] Define EventSpec + DescriptorSpec in `src/tokens/schema.py` with version `remi_tab_v1` and `tpq=24`.
 - [x] Implement interval math helpers (melodic + harmonic ref) with QA vs production behavior.
-- [ ] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering.
+- [x] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering.
 - [ ] Add round-trip tests for pitch reconstruction and `HARM_*` consistency.
 - [ ] Implement `scripts/make_dataset.py` basic pipeline (parse, normalize, collapse doublings, track assignment, tokenize, stats).
 - [ ] Produce dataset artifacts in `data/processed/` and document counts in `stats.json`.
diff --git a/src/tokens/eventizer.py b/src/tokens/eventizer.py
index f7c666b524c3f03edbf62558e3a89e26917d8d43..53385718babf181fdc8367c1ca227731a1c1b799
--- a/src/tokens/eventizer.py
+++ b/src/tokens/eventizer.py
@@ -12,9 +12,9 @@
     IntervalRepairStats,
     compute_reference_pitch,
     compute_melodic_interval,
-    format_signed_interval,
     harmonic_tokens_for_pitch,
 )
+from src.tokens.tokenizer import VoiceEvent, serialize_voice_event
 
 DEFAULT_MAX_VOICES = 8
 DEFAULT_MAX_OCTAVES_PER_PITCH_CLASS = 3
@@ -584,15 +584,12 @@
                         tokens.append(f"ABS_VOICE_{v}_{ev.pitch}")
                     prev_pitch[v] = ev.pitch
 
-                tokens.append(f"VOICE_{v}")
-                tokens.append(f"DUR_{ev.duration_tick}")
                 base_pitch = prev_pitch[v] if prev_pitch[v] is not None else ev.pitch
                 mel_int = compute_melodic_interval(
                     ev.pitch,
                     base_pitch,
                     qa_mode=True,
                 )
-                tokens.append(f"MEL_INT12_{format_signed_interval(mel_int)}")
 
                 harm_oct_tok, harm_cls_tok = harmonic_tokens_for_pitch(
                     ev.pitch,
@@ -600,8 +597,21 @@
                     qa_mode=qa_mode,
                     stats=interval_repairs,
                 )
-                tokens.append(harm_oct_tok)
-                tokens.append(harm_cls_tok)
+                harm_oct = None if harm_oct_tok.endswith("_NA") else int(harm_oct_tok.rsplit("_", 1)[1])
+                harm_class = (
+                    None if harm_cls_tok.endswith("_NA") else int(harm_cls_tok.rsplit("_", 1)[1])
+                )
+                tokens.extend(
+                    serialize_voice_event(
+                        VoiceEvent(
+                            voice=v,
+                            duration_ticks=ev.duration_tick,
+                            mel_int=mel_int,
+                            harm_oct=harm_oct,
+                            harm_class=harm_class,
+                        )
+                    )
+                )
 
                 prev_pitch[v] = ev.pitch
                 active_until[v] = t + ev.duration_tick
diff --git a/src/tokens/tokenizer.py b/src/tokens/tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..3d7839be70301fe3c5376ba858263c8c4f66da1b
--- /dev/null
+++ b/src/tokens/tokenizer.py
@@ -0,0 +1,219 @@
+from dataclasses import dataclass
+from typing import List, Sequence, Tuple, Union
+
+from src.tokens.intervals import format_signed_interval
+
+
+@dataclass(frozen=True)
+class VoiceEvent:
+    voice: int
+    duration_ticks: int = 0
+    rest_ticks: int = 0
+    mel_int: int = 0
+    harm_oct: Union[int, None] = None
+    harm_class: Union[int, None] = None
+    dup_count: Union[int, None] = None
+    string: Union[int, None] = None
+    fret: Union[int, None] = None
+
+    def __post_init__(self) -> None:
+        if self.voice < 0:
+            raise ValueError("voice must be >= 0")
+        if self.duration_ticks < 0:
+            raise ValueError("duration_ticks must be >= 0")
+        if self.rest_ticks < 0:
+            raise ValueError("rest_ticks must be >= 0")
+        if self.dup_count is not None and self.dup_count < 1:
+            raise ValueError("dup_count must be >= 1 when set")
+        if (self.string is None) ^ (self.fret is None):
+            raise ValueError("string and fret must both be set or both be None")
+
+        is_rest = self.rest_ticks > 0
+        is_pitched = self.duration_ticks > 0
+        if is_rest == is_pitched:
+            raise ValueError("voice event must be exactly one of rest or pitched")
+
+        if is_rest:
+            if self.dup_count is not None:
+                raise ValueError("rest event cannot set dup_count")
+            if self.harm_oct is not None or self.harm_class is not None:
+                raise ValueError("rest event cannot set harmonic interval fields")
+            if self.string is not None or self.fret is not None:
+                raise ValueError("rest event cannot set tab fields")
+            return
+
+        if (self.harm_oct is None) ^ (self.harm_class is None):
+            raise ValueError("harm_oct and harm_class must both be set or both be None")
+        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
+            raise ValueError("harm_class must be in [0, 11]")
+
+    @property
+    def is_rest(self) -> bool:
+        return self.rest_ticks > 0
+
+
+def tokenize_event_text(text: str) -> List[str]:
+    raw = text.replace("\n", ",")
+    tokens = [tok.strip() for tok in raw.split(",")]
+    return [tok for tok in tokens if tok]
+
+
+def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
+    if start_idx >= len(tokens):
+        raise ValueError("start_idx out of range")
+
+    voice_tok = tokens[start_idx]
+    if not voice_tok.startswith("VOICE_"):
+        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")
+
+    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
+    next_idx = start_idx + 1
+    if next_idx >= len(tokens):
+        raise ValueError(f"truncated VOICE event at index {start_idx}")
+
+    first_tok = tokens[next_idx]
+    if first_tok.startswith("REST_"):
+        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
+        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1
+
+    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
+    next_idx += 1
+
+    dup_count = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
+        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
+        next_idx += 1
+
+    if next_idx + 2 >= len(tokens):
+        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")
+
+    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
+    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
+    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
+    next_idx += 3
+
+    string = None
+    fret = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
+        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
+        next_idx += 1
+        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
+            raise ValueError(f"missing FRET token after STR at index {next_idx}")
+        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
+        next_idx += 1
+    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
+        raise ValueError(f"FRET token must follow STR token at index {next_idx}")
+
+    return (
+        VoiceEvent(
+            voice=voice,
+            duration_ticks=duration_ticks,
+            mel_int=mel_int,
+            harm_oct=harm_oct,
+            harm_class=harm_class,
+            dup_count=dup_count,
+            string=string,
+            fret=fret,
+        ),
+        next_idx,
+    )
+
+
+def serialize_voice_event(event: VoiceEvent) -> List[str]:
+    tokens = [f"VOICE_{event.voice}"]
+    if event.is_rest:
+        tokens.append(f"REST_{event.rest_ticks}")
+        return tokens
+
+    tokens.append(f"DUR_{event.duration_ticks}")
+    if event.dup_count is not None:
+        tokens.append(f"DUP_{event.dup_count}")
+
+    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
+    if event.harm_oct is None:
+        tokens.append("HARM_OCT_NA")
+        tokens.append("HARM_CLASS_NA")
+    else:
+        tokens.append(f"HARM_OCT_{event.harm_oct}")
+        tokens.append(f"HARM_CLASS_{event.harm_class}")
+
+    if event.string is not None:
+        tokens.append(f"STR_{event.string}")
+        tokens.append(f"FRET_{event.fret}")
+
+    return tokens
+
+
+def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
+    parsed: List[Union[str, VoiceEvent]] = []
+    idx = 0
+    while idx < len(tokens):
+        tok = tokens[idx]
+        if tok.startswith("VOICE_"):
+            event, idx = parse_voice_event(tokens, idx)
+            parsed.append(event)
+            continue
+        parsed.append(tok)
+        idx += 1
+    return parsed
+
+
+def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
+    tokens: List[str] = []
+    for item in items:
+        if isinstance(item, VoiceEvent):
+            tokens.extend(serialize_voice_event(item))
+        else:
+            tokens.append(item)
+    return tokens
+
+
+def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
+    return serialize_event_stream(parse_event_stream(tokens))
+
+
+def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
+    if not token.startswith(prefix):
+        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
+    value = token[len(prefix) :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc
+
+
+def _parse_signed_mel(token: str, idx: int) -> int:
+    if not token.startswith("MEL_INT12_"):
+        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
+    value = token[len("MEL_INT12_") :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_OCT_"):
+        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
+    value = token[len("HARM_OCT_") :]
+    if value == "NA":
+        return None
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_CLASS_"):
+        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
+    value = token[len("HARM_CLASS_") :]
+    if value == "NA":
+        return None
+    try:
+        parsed = int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
+    if not (0 <= parsed <= 11):
+        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
+    return parsed
diff --git a/tests/test_tokenizer.py b/tests/test_tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..4f4741f7ebc6a851a695b5f6081e10cfc2c03f03
--- /dev/null
+++ b/tests/test_tokenizer.py
@@ -0,0 +1,107 @@
+import sys
+from pathlib import Path
+
+import pytest
+
+sys.path.insert(0, str(Path(__file__).parents[1]))
+
+from src.tokens.tokenizer import (
+    VoiceEvent,
+    canonicalize_event_stream,
+    parse_event_stream,
+    parse_voice_event,
+    serialize_event_stream,
+    serialize_voice_event,
+    tokenize_event_text,
+)
+
+
+def test_parse_pitched_voice_event_with_dup_and_tab():
+    tokens = [
+        "VOICE_2",
+        "DUR_12",
+        "DUP_3",
+        "MEL_INT12_+7",
+        "HARM_OCT_1",
+        "HARM_CLASS_7",
+        "STR_4",
+        "FRET_2",
+        "POS_6",
+    ]
+
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 8
+    assert event.voice == 2
+    assert event.duration_ticks == 12
+    assert event.dup_count == 3
+    assert event.mel_int == 7
+    assert event.harm_oct == 1
+    assert event.harm_class == 7
+    assert event.string == 4
+    assert event.fret == 2
+
+
+def test_parse_rest_voice_event():
+    tokens = ["VOICE_1", "REST_18", "BAR"]
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 2
+    assert event.voice == 1
+    assert event.is_rest
+    assert event.rest_ticks == 18
+
+
+def test_serialize_voice_event_uses_canonical_order():
+    event = VoiceEvent(
+        voice=3,
+        duration_ticks=24,
+        dup_count=2,
+        mel_int=11,
+        harm_oct=0,
+        harm_class=11,
+    )
+    assert serialize_voice_event(event) == [
+        "VOICE_3",
+        "DUR_24",
+        "DUP_2",
+        "MEL_INT12_+11",
+        "HARM_OCT_0",
+        "HARM_CLASS_11",
+    ]
+
+
+def test_canonicalize_event_stream_normalizes_mel_sign():
+    tokens = [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+    assert canonicalize_event_stream(tokens) == [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_+2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+
+
+def test_parse_event_stream_roundtrip_and_text_tokenization():
+    text = "BAR, POS_0, VOICE_0, DUR_4, MEL_INT12_0, HARM_OCT_NA, HARM_CLASS_NA"
+    tokens = tokenize_event_text(text)
+    parsed = parse_event_stream(tokens)
+    assert serialize_event_stream(parsed) == tokens
+
+
+def test_parse_rejects_mixed_harm_na_state():
+    tokens = ["VOICE_0", "DUR_8", "MEL_INT12_0", "HARM_OCT_NA", "HARM_CLASS_2"]
+    with pytest.raises(ValueError, match="harm_oct and harm_class must both be set or both be None"):
+        parse_voice_event(tokens, 0)


thinking
**Reviewing event parsing edge cases**
codex
{"done":false,"progress_made":true,"summary":"Completed the next unchecked ExecPlan item by adding `src/tokens/tokenizer.py` with voice-event parsing/serialization and stream canonicalization helpers, wired `src/tokens/eventizer.py` to emit pitched voice events through canonical serializer ordering, added `tests/test_tokenizer.py`, marked the ExecPlan/TODO item complete, checked CI in TODO, and appended a PROGRESS entry.","tests_ran":true,"tests_status":"pass","next_step":"Do the next unchecked item in `docs/exec/EXECPLAN_stage1.md`: add round-trip tests for pitch reconstruction and `HARM_*` consistency.","stop_reason":""}
file update:
diff --git a/PROGRESS.md b/PROGRESS.md
index 551ac82b1a8e3c568478d8e3046e4875e6badff8..11f763e2dbb4256baeb84ecbe0f89f7a55bf2222
--- a/PROGRESS.md
+++ b/PROGRESS.md
@@ -7,3 +7,4 @@
 - 2026-02-24: Updated loop to use `--full-auto` on newer Codex CLI builds where explicit approval flags are unavailable.
 - 2026-02-24: Completed Stage-1 ExecPlan item 1 by defining versioned `EventSpec`/`DescriptorSpec` in `src/tokens/schema.py` (`remi_tab_v1`, `tpq=24`) and added `tests/test_schema.py`.
 - 2026-02-24: Completed Stage-1 ExecPlan item 2 by adding `src/tokens/intervals.py` (melodic/harmonic helpers with QA vs production behavior), wiring repairs into `eventize_musicxml`, updating harmonic validation to shared math helpers, and adding `tests/test_intervals.py`; verify run: `53 passed, 1 skipped`.
+- 2026-02-24: Completed Stage-1 ExecPlan item 3 by adding `src/tokens/tokenizer.py` (voice-event parsing/serialization, stream canonicalization helpers), wiring `eventize_musicxml` pitched-onset emission through tokenizer serialization for canonical ordering, and adding `tests/test_tokenizer.py`; verify run: `59 passed, 1 skipped`.
diff --git a/TODO.md b/TODO.md
index da217dc3c318707990396dec3407998602311621..f34ae3002cbb8b57163085ffe73aebe53b830893
--- a/TODO.md
+++ b/TODO.md
@@ -3,6 +3,7 @@
 ## Milestone: Stage-1 Tokenizer & Dataset
 - [x] Implement `src/tokens/schema.py` (EventSpec + DescriptorSpec, versioned)
 - [x] Implement tokenizer interval math + round-trip tests
+- [x] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering
 - [ ] Implement `scripts/make_dataset.py` basic pipeline
 - [ ] Produce `data/processed/{events.parquet,barplans.parquet,stats.json}`
-- [ ] CI: `pytest -q` passes
+- [x] CI: `pytest -q` passes
diff --git a/docs/exec/EXECPLAN_stage1.md b/docs/exec/EXECPLAN_stage1.md
index 7f2647e15e5b673df54c0e21549951d68753a8c6..b8ee585fca521d0ea892fa6b21d13bb879078f5d
--- a/docs/exec/EXECPLAN_stage1.md
+++ b/docs/exec/EXECPLAN_stage1.md
@@ -11,7 +11,7 @@
 ## Checklist (one item per loop)
 - [x] Define EventSpec + DescriptorSpec in `src/tokens/schema.py` with version `remi_tab_v1` and `tpq=24`.
 - [x] Implement interval math helpers (melodic + harmonic ref) with QA vs production behavior.
-- [ ] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering.
+- [x] Implement event parsing/serialization in `src/tokens/tokenizer.py` with canonical ordering.
 - [ ] Add round-trip tests for pitch reconstruction and `HARM_*` consistency.
 - [ ] Implement `scripts/make_dataset.py` basic pipeline (parse, normalize, collapse doublings, track assignment, tokenize, stats).
 - [ ] Produce dataset artifacts in `data/processed/` and document counts in `stats.json`.
diff --git a/src/tokens/eventizer.py b/src/tokens/eventizer.py
index f7c666b524c3f03edbf62558e3a89e26917d8d43..53385718babf181fdc8367c1ca227731a1c1b799
--- a/src/tokens/eventizer.py
+++ b/src/tokens/eventizer.py
@@ -12,9 +12,9 @@
     IntervalRepairStats,
     compute_reference_pitch,
     compute_melodic_interval,
-    format_signed_interval,
     harmonic_tokens_for_pitch,
 )
+from src.tokens.tokenizer import VoiceEvent, serialize_voice_event
 
 DEFAULT_MAX_VOICES = 8
 DEFAULT_MAX_OCTAVES_PER_PITCH_CLASS = 3
@@ -584,15 +584,12 @@
                         tokens.append(f"ABS_VOICE_{v}_{ev.pitch}")
                     prev_pitch[v] = ev.pitch
 
-                tokens.append(f"VOICE_{v}")
-                tokens.append(f"DUR_{ev.duration_tick}")
                 base_pitch = prev_pitch[v] if prev_pitch[v] is not None else ev.pitch
                 mel_int = compute_melodic_interval(
                     ev.pitch,
                     base_pitch,
                     qa_mode=True,
                 )
-                tokens.append(f"MEL_INT12_{format_signed_interval(mel_int)}")
 
                 harm_oct_tok, harm_cls_tok = harmonic_tokens_for_pitch(
                     ev.pitch,
@@ -600,8 +597,21 @@
                     qa_mode=qa_mode,
                     stats=interval_repairs,
                 )
-                tokens.append(harm_oct_tok)
-                tokens.append(harm_cls_tok)
+                harm_oct = None if harm_oct_tok.endswith("_NA") else int(harm_oct_tok.rsplit("_", 1)[1])
+                harm_class = (
+                    None if harm_cls_tok.endswith("_NA") else int(harm_cls_tok.rsplit("_", 1)[1])
+                )
+                tokens.extend(
+                    serialize_voice_event(
+                        VoiceEvent(
+                            voice=v,
+                            duration_ticks=ev.duration_tick,
+                            mel_int=mel_int,
+                            harm_oct=harm_oct,
+                            harm_class=harm_class,
+                        )
+                    )
+                )
 
                 prev_pitch[v] = ev.pitch
                 active_until[v] = t + ev.duration_tick
diff --git a/src/tokens/tokenizer.py b/src/tokens/tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..3d7839be70301fe3c5376ba858263c8c4f66da1b
--- /dev/null
+++ b/src/tokens/tokenizer.py
@@ -0,0 +1,219 @@
+from dataclasses import dataclass
+from typing import List, Sequence, Tuple, Union
+
+from src.tokens.intervals import format_signed_interval
+
+
+@dataclass(frozen=True)
+class VoiceEvent:
+    voice: int
+    duration_ticks: int = 0
+    rest_ticks: int = 0
+    mel_int: int = 0
+    harm_oct: Union[int, None] = None
+    harm_class: Union[int, None] = None
+    dup_count: Union[int, None] = None
+    string: Union[int, None] = None
+    fret: Union[int, None] = None
+
+    def __post_init__(self) -> None:
+        if self.voice < 0:
+            raise ValueError("voice must be >= 0")
+        if self.duration_ticks < 0:
+            raise ValueError("duration_ticks must be >= 0")
+        if self.rest_ticks < 0:
+            raise ValueError("rest_ticks must be >= 0")
+        if self.dup_count is not None and self.dup_count < 1:
+            raise ValueError("dup_count must be >= 1 when set")
+        if (self.string is None) ^ (self.fret is None):
+            raise ValueError("string and fret must both be set or both be None")
+
+        is_rest = self.rest_ticks > 0
+        is_pitched = self.duration_ticks > 0
+        if is_rest == is_pitched:
+            raise ValueError("voice event must be exactly one of rest or pitched")
+
+        if is_rest:
+            if self.dup_count is not None:
+                raise ValueError("rest event cannot set dup_count")
+            if self.harm_oct is not None or self.harm_class is not None:
+                raise ValueError("rest event cannot set harmonic interval fields")
+            if self.string is not None or self.fret is not None:
+                raise ValueError("rest event cannot set tab fields")
+            return
+
+        if (self.harm_oct is None) ^ (self.harm_class is None):
+            raise ValueError("harm_oct and harm_class must both be set or both be None")
+        if self.harm_class is not None and not (0 <= self.harm_class <= 11):
+            raise ValueError("harm_class must be in [0, 11]")
+
+    @property
+    def is_rest(self) -> bool:
+        return self.rest_ticks > 0
+
+
+def tokenize_event_text(text: str) -> List[str]:
+    raw = text.replace("\n", ",")
+    tokens = [tok.strip() for tok in raw.split(",")]
+    return [tok for tok in tokens if tok]
+
+
+def parse_voice_event(tokens: Sequence[str], start_idx: int) -> Tuple[VoiceEvent, int]:
+    if start_idx >= len(tokens):
+        raise ValueError("start_idx out of range")
+
+    voice_tok = tokens[start_idx]
+    if not voice_tok.startswith("VOICE_"):
+        raise ValueError(f"expected VOICE token at index {start_idx}, got {voice_tok!r}")
+
+    voice = _parse_prefixed_int(voice_tok, "VOICE_", start_idx)
+    next_idx = start_idx + 1
+    if next_idx >= len(tokens):
+        raise ValueError(f"truncated VOICE event at index {start_idx}")
+
+    first_tok = tokens[next_idx]
+    if first_tok.startswith("REST_"):
+        rest_ticks = _parse_prefixed_int(first_tok, "REST_", next_idx)
+        return VoiceEvent(voice=voice, rest_ticks=rest_ticks), next_idx + 1
+
+    duration_ticks = _parse_prefixed_int(first_tok, "DUR_", next_idx)
+    next_idx += 1
+
+    dup_count = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("DUP_"):
+        dup_count = _parse_prefixed_int(tokens[next_idx], "DUP_", next_idx)
+        next_idx += 1
+
+    if next_idx + 2 >= len(tokens):
+        raise ValueError(f"truncated pitched VOICE event at index {start_idx}")
+
+    mel_int = _parse_signed_mel(tokens[next_idx], next_idx)
+    harm_oct = _parse_harm_oct(tokens[next_idx + 1], next_idx + 1)
+    harm_class = _parse_harm_class(tokens[next_idx + 2], next_idx + 2)
+    next_idx += 3
+
+    string = None
+    fret = None
+    if next_idx < len(tokens) and tokens[next_idx].startswith("STR_"):
+        string = _parse_prefixed_int(tokens[next_idx], "STR_", next_idx)
+        next_idx += 1
+        if next_idx >= len(tokens) or not tokens[next_idx].startswith("FRET_"):
+            raise ValueError(f"missing FRET token after STR at index {next_idx}")
+        fret = _parse_prefixed_int(tokens[next_idx], "FRET_", next_idx)
+        next_idx += 1
+    elif next_idx < len(tokens) and tokens[next_idx].startswith("FRET_"):
+        raise ValueError(f"FRET token must follow STR token at index {next_idx}")
+
+    return (
+        VoiceEvent(
+            voice=voice,
+            duration_ticks=duration_ticks,
+            mel_int=mel_int,
+            harm_oct=harm_oct,
+            harm_class=harm_class,
+            dup_count=dup_count,
+            string=string,
+            fret=fret,
+        ),
+        next_idx,
+    )
+
+
+def serialize_voice_event(event: VoiceEvent) -> List[str]:
+    tokens = [f"VOICE_{event.voice}"]
+    if event.is_rest:
+        tokens.append(f"REST_{event.rest_ticks}")
+        return tokens
+
+    tokens.append(f"DUR_{event.duration_ticks}")
+    if event.dup_count is not None:
+        tokens.append(f"DUP_{event.dup_count}")
+
+    tokens.append(f"MEL_INT12_{format_signed_interval(event.mel_int)}")
+    if event.harm_oct is None:
+        tokens.append("HARM_OCT_NA")
+        tokens.append("HARM_CLASS_NA")
+    else:
+        tokens.append(f"HARM_OCT_{event.harm_oct}")
+        tokens.append(f"HARM_CLASS_{event.harm_class}")
+
+    if event.string is not None:
+        tokens.append(f"STR_{event.string}")
+        tokens.append(f"FRET_{event.fret}")
+
+    return tokens
+
+
+def parse_event_stream(tokens: Sequence[str]) -> List[Union[str, VoiceEvent]]:
+    parsed: List[Union[str, VoiceEvent]] = []
+    idx = 0
+    while idx < len(tokens):
+        tok = tokens[idx]
+        if tok.startswith("VOICE_"):
+            event, idx = parse_voice_event(tokens, idx)
+            parsed.append(event)
+            continue
+        parsed.append(tok)
+        idx += 1
+    return parsed
+
+
+def serialize_event_stream(items: Sequence[Union[str, VoiceEvent]]) -> List[str]:
+    tokens: List[str] = []
+    for item in items:
+        if isinstance(item, VoiceEvent):
+            tokens.extend(serialize_voice_event(item))
+        else:
+            tokens.append(item)
+    return tokens
+
+
+def canonicalize_event_stream(tokens: Sequence[str]) -> List[str]:
+    return serialize_event_stream(parse_event_stream(tokens))
+
+
+def _parse_prefixed_int(token: str, prefix: str, idx: int) -> int:
+    if not token.startswith(prefix):
+        raise ValueError(f"expected {prefix} token at index {idx}, got {token!r}")
+    value = token[len(prefix) :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad integer token at index {idx}: {token!r}") from exc
+
+
+def _parse_signed_mel(token: str, idx: int) -> int:
+    if not token.startswith("MEL_INT12_"):
+        raise ValueError(f"expected MEL_INT12 token at index {idx}, got {token!r}")
+    value = token[len("MEL_INT12_") :]
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad MEL_INT12 token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_oct(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_OCT_"):
+        raise ValueError(f"expected HARM_OCT token at index {idx}, got {token!r}")
+    value = token[len("HARM_OCT_") :]
+    if value == "NA":
+        return None
+    try:
+        return int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_OCT token at index {idx}: {token!r}") from exc
+
+
+def _parse_harm_class(token: str, idx: int) -> Union[int, None]:
+    if not token.startswith("HARM_CLASS_"):
+        raise ValueError(f"expected HARM_CLASS token at index {idx}, got {token!r}")
+    value = token[len("HARM_CLASS_") :]
+    if value == "NA":
+        return None
+    try:
+        parsed = int(value)
+    except ValueError as exc:
+        raise ValueError(f"bad HARM_CLASS token at index {idx}: {token!r}") from exc
+    if not (0 <= parsed <= 11):
+        raise ValueError(f"HARM_CLASS out of range at index {idx}: {token!r}")
+    return parsed
diff --git a/tests/test_tokenizer.py b/tests/test_tokenizer.py
new file mode 100644
index 0000000000000000000000000000000000000000..4f4741f7ebc6a851a695b5f6081e10cfc2c03f03
--- /dev/null
+++ b/tests/test_tokenizer.py
@@ -0,0 +1,107 @@
+import sys
+from pathlib import Path
+
+import pytest
+
+sys.path.insert(0, str(Path(__file__).parents[1]))
+
+from src.tokens.tokenizer import (
+    VoiceEvent,
+    canonicalize_event_stream,
+    parse_event_stream,
+    parse_voice_event,
+    serialize_event_stream,
+    serialize_voice_event,
+    tokenize_event_text,
+)
+
+
+def test_parse_pitched_voice_event_with_dup_and_tab():
+    tokens = [
+        "VOICE_2",
+        "DUR_12",
+        "DUP_3",
+        "MEL_INT12_+7",
+        "HARM_OCT_1",
+        "HARM_CLASS_7",
+        "STR_4",
+        "FRET_2",
+        "POS_6",
+    ]
+
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 8
+    assert event.voice == 2
+    assert event.duration_ticks == 12
+    assert event.dup_count == 3
+    assert event.mel_int == 7
+    assert event.harm_oct == 1
+    assert event.harm_class == 7
+    assert event.string == 4
+    assert event.fret == 2
+
+
+def test_parse_rest_voice_event():
+    tokens = ["VOICE_1", "REST_18", "BAR"]
+    event, next_idx = parse_voice_event(tokens, 0)
+    assert next_idx == 2
+    assert event.voice == 1
+    assert event.is_rest
+    assert event.rest_ticks == 18
+
+
+def test_serialize_voice_event_uses_canonical_order():
+    event = VoiceEvent(
+        voice=3,
+        duration_ticks=24,
+        dup_count=2,
+        mel_int=11,
+        harm_oct=0,
+        harm_class=11,
+    )
+    assert serialize_voice_event(event) == [
+        "VOICE_3",
+        "DUR_24",
+        "DUP_2",
+        "MEL_INT12_+11",
+        "HARM_OCT_0",
+        "HARM_CLASS_11",
+    ]
+
+
+def test_canonicalize_event_stream_normalizes_mel_sign():
+    tokens = [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+    assert canonicalize_event_stream(tokens) == [
+        "BAR",
+        "POS_0",
+        "VOICE_0",
+        "DUR_6",
+        "MEL_INT12_+2",
+        "HARM_OCT_0",
+        "HARM_CLASS_2",
+        "VOICE_1",
+        "REST_3",
+    ]
+
+
+def test_parse_event_stream_roundtrip_and_text_tokenization():
+    text = "BAR, POS_0, VOICE_0, DUR_4, MEL_INT12_0, HARM_OCT_NA, HARM_CLASS_NA"
+    tokens = tokenize_event_text(text)
+    parsed = parse_event_stream(tokens)
+    assert serialize_event_stream(parsed) == tokens
+
+
+def test_parse_rejects_mixed_harm_na_state():
+    tokens = ["VOICE_0", "DUR_8", "MEL_INT12_0", "HARM_OCT_NA", "HARM_CLASS_2"]
+    with pytest.raises(ValueError, match="harm_oct and harm_class must both be set or both be None"):
+        parse_voice_event(tokens, 0)

tokens used
68,751
