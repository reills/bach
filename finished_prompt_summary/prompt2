P02 summary:
- Kept the existing FastAPI backend skeleton in `src/api`, with `create_app()` registering the `/healthz` router.
- Updated `src/api/routes/health.py` so the health endpoint is an async route returning `{\"status\": \"ok\"}`.
- Reworked `tests/test_api_health.py` to hit the app through `httpx.ASGITransport` after reproducing a `fastapi.testclient.TestClient` deadlock in the installed Starlette/AnyIO stack.
- Appended a new `PROGRESS.md` entry with the real verification result.
- Ran `bash docs/skills/python-test-env/scripts/run_tests.sh -- tests/test_api_health.py` and got `1 passed in 0.20s`.
